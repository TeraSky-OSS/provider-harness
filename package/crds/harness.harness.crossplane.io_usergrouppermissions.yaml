---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: usergrouppermissions.harness.harness.crossplane.io
spec:
  group: harness.harness.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - harness
    kind: UserGroupPermissions
    listKind: UserGroupPermissionsList
    plural: usergrouppermissions
    singular: usergrouppermissions
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UserGroupPermissions is the Schema for the UserGroupPermissionss
          API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UserGroupPermissionsSpec defines the desired state of UserGroupPermissions
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  accountPermissions:
                    description: The account permissions of the user group. Valid
                      options are ADMINISTER_OTHER_ACCOUNT_FUNCTIONS, CREATE_AND_DELETE_APPLICATION,
                      CREATE_CUSTOM_DASHBOARDS, MANAGE_ALERT_NOTIFICATION_RULES, MANAGE_API_KEYS,
                      MANAGE_APPLICATION_STACKS, MANAGE_AUTHENTICATION_SETTINGS, MANAGE_CLOUD_PROVIDERS,
                      MANAGE_CONFIG_AS_CODE, MANAGE_CONNECTORS, MANAGE_CUSTOM_DASHBOARDS,
                      MANAGE_DELEGATE_PROFILES, MANAGE_DELEGATES, MANAGE_DEPLOYMENT_FREEZES,
                      MANAGE_IP_WHITELIST, MANAGE_PIPELINE_GOVERNANCE_STANDARDS, MANAGE_RESTRICTED_ACCESS,
                      MANAGE_SECRET_MANAGERS, MANAGE_SECRETS, MANAGE_SSH_AND_WINRM,
                      MANAGE_TAGS, MANAGE_TEMPLATE_LIBRARY, MANAGE_USER_AND_USER_GROUPS_AND_API_KEYS,
                      MANAGE_USERS_AND_GROUPS, READ_USERS_AND_GROUPS, VIEW_AUDITS,
                      VIEW_USER_AND_USER_GROUPS_AND_API_KEYS
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  appPermissions:
                    description: Application specific permissions
                    items:
                      properties:
                        all:
                          description: The permission to perform actions against all
                            resources.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are CREATE, READ, UPDATE, DELETE,
                                  EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        deployment:
                          description: Permission configuration to perform actions
                            against deployments.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are READ, EXECUTE_WORKFLOW, EXECUTE_PIPELINE,
                                  ROLLBACK_WORKFLOW, ABORT_WORKFLOW
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              envIds:
                                description: The environment IDs to which the permission
                                  applies. Leave empty to apply to all environments.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              filters:
                                description: 'The filters to apply to the action.
                                  Valid options are: NON_PRODUCTION_ENVIRONMENTS,
                                  PRODUCTION_ENVIRONMENTS.'
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        environment:
                          description: Permission configuration to perform actions
                            against workflows.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are CREATE, READ, UPDATE, DELETE
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              envIds:
                                description: The environment IDs to which the permission
                                  applies. Leave empty to apply to all environments.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              filters:
                                description: 'The filters to apply to the action.
                                  Valid options are: NON_PRODUCTION_ENVIRONMENTS,
                                  PRODUCTION_ENVIRONMENTS.'
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        pipeline:
                          description: Permission configuration to perform actions
                            against pipelines.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are [CREATE READ UPDATE DELETE]
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              envIds:
                                description: The environment IDs to which the permission
                                  applies. Leave empty to apply to all environments.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              filters:
                                description: 'The filters to apply to the action.
                                  Valid options are: NON_PRODUCTION_PIPELINES, PRODUCTION_PIPELINES.'
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        provisioner:
                          description: Permission configuration to perform actions
                            against provisioners.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are CREATE, READ, UPDATE, DELETE
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              provisionerIds:
                                description: The provisioner IDs to which the permission
                                  applies. Leave empty to apply to all provisioners.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        service:
                          description: Permission configuration to perform actions
                            against services.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are CREATE, READ, UPDATE, DELETE
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              serviceIds:
                                description: The service IDs to which the permission
                                  applies. Leave empty to apply to all services.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        template:
                          description: Permission configuration to perform actions
                            against templates.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are CREATE, READ, UPDATE, DELETE
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              templateIds:
                                description: The template IDs to which the permission
                                  applies. Leave empty to apply to all environments.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        workflow:
                          description: Permission configuration to perform actions
                            against workflows.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are CREATE, READ, UPDATE, DELETE
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              filters:
                                description: 'The filters to apply to the action.
                                  Valid options are: NON_PRODUCTION_WORKFLOWS, PRODUCTION_WORKFLOWS,
                                  WORKFLOW_TEMPLATES.'
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                      type: object
                    type: array
                  userGroupId:
                    description: Unique identifier of the user group.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accountPermissions:
                    description: The account permissions of the user group. Valid
                      options are ADMINISTER_OTHER_ACCOUNT_FUNCTIONS, CREATE_AND_DELETE_APPLICATION,
                      CREATE_CUSTOM_DASHBOARDS, MANAGE_ALERT_NOTIFICATION_RULES, MANAGE_API_KEYS,
                      MANAGE_APPLICATION_STACKS, MANAGE_AUTHENTICATION_SETTINGS, MANAGE_CLOUD_PROVIDERS,
                      MANAGE_CONFIG_AS_CODE, MANAGE_CONNECTORS, MANAGE_CUSTOM_DASHBOARDS,
                      MANAGE_DELEGATE_PROFILES, MANAGE_DELEGATES, MANAGE_DEPLOYMENT_FREEZES,
                      MANAGE_IP_WHITELIST, MANAGE_PIPELINE_GOVERNANCE_STANDARDS, MANAGE_RESTRICTED_ACCESS,
                      MANAGE_SECRET_MANAGERS, MANAGE_SECRETS, MANAGE_SSH_AND_WINRM,
                      MANAGE_TAGS, MANAGE_TEMPLATE_LIBRARY, MANAGE_USER_AND_USER_GROUPS_AND_API_KEYS,
                      MANAGE_USERS_AND_GROUPS, READ_USERS_AND_GROUPS, VIEW_AUDITS,
                      VIEW_USER_AND_USER_GROUPS_AND_API_KEYS
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  appPermissions:
                    description: Application specific permissions
                    items:
                      properties:
                        all:
                          description: The permission to perform actions against all
                            resources.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are CREATE, READ, UPDATE, DELETE,
                                  EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        deployment:
                          description: Permission configuration to perform actions
                            against deployments.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are READ, EXECUTE_WORKFLOW, EXECUTE_PIPELINE,
                                  ROLLBACK_WORKFLOW, ABORT_WORKFLOW
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              envIds:
                                description: The environment IDs to which the permission
                                  applies. Leave empty to apply to all environments.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              filters:
                                description: 'The filters to apply to the action.
                                  Valid options are: NON_PRODUCTION_ENVIRONMENTS,
                                  PRODUCTION_ENVIRONMENTS.'
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        environment:
                          description: Permission configuration to perform actions
                            against workflows.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are CREATE, READ, UPDATE, DELETE
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              envIds:
                                description: The environment IDs to which the permission
                                  applies. Leave empty to apply to all environments.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              filters:
                                description: 'The filters to apply to the action.
                                  Valid options are: NON_PRODUCTION_ENVIRONMENTS,
                                  PRODUCTION_ENVIRONMENTS.'
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        pipeline:
                          description: Permission configuration to perform actions
                            against pipelines.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are [CREATE READ UPDATE DELETE]
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              envIds:
                                description: The environment IDs to which the permission
                                  applies. Leave empty to apply to all environments.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              filters:
                                description: 'The filters to apply to the action.
                                  Valid options are: NON_PRODUCTION_PIPELINES, PRODUCTION_PIPELINES.'
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        provisioner:
                          description: Permission configuration to perform actions
                            against provisioners.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are CREATE, READ, UPDATE, DELETE
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              provisionerIds:
                                description: The provisioner IDs to which the permission
                                  applies. Leave empty to apply to all provisioners.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        service:
                          description: Permission configuration to perform actions
                            against services.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are CREATE, READ, UPDATE, DELETE
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              serviceIds:
                                description: The service IDs to which the permission
                                  applies. Leave empty to apply to all services.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        template:
                          description: Permission configuration to perform actions
                            against templates.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are CREATE, READ, UPDATE, DELETE
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              templateIds:
                                description: The template IDs to which the permission
                                  applies. Leave empty to apply to all environments.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        workflow:
                          description: Permission configuration to perform actions
                            against workflows.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are CREATE, READ, UPDATE, DELETE
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              filters:
                                description: 'The filters to apply to the action.
                                  Valid options are: NON_PRODUCTION_WORKFLOWS, PRODUCTION_WORKFLOWS,
                                  WORKFLOW_TEMPLATES.'
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                      type: object
                    type: array
                  userGroupId:
                    description: Unique identifier of the user group.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.userGroupId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.userGroupId)
                || (has(self.initProvider) && has(self.initProvider.userGroupId))'
          status:
            description: UserGroupPermissionsStatus defines the observed state of
              UserGroupPermissions.
            properties:
              atProvider:
                properties:
                  accountPermissions:
                    description: The account permissions of the user group. Valid
                      options are ADMINISTER_OTHER_ACCOUNT_FUNCTIONS, CREATE_AND_DELETE_APPLICATION,
                      CREATE_CUSTOM_DASHBOARDS, MANAGE_ALERT_NOTIFICATION_RULES, MANAGE_API_KEYS,
                      MANAGE_APPLICATION_STACKS, MANAGE_AUTHENTICATION_SETTINGS, MANAGE_CLOUD_PROVIDERS,
                      MANAGE_CONFIG_AS_CODE, MANAGE_CONNECTORS, MANAGE_CUSTOM_DASHBOARDS,
                      MANAGE_DELEGATE_PROFILES, MANAGE_DELEGATES, MANAGE_DEPLOYMENT_FREEZES,
                      MANAGE_IP_WHITELIST, MANAGE_PIPELINE_GOVERNANCE_STANDARDS, MANAGE_RESTRICTED_ACCESS,
                      MANAGE_SECRET_MANAGERS, MANAGE_SECRETS, MANAGE_SSH_AND_WINRM,
                      MANAGE_TAGS, MANAGE_TEMPLATE_LIBRARY, MANAGE_USER_AND_USER_GROUPS_AND_API_KEYS,
                      MANAGE_USERS_AND_GROUPS, READ_USERS_AND_GROUPS, VIEW_AUDITS,
                      VIEW_USER_AND_USER_GROUPS_AND_API_KEYS
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  appPermissions:
                    description: Application specific permissions
                    items:
                      properties:
                        all:
                          description: The permission to perform actions against all
                            resources.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are CREATE, READ, UPDATE, DELETE,
                                  EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        deployment:
                          description: Permission configuration to perform actions
                            against deployments.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are READ, EXECUTE_WORKFLOW, EXECUTE_PIPELINE,
                                  ROLLBACK_WORKFLOW, ABORT_WORKFLOW
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              envIds:
                                description: The environment IDs to which the permission
                                  applies. Leave empty to apply to all environments.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              filters:
                                description: 'The filters to apply to the action.
                                  Valid options are: NON_PRODUCTION_ENVIRONMENTS,
                                  PRODUCTION_ENVIRONMENTS.'
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        environment:
                          description: Permission configuration to perform actions
                            against workflows.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are CREATE, READ, UPDATE, DELETE
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              envIds:
                                description: The environment IDs to which the permission
                                  applies. Leave empty to apply to all environments.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              filters:
                                description: 'The filters to apply to the action.
                                  Valid options are: NON_PRODUCTION_ENVIRONMENTS,
                                  PRODUCTION_ENVIRONMENTS.'
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        pipeline:
                          description: Permission configuration to perform actions
                            against pipelines.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are [CREATE READ UPDATE DELETE]
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              envIds:
                                description: The environment IDs to which the permission
                                  applies. Leave empty to apply to all environments.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              filters:
                                description: 'The filters to apply to the action.
                                  Valid options are: NON_PRODUCTION_PIPELINES, PRODUCTION_PIPELINES.'
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        provisioner:
                          description: Permission configuration to perform actions
                            against provisioners.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are CREATE, READ, UPDATE, DELETE
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              provisionerIds:
                                description: The provisioner IDs to which the permission
                                  applies. Leave empty to apply to all provisioners.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        service:
                          description: Permission configuration to perform actions
                            against services.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are CREATE, READ, UPDATE, DELETE
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              serviceIds:
                                description: The service IDs to which the permission
                                  applies. Leave empty to apply to all services.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        template:
                          description: Permission configuration to perform actions
                            against templates.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are CREATE, READ, UPDATE, DELETE
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              templateIds:
                                description: The template IDs to which the permission
                                  applies. Leave empty to apply to all environments.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        workflow:
                          description: Permission configuration to perform actions
                            against workflows.
                          items:
                            properties:
                              actions:
                                description: The actions allowed to be performed.
                                  Valid options are CREATE, READ, UPDATE, DELETE
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              appIds:
                                description: The application IDs to which the permission
                                  applies. Leave empty to apply to all applications.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              filters:
                                description: 'The filters to apply to the action.
                                  Valid options are: NON_PRODUCTION_WORKFLOWS, PRODUCTION_WORKFLOWS,
                                  WORKFLOW_TEMPLATES.'
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                      type: object
                    type: array
                  id:
                    type: string
                  userGroupId:
                    description: Unique identifier of the user group.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
