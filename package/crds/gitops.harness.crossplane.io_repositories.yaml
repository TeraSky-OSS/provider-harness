---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: repositories.gitops.harness.crossplane.io
spec:
  group: gitops.harness.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - harness
    kind: Repository
    listKind: RepositoryList
    plural: repositories
    singular: repository
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Repository is the Schema for the Repositorys API. Resource for
          managing Harness Gitops Repository.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RepositorySpec defines the desired state of Repository
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  accountId:
                    description: |-
                      (String, Deprecated) Account identifier of the GitOps repository.
                      Account identifier of the GitOps repository.
                    type: string
                  agentId:
                    description: |-
                      (String) Agent identifier of the GitOps repository.
                      Agent identifier of the GitOps repository.
                    type: string
                  agentIdRef:
                    description: Reference to a Agent in gitops to populate agentId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  agentIdSelector:
                    description: Selector for a Agent in gitops to populate agentId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  credsOnly:
                    description: |-
                      (Boolean) Indicates if to operate on credential set instead of repository.
                      Indicates if to operate on credential set instead of repository.
                    type: boolean
                  ecrGen:
                    description: |-
                      (Block List, Max: 1) ECR access token generator specific configuration. (see below for nested schema)
                      ECR access token generator specific configuration.
                    items:
                      properties:
                        jwtAuth:
                          description: |-
                            (Block List, Max: 1) JWT authentication specific configuration. (see below for nested schema)
                            JWT authentication specific configuration.
                          items:
                            properties:
                              audiences:
                                description: |-
                                  known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
                                  Audience specifies the `aud` claim for the service account token If the service account uses a well-known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
                                items:
                                  type: string
                                type: array
                              name:
                                description: |-
                                  (String) Name to be used for this repo. Only used with Helm repos.
                                  The name of the ServiceAccount resource being referred to.
                                type: string
                              namespace:
                                description: |-
                                  scoped. cluster-scoped defaults to the namespace of the referent.
                                  Namespace of the resource being referred to. Ignored if referent is not cluster-scoped. cluster-scoped defaults to the namespace of the referent.
                                type: string
                            type: object
                          type: array
                        region:
                          description: |-
                            (String) AWS region.
                            AWS region.
                          type: string
                        secretRef:
                          description: |-
                            (Block List, Max: 1) Secret reference to the AWS credentials. (see below for nested schema)
                            Secret reference to the AWS credentials.
                          items:
                            properties:
                              awsAccessKeyId:
                                description: |-
                                  (String) AWS access key id.
                                  AWS access key id.
                                type: string
                              awsSecretAccessKeySecretRef:
                                description: |-
                                  (String, Sensitive) AWS secret access key.
                                  AWS secret access key.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              awsSessionTokenSecretRef:
                                description: |-
                                  (String, Sensitive) AWS session token.
                                  AWS session token.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          type: array
                      type: object
                    type: array
                  forceDelete:
                    description: |-
                      (Boolean) Indicates if the repository should be deleted forcefully, regardless of existing applications using that repo.
                      Indicates if the repository should be deleted forcefully, regardless of existing applications using that repo.
                    type: boolean
                  gcrGen:
                    description: |-
                      (Block List, Max: 1) GCR access token generator specific configuration. (see below for nested schema)
                      GCR access token generator specific configuration.
                    items:
                      properties:
                        accessKeySecretRef:
                          description: |-
                            (String, Sensitive) GCP access key.
                            GCP access key.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        projectId:
                          description: |-
                            (String) Project identifier of the GitOps repository.
                            GCP project id.
                          type: string
                        workloadIdentity:
                          description: |-
                            (Block List, Max: 1) GCP workload identity. (see below for nested schema)
                            GCP workload identity.
                          items:
                            properties:
                              clusterLocation:
                                description: |-
                                  (String) Cluster location.
                                  Cluster location.
                                type: string
                              clusterName:
                                description: |-
                                  (String) Cluster name.
                                  Cluster name.
                                type: string
                              clusterProjectId:
                                description: |-
                                  (String) Cluster project id.
                                  Cluster project id.
                                type: string
                              serviceAccountRef:
                                description: |-
                                  (Block List, Max: 1) Service account reference. (see below for nested schema)
                                  Service account reference.
                                items:
                                  properties:
                                    audiences:
                                      description: |-
                                        known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
                                        Audience specifies the `aud` claim for the service account token If the service account uses a well-known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
                                      items:
                                        type: string
                                      type: array
                                    name:
                                      description: |-
                                        (String) Name to be used for this repo. Only used with Helm repos.
                                        The name of the ServiceAccount resource being referred to.
                                      type: string
                                    namespace:
                                      description: |-
                                        scoped. cluster-scoped defaults to the namespace of the referent.
                                        Namespace of the resource being referred to. Ignored if referent is not cluster-scoped. cluster-scoped defaults to the namespace of the referent.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  genType:
                    description: |-
                      (String) Default: "UNSET"
                      Enum: "UNSET" "AWS_ECR" "GOOGLE_GCR"
                      Default: "UNSET"
                      Enum: "UNSET" "AWS_ECR" "GOOGLE_GCR"
                    type: string
                  identifier:
                    description: |-
                      (String) Identifier of the GitOps repository.
                      Identifier of the GitOps repository.
                    type: string
                  orgId:
                    description: |-
                      (String) Organization identifier of the GitOps repository.
                      Organization identifier of the GitOps repository.
                    type: string
                  orgIdRef:
                    description: Reference to a Organization in platform to populate
                      orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  orgIdSelector:
                    description: Selector for a Organization in platform to populate
                      orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  projectId:
                    description: |-
                      (String) Project identifier of the GitOps repository.
                      Project identifier of the GitOps repository.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in platform to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in platform to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  refreshInterval:
                    description: |-
                      (String) For OCI repos, this is the interval to refresh the token to access the registry.
                      For OCI repos, this is the interval to refresh the token to access the registry.
                    type: string
                  repo:
                    description: |-
                      (Block List, Min: 1, Max: 1) Repo details holding application configurations. (see below for nested schema)
                      Repo details holding application configurations.
                    items:
                      properties:
                        connectionType:
                          description: |-
                            (String) Identifies the authentication method used to connect to the repository. Possible values: "HTTPS" "SSH" "GITHUB" "HTTPS_ANONYMOUS", "GITHUB_ENTERPRISE".
                            Identifies the authentication method used to connect to the repository. Possible values: "HTTPS" "SSH" "GITHUB" "HTTPS_ANONYMOUS", "GITHUB_ENTERPRISE".
                          type: string
                        enableLfs:
                          description: |-
                            lfs support must be enabled for this repo. This is valid only for Git repositories.
                            Indicates if git-lfs support must be enabled for this repo. This is valid only for Git repositories.
                          type: boolean
                        enableOci:
                          description: |-
                            oci support must be enabled for this repo.
                            Indicates if helm-oci support must be enabled for this repo.
                          type: boolean
                        githubAppEnterpriseBaseUrl:
                          description: |-
                            (String) Base URL of GitHub Enterprise installation. If left empty, this defaults to https://api.github.com.
                            Base URL of GitHub Enterprise installation. If left empty, this defaults to https://api.github.com.
                          type: string
                        githubAppIdSecretRef:
                          description: |-
                            (String, Sensitive) Id of the GitHub app used to access the repo.
                            Id of the GitHub app used to access the repo.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        githubAppInstallationIdSecretRef:
                          description: |-
                            (String, Sensitive) Installation id of the GitHub app used to access the repo.
                            Installation id of the GitHub app used to access the repo.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        githubAppPrivateKeySecretRef:
                          description: |-
                            (String, Sensitive) GitHub app private key PEM data.
                            GitHub app private key PEM data.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        insecure:
                          description: |-
                            (Boolean) Indicates if the connection to the repository ignores any errors when verifying TLS certificates or SSH host keys.
                            Indicates if the connection to the repository ignores any errors when verifying TLS certificates or SSH host keys.
                          type: boolean
                        insecureIgnoreHostKey:
                          description: |-
                            (Boolean) Indicates if InsecureIgnoreHostKey should be used. Insecure is favored used only for git repos. Deprecated.
                            Indicates if InsecureIgnoreHostKey should be used. Insecure is favored used only for git repos. Deprecated.
                          type: boolean
                        name:
                          description: |-
                            (String) Name to be used for this repo. Only used with Helm repos.
                            Name to be used for this repo. Only used with Helm repos.
                          type: string
                        passwordSecretRef:
                          description: |-
                            (String, Sensitive) Password or PAT to be used for authenticating the remote repository.
                            Password or PAT to be used for authenticating the remote repository.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        project:
                          description: |-
                            (String) The ArgoCD project name corresponding to this GitOps repository. An empty string means that the GitOps repository belongs to the default project created by Harness.
                            The ArgoCD project name corresponding to this GitOps repository. An empty string means that the GitOps repository belongs to the default project created by Harness.
                          type: string
                        proxy:
                          description: |-
                            (String) The HTTP/HTTPS proxy used to access the repo.
                            The HTTP/HTTPS proxy used to access the repo.
                          type: string
                        repo:
                          description: |-
                            (Block List, Min: 1, Max: 1) Repo details holding application configurations. (see below for nested schema)
                            URL to the remote repository.
                          type: string
                        sshPrivateKeySecretRef:
                          description: |-
                            (String, Sensitive) SSH Key in PEM format for authenticating the repository. Used only for Git repository.
                            SSH Key in PEM format for authenticating the repository. Used only for Git repository.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        tlsClientCertDataSecretRef:
                          description: |-
                            (String, Sensitive) Certificate in PEM format for authenticating at the repo server. This is used for mTLS. The value should be base64 encoded.
                            Certificate in PEM format for authenticating at the repo server. This is used for mTLS. The value should be base64 encoded.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        tlsClientCertKeySecretRef:
                          description: |-
                            (String, Sensitive) Private key in PEM format for authenticating at the repo server. This is used for mTLS. The value should be base64 encoded.
                            Private key in PEM format for authenticating at the repo server. This is used for mTLS. The value should be base64 encoded.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        type:
                          description: |-
                            (String) Type specifies the type of the repo. Can be either "git" or "helm. "git" is assumed if empty or absent.
                            Type specifies the type of the repo. Can be either "git" or "helm. "git" is assumed if empty or absent.
                          type: string
                        username:
                          description: |-
                            (String) Username to be used for authenticating the remote repository.
                            Username to be used for authenticating the remote repository.
                          type: string
                      type: object
                    type: array
                  updateMask:
                    description: |-
                      (Block List, Deprecated) Update mask of the repository. (see below for nested schema)
                      Update mask of the repository.
                    items:
                      properties:
                        paths:
                          description: |-
                            (List of String) The set of field mask paths.
                            The set of field mask paths.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  upsert:
                    description: |-
                      (Boolean) Indicates if the GitOps repository should be updated if existing and inserted if not.
                      Indicates if the GitOps repository should be updated if existing and inserted if not.
                    type: boolean
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accountId:
                    description: |-
                      (String, Deprecated) Account identifier of the GitOps repository.
                      Account identifier of the GitOps repository.
                    type: string
                  agentId:
                    description: |-
                      (String) Agent identifier of the GitOps repository.
                      Agent identifier of the GitOps repository.
                    type: string
                  agentIdRef:
                    description: Reference to a Agent in gitops to populate agentId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  agentIdSelector:
                    description: Selector for a Agent in gitops to populate agentId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  credsOnly:
                    description: |-
                      (Boolean) Indicates if to operate on credential set instead of repository.
                      Indicates if to operate on credential set instead of repository.
                    type: boolean
                  ecrGen:
                    description: |-
                      (Block List, Max: 1) ECR access token generator specific configuration. (see below for nested schema)
                      ECR access token generator specific configuration.
                    items:
                      properties:
                        jwtAuth:
                          description: |-
                            (Block List, Max: 1) JWT authentication specific configuration. (see below for nested schema)
                            JWT authentication specific configuration.
                          items:
                            properties:
                              audiences:
                                description: |-
                                  known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
                                  Audience specifies the `aud` claim for the service account token If the service account uses a well-known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
                                items:
                                  type: string
                                type: array
                              name:
                                description: |-
                                  (String) Name to be used for this repo. Only used with Helm repos.
                                  The name of the ServiceAccount resource being referred to.
                                type: string
                              namespace:
                                description: |-
                                  scoped. cluster-scoped defaults to the namespace of the referent.
                                  Namespace of the resource being referred to. Ignored if referent is not cluster-scoped. cluster-scoped defaults to the namespace of the referent.
                                type: string
                            type: object
                          type: array
                        region:
                          description: |-
                            (String) AWS region.
                            AWS region.
                          type: string
                        secretRef:
                          description: |-
                            (Block List, Max: 1) Secret reference to the AWS credentials. (see below for nested schema)
                            Secret reference to the AWS credentials.
                          items:
                            properties:
                              awsAccessKeyId:
                                description: |-
                                  (String) AWS access key id.
                                  AWS access key id.
                                type: string
                              awsSecretAccessKeySecretRef:
                                description: |-
                                  (String, Sensitive) AWS secret access key.
                                  AWS secret access key.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                              awsSessionTokenSecretRef:
                                description: |-
                                  (String, Sensitive) AWS session token.
                                  AWS session token.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                  namespace:
                                    description: Namespace of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                - namespace
                                type: object
                            type: object
                          type: array
                      type: object
                    type: array
                  forceDelete:
                    description: |-
                      (Boolean) Indicates if the repository should be deleted forcefully, regardless of existing applications using that repo.
                      Indicates if the repository should be deleted forcefully, regardless of existing applications using that repo.
                    type: boolean
                  gcrGen:
                    description: |-
                      (Block List, Max: 1) GCR access token generator specific configuration. (see below for nested schema)
                      GCR access token generator specific configuration.
                    items:
                      properties:
                        accessKeySecretRef:
                          description: |-
                            (String, Sensitive) GCP access key.
                            GCP access key.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        projectId:
                          description: |-
                            (String) Project identifier of the GitOps repository.
                            GCP project id.
                          type: string
                        workloadIdentity:
                          description: |-
                            (Block List, Max: 1) GCP workload identity. (see below for nested schema)
                            GCP workload identity.
                          items:
                            properties:
                              clusterLocation:
                                description: |-
                                  (String) Cluster location.
                                  Cluster location.
                                type: string
                              clusterName:
                                description: |-
                                  (String) Cluster name.
                                  Cluster name.
                                type: string
                              clusterProjectId:
                                description: |-
                                  (String) Cluster project id.
                                  Cluster project id.
                                type: string
                              serviceAccountRef:
                                description: |-
                                  (Block List, Max: 1) Service account reference. (see below for nested schema)
                                  Service account reference.
                                items:
                                  properties:
                                    audiences:
                                      description: |-
                                        known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
                                        Audience specifies the `aud` claim for the service account token If the service account uses a well-known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
                                      items:
                                        type: string
                                      type: array
                                    name:
                                      description: |-
                                        (String) Name to be used for this repo. Only used with Helm repos.
                                        The name of the ServiceAccount resource being referred to.
                                      type: string
                                    namespace:
                                      description: |-
                                        scoped. cluster-scoped defaults to the namespace of the referent.
                                        Namespace of the resource being referred to. Ignored if referent is not cluster-scoped. cluster-scoped defaults to the namespace of the referent.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  genType:
                    description: |-
                      (String) Default: "UNSET"
                      Enum: "UNSET" "AWS_ECR" "GOOGLE_GCR"
                      Default: "UNSET"
                      Enum: "UNSET" "AWS_ECR" "GOOGLE_GCR"
                    type: string
                  identifier:
                    description: |-
                      (String) Identifier of the GitOps repository.
                      Identifier of the GitOps repository.
                    type: string
                  orgId:
                    description: |-
                      (String) Organization identifier of the GitOps repository.
                      Organization identifier of the GitOps repository.
                    type: string
                  orgIdRef:
                    description: Reference to a Organization in platform to populate
                      orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  orgIdSelector:
                    description: Selector for a Organization in platform to populate
                      orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  projectId:
                    description: |-
                      (String) Project identifier of the GitOps repository.
                      Project identifier of the GitOps repository.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in platform to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in platform to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  refreshInterval:
                    description: |-
                      (String) For OCI repos, this is the interval to refresh the token to access the registry.
                      For OCI repos, this is the interval to refresh the token to access the registry.
                    type: string
                  repo:
                    description: |-
                      (Block List, Min: 1, Max: 1) Repo details holding application configurations. (see below for nested schema)
                      Repo details holding application configurations.
                    items:
                      properties:
                        connectionType:
                          description: |-
                            (String) Identifies the authentication method used to connect to the repository. Possible values: "HTTPS" "SSH" "GITHUB" "HTTPS_ANONYMOUS", "GITHUB_ENTERPRISE".
                            Identifies the authentication method used to connect to the repository. Possible values: "HTTPS" "SSH" "GITHUB" "HTTPS_ANONYMOUS", "GITHUB_ENTERPRISE".
                          type: string
                        enableLfs:
                          description: |-
                            lfs support must be enabled for this repo. This is valid only for Git repositories.
                            Indicates if git-lfs support must be enabled for this repo. This is valid only for Git repositories.
                          type: boolean
                        enableOci:
                          description: |-
                            oci support must be enabled for this repo.
                            Indicates if helm-oci support must be enabled for this repo.
                          type: boolean
                        githubAppEnterpriseBaseUrl:
                          description: |-
                            (String) Base URL of GitHub Enterprise installation. If left empty, this defaults to https://api.github.com.
                            Base URL of GitHub Enterprise installation. If left empty, this defaults to https://api.github.com.
                          type: string
                        githubAppIdSecretRef:
                          description: |-
                            (String, Sensitive) Id of the GitHub app used to access the repo.
                            Id of the GitHub app used to access the repo.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        githubAppInstallationIdSecretRef:
                          description: |-
                            (String, Sensitive) Installation id of the GitHub app used to access the repo.
                            Installation id of the GitHub app used to access the repo.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        githubAppPrivateKeySecretRef:
                          description: |-
                            (String, Sensitive) GitHub app private key PEM data.
                            GitHub app private key PEM data.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        insecure:
                          description: |-
                            (Boolean) Indicates if the connection to the repository ignores any errors when verifying TLS certificates or SSH host keys.
                            Indicates if the connection to the repository ignores any errors when verifying TLS certificates or SSH host keys.
                          type: boolean
                        insecureIgnoreHostKey:
                          description: |-
                            (Boolean) Indicates if InsecureIgnoreHostKey should be used. Insecure is favored used only for git repos. Deprecated.
                            Indicates if InsecureIgnoreHostKey should be used. Insecure is favored used only for git repos. Deprecated.
                          type: boolean
                        name:
                          description: |-
                            (String) Name to be used for this repo. Only used with Helm repos.
                            Name to be used for this repo. Only used with Helm repos.
                          type: string
                        passwordSecretRef:
                          description: |-
                            (String, Sensitive) Password or PAT to be used for authenticating the remote repository.
                            Password or PAT to be used for authenticating the remote repository.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        project:
                          description: |-
                            (String) The ArgoCD project name corresponding to this GitOps repository. An empty string means that the GitOps repository belongs to the default project created by Harness.
                            The ArgoCD project name corresponding to this GitOps repository. An empty string means that the GitOps repository belongs to the default project created by Harness.
                          type: string
                        proxy:
                          description: |-
                            (String) The HTTP/HTTPS proxy used to access the repo.
                            The HTTP/HTTPS proxy used to access the repo.
                          type: string
                        repo:
                          description: |-
                            (Block List, Min: 1, Max: 1) Repo details holding application configurations. (see below for nested schema)
                            URL to the remote repository.
                          type: string
                        sshPrivateKeySecretRef:
                          description: |-
                            (String, Sensitive) SSH Key in PEM format for authenticating the repository. Used only for Git repository.
                            SSH Key in PEM format for authenticating the repository. Used only for Git repository.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        tlsClientCertDataSecretRef:
                          description: |-
                            (String, Sensitive) Certificate in PEM format for authenticating at the repo server. This is used for mTLS. The value should be base64 encoded.
                            Certificate in PEM format for authenticating at the repo server. This is used for mTLS. The value should be base64 encoded.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        tlsClientCertKeySecretRef:
                          description: |-
                            (String, Sensitive) Private key in PEM format for authenticating at the repo server. This is used for mTLS. The value should be base64 encoded.
                            Private key in PEM format for authenticating at the repo server. This is used for mTLS. The value should be base64 encoded.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        type:
                          description: |-
                            (String) Type specifies the type of the repo. Can be either "git" or "helm. "git" is assumed if empty or absent.
                            Type specifies the type of the repo. Can be either "git" or "helm. "git" is assumed if empty or absent.
                          type: string
                        username:
                          description: |-
                            (String) Username to be used for authenticating the remote repository.
                            Username to be used for authenticating the remote repository.
                          type: string
                      type: object
                    type: array
                  updateMask:
                    description: |-
                      (Block List, Deprecated) Update mask of the repository. (see below for nested schema)
                      Update mask of the repository.
                    items:
                      properties:
                        paths:
                          description: |-
                            (List of String) The set of field mask paths.
                            The set of field mask paths.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  upsert:
                    description: |-
                      (Boolean) Indicates if the GitOps repository should be updated if existing and inserted if not.
                      Indicates if the GitOps repository should be updated if existing and inserted if not.
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.identifier is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.identifier)
                || (has(self.initProvider) && has(self.initProvider.identifier))'
            - message: spec.forProvider.repo is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.repo)
                || (has(self.initProvider) && has(self.initProvider.repo))'
          status:
            description: RepositoryStatus defines the observed state of Repository.
            properties:
              atProvider:
                properties:
                  accountId:
                    description: |-
                      (String, Deprecated) Account identifier of the GitOps repository.
                      Account identifier of the GitOps repository.
                    type: string
                  agentId:
                    description: |-
                      (String) Agent identifier of the GitOps repository.
                      Agent identifier of the GitOps repository.
                    type: string
                  credsOnly:
                    description: |-
                      (Boolean) Indicates if to operate on credential set instead of repository.
                      Indicates if to operate on credential set instead of repository.
                    type: boolean
                  ecrGen:
                    description: |-
                      (Block List, Max: 1) ECR access token generator specific configuration. (see below for nested schema)
                      ECR access token generator specific configuration.
                    items:
                      properties:
                        jwtAuth:
                          description: |-
                            (Block List, Max: 1) JWT authentication specific configuration. (see below for nested schema)
                            JWT authentication specific configuration.
                          items:
                            properties:
                              audiences:
                                description: |-
                                  known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
                                  Audience specifies the `aud` claim for the service account token If the service account uses a well-known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
                                items:
                                  type: string
                                type: array
                              name:
                                description: |-
                                  (String) Name to be used for this repo. Only used with Helm repos.
                                  The name of the ServiceAccount resource being referred to.
                                type: string
                              namespace:
                                description: |-
                                  scoped. cluster-scoped defaults to the namespace of the referent.
                                  Namespace of the resource being referred to. Ignored if referent is not cluster-scoped. cluster-scoped defaults to the namespace of the referent.
                                type: string
                            type: object
                          type: array
                        region:
                          description: |-
                            (String) AWS region.
                            AWS region.
                          type: string
                        secretRef:
                          description: |-
                            (Block List, Max: 1) Secret reference to the AWS credentials. (see below for nested schema)
                            Secret reference to the AWS credentials.
                          items:
                            properties:
                              awsAccessKeyId:
                                description: |-
                                  (String) AWS access key id.
                                  AWS access key id.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  forceDelete:
                    description: |-
                      (Boolean) Indicates if the repository should be deleted forcefully, regardless of existing applications using that repo.
                      Indicates if the repository should be deleted forcefully, regardless of existing applications using that repo.
                    type: boolean
                  gcrGen:
                    description: |-
                      (Block List, Max: 1) GCR access token generator specific configuration. (see below for nested schema)
                      GCR access token generator specific configuration.
                    items:
                      properties:
                        projectId:
                          description: |-
                            (String) Project identifier of the GitOps repository.
                            GCP project id.
                          type: string
                        workloadIdentity:
                          description: |-
                            (Block List, Max: 1) GCP workload identity. (see below for nested schema)
                            GCP workload identity.
                          items:
                            properties:
                              clusterLocation:
                                description: |-
                                  (String) Cluster location.
                                  Cluster location.
                                type: string
                              clusterName:
                                description: |-
                                  (String) Cluster name.
                                  Cluster name.
                                type: string
                              clusterProjectId:
                                description: |-
                                  (String) Cluster project id.
                                  Cluster project id.
                                type: string
                              serviceAccountRef:
                                description: |-
                                  (Block List, Max: 1) Service account reference. (see below for nested schema)
                                  Service account reference.
                                items:
                                  properties:
                                    audiences:
                                      description: |-
                                        known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
                                        Audience specifies the `aud` claim for the service account token If the service account uses a well-known annotation for e.g. IRSA or GCP Workload Identity then this audiences will be appended to the list
                                      items:
                                        type: string
                                      type: array
                                    name:
                                      description: |-
                                        (String) Name to be used for this repo. Only used with Helm repos.
                                        The name of the ServiceAccount resource being referred to.
                                      type: string
                                    namespace:
                                      description: |-
                                        scoped. cluster-scoped defaults to the namespace of the referent.
                                        Namespace of the resource being referred to. Ignored if referent is not cluster-scoped. cluster-scoped defaults to the namespace of the referent.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  genType:
                    description: |-
                      (String) Default: "UNSET"
                      Enum: "UNSET" "AWS_ECR" "GOOGLE_GCR"
                      Default: "UNSET"
                      Enum: "UNSET" "AWS_ECR" "GOOGLE_GCR"
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  identifier:
                    description: |-
                      (String) Identifier of the GitOps repository.
                      Identifier of the GitOps repository.
                    type: string
                  orgId:
                    description: |-
                      (String) Organization identifier of the GitOps repository.
                      Organization identifier of the GitOps repository.
                    type: string
                  projectId:
                    description: |-
                      (String) Project identifier of the GitOps repository.
                      Project identifier of the GitOps repository.
                    type: string
                  refreshInterval:
                    description: |-
                      (String) For OCI repos, this is the interval to refresh the token to access the registry.
                      For OCI repos, this is the interval to refresh the token to access the registry.
                    type: string
                  repo:
                    description: |-
                      (Block List, Min: 1, Max: 1) Repo details holding application configurations. (see below for nested schema)
                      Repo details holding application configurations.
                    items:
                      properties:
                        connectionType:
                          description: |-
                            (String) Identifies the authentication method used to connect to the repository. Possible values: "HTTPS" "SSH" "GITHUB" "HTTPS_ANONYMOUS", "GITHUB_ENTERPRISE".
                            Identifies the authentication method used to connect to the repository. Possible values: "HTTPS" "SSH" "GITHUB" "HTTPS_ANONYMOUS", "GITHUB_ENTERPRISE".
                          type: string
                        enableLfs:
                          description: |-
                            lfs support must be enabled for this repo. This is valid only for Git repositories.
                            Indicates if git-lfs support must be enabled for this repo. This is valid only for Git repositories.
                          type: boolean
                        enableOci:
                          description: |-
                            oci support must be enabled for this repo.
                            Indicates if helm-oci support must be enabled for this repo.
                          type: boolean
                        githubAppEnterpriseBaseUrl:
                          description: |-
                            (String) Base URL of GitHub Enterprise installation. If left empty, this defaults to https://api.github.com.
                            Base URL of GitHub Enterprise installation. If left empty, this defaults to https://api.github.com.
                          type: string
                        inheritedCreds:
                          description: |-
                            (Boolean) Indicates if the credentials were inherited from a repository credential.
                            Indicates if the credentials were inherited from a repository credential.
                          type: boolean
                        insecure:
                          description: |-
                            (Boolean) Indicates if the connection to the repository ignores any errors when verifying TLS certificates or SSH host keys.
                            Indicates if the connection to the repository ignores any errors when verifying TLS certificates or SSH host keys.
                          type: boolean
                        insecureIgnoreHostKey:
                          description: |-
                            (Boolean) Indicates if InsecureIgnoreHostKey should be used. Insecure is favored used only for git repos. Deprecated.
                            Indicates if InsecureIgnoreHostKey should be used. Insecure is favored used only for git repos. Deprecated.
                          type: boolean
                        name:
                          description: |-
                            (String) Name to be used for this repo. Only used with Helm repos.
                            Name to be used for this repo. Only used with Helm repos.
                          type: string
                        project:
                          description: |-
                            (String) The ArgoCD project name corresponding to this GitOps repository. An empty string means that the GitOps repository belongs to the default project created by Harness.
                            The ArgoCD project name corresponding to this GitOps repository. An empty string means that the GitOps repository belongs to the default project created by Harness.
                          type: string
                        proxy:
                          description: |-
                            (String) The HTTP/HTTPS proxy used to access the repo.
                            The HTTP/HTTPS proxy used to access the repo.
                          type: string
                        repo:
                          description: |-
                            (Block List, Min: 1, Max: 1) Repo details holding application configurations. (see below for nested schema)
                            URL to the remote repository.
                          type: string
                        type:
                          description: |-
                            (String) Type specifies the type of the repo. Can be either "git" or "helm. "git" is assumed if empty or absent.
                            Type specifies the type of the repo. Can be either "git" or "helm. "git" is assumed if empty or absent.
                          type: string
                        username:
                          description: |-
                            (String) Username to be used for authenticating the remote repository.
                            Username to be used for authenticating the remote repository.
                          type: string
                      type: object
                    type: array
                  updateMask:
                    description: |-
                      (Block List, Deprecated) Update mask of the repository. (see below for nested schema)
                      Update mask of the repository.
                    items:
                      properties:
                        paths:
                          description: |-
                            (List of String) The set of field mask paths.
                            The set of field mask paths.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  upsert:
                    description: |-
                      (Boolean) Indicates if the GitOps repository should be updated if existing and inserted if not.
                      Indicates if the GitOps repository should be updated if existing and inserted if not.
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
