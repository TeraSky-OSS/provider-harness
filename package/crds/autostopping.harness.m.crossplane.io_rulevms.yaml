---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: rulevms.autostopping.harness.m.crossplane.io
spec:
  group: autostopping.harness.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - harness
    kind: RuleVm
    listKind: RuleVmList
    plural: rulevms
    singular: rulevm
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RuleVm is the Schema for the RuleVms API. Resource for creating
          a AutoStopping rule for VMs.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RuleVmSpec defines the desired state of RuleVm
            properties:
              forProvider:
                properties:
                  cloudConnectorId:
                    description: |-
                      (String) Id of the cloud connector
                      Id of the cloud connector
                    type: string
                  customDomains:
                    description: |-
                      (List of String) Custom URLs used to access the instances
                      Custom URLs used to access the instances
                    items:
                      type: string
                    type: array
                  depends:
                    description: |-
                      (Block List) Dependent rules (see below for nested schema)
                      Dependent rules
                    items:
                      properties:
                        delayInSec:
                          description: |-
                            (Number) Number of seconds the rule should wait after warming up the dependent rule
                            Number of seconds the rule should wait after warming up the dependent rule
                          type: number
                        ruleId:
                          description: |-
                            (Number) Rule id of the dependent rule
                            Rule id of the dependent rule
                          type: number
                      type: object
                    type: array
                  dryRun:
                    description: |-
                      (Boolean) Boolean that indicates whether the AutoStopping rule should be created in DryRun mode
                      Boolean that indicates whether the AutoStopping rule should be created in DryRun mode
                    type: boolean
                  filter:
                    description: '(Block List, Min: 1, Max: 1) (see below for nested
                      schema)'
                    items:
                      properties:
                        regions:
                          description: |-
                            (List of String) Regions of instances that needs to be managed using the AutoStopping rules
                            Regions of instances that needs to be managed using the AutoStopping rules
                          items:
                            type: string
                          type: array
                        tags:
                          description: |-
                            (Block List) Tags of instances that needs to be managed using the AutoStopping rules (see below for nested schema)
                            Tags of instances that needs to be managed using the AutoStopping rules
                          items:
                            properties:
                              key:
                                description: (String)
                                type: string
                              value:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        vmIds:
                          description: |-
                            (List of String) Ids of instances that needs to be managed using the AutoStopping rules
                            Ids of instances that needs to be managed using the AutoStopping rules
                          items:
                            type: string
                          type: array
                        zones:
                          description: |-
                            (List of String) Zones of instances that needs to be managed using the AutoStopping rules
                            Zones of instances that needs to be managed using the AutoStopping rules
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  http:
                    description: |-
                      (Block List) Http routing configuration (see below for nested schema)
                      Http routing configuration
                    items:
                      properties:
                        health:
                          description: |-
                            (Block List) Health Check Details (see below for nested schema)
                            Health Check Details
                          items:
                            properties:
                              path:
                                description: |-
                                  (String) API path to use for health check
                                  API path to use for health check
                                type: string
                              port:
                                description: |-
                                  (Number) Health check port on the VM
                                  Health check port on the VM
                                type: number
                              protocol:
                                description: |-
                                  (String) Protocol can be http or https
                                  Protocol can be http or https
                                type: string
                              statusCodeFrom:
                                description: |-
                                  (Number) Lower limit for acceptable status code
                                  Lower limit for acceptable status code
                                type: number
                              statusCodeTo:
                                description: |-
                                  (Number) Upper limit for acceptable status code
                                  Upper limit for acceptable status code
                                type: number
                              timeout:
                                description: |-
                                  (Number) Health check timeout
                                  Health check timeout
                                type: number
                            type: object
                          type: array
                        proxyId:
                          description: |-
                            (String) Id of the proxy
                            Id of the proxy
                          type: string
                        routing:
                          description: |-
                            (Block List) Routing configuration used to access the instances (see below for nested schema)
                            Routing configuration used to access the instances
                          items:
                            properties:
                              action:
                                description: |-
                                  (String) Action to take for the routing rule
                                  Action to take for the routing rule
                                type: string
                              path:
                                description: |-
                                  (String) API path to use for health check
                                  Path to use for the proxy
                                type: string
                              sourcePort:
                                description: |-
                                  (Number) Port on the proxy
                                  Port on the proxy
                                type: number
                              sourceProtocol:
                                description: |-
                                  (String) Source protocol of the proxy can be http or https
                                  Source protocol of the proxy can be http or https
                                type: string
                              targetPort:
                                description: |-
                                  (Number) Port on the VM
                                  Port on the VM
                                type: number
                              targetProtocol:
                                description: |-
                                  (String) Target protocol of the instance can be http or https
                                  Target protocol of the instance can be http or https
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  idleTimeMins:
                    description: |-
                      (Number) Idle time in minutes. This is the time that the AutoStopping rule waits before stopping the idle instances.
                      Idle time in minutes. This is the time that the AutoStopping rule waits before stopping the idle instances.
                    type: number
                  name:
                    description: |-
                      (String) Name of the rule
                      Name of the rule
                    type: string
                  tcp:
                    description: |-
                      (Block List) TCP routing configuration (see below for nested schema)
                      TCP routing configuration
                    items:
                      properties:
                        forwardRule:
                          description: |-
                            (Block List) Additional tcp forwarding rules (see below for nested schema)
                            Additional tcp forwarding rules
                          items:
                            properties:
                              connectOn:
                                description: |-
                                  (Number) Port to listen on the proxy
                                  Port to listen on the proxy
                                type: number
                              port:
                                description: |-
                                  (Number) Health check port on the VM
                                  Port to listen on the vm
                                type: number
                            type: object
                          type: array
                        proxyId:
                          description: |-
                            (String) Id of the proxy
                            Id of the Proxy
                          type: string
                        rdp:
                          description: |-
                            (Block List) RDP configuration (see below for nested schema)
                            RDP configuration
                          items:
                            properties:
                              connectOn:
                                description: |-
                                  (Number) Port to listen on the proxy
                                  Port to listen on the proxy
                                type: number
                              port:
                                description: |-
                                  (Number) Health check port on the VM
                                  Port to listen on the vm
                                type: number
                            type: object
                          type: array
                        ssh:
                          description: |-
                            (Block List) SSH configuration (see below for nested schema)
                            SSH configuration
                          items:
                            properties:
                              connectOn:
                                description: |-
                                  (Number) Port to listen on the proxy
                                  Port to listen on the proxy
                                type: number
                              port:
                                description: |-
                                  (Number) Health check port on the VM
                                  Port to listen on the vm
                                type: number
                            type: object
                          type: array
                      type: object
                    type: array
                  useSpot:
                    description: |-
                      (Boolean) Boolean that indicates whether the selected instances should be converted to spot vm
                      Boolean that indicates whether the selected instances should be converted to spot vm
                    type: boolean
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  cloudConnectorId:
                    description: |-
                      (String) Id of the cloud connector
                      Id of the cloud connector
                    type: string
                  customDomains:
                    description: |-
                      (List of String) Custom URLs used to access the instances
                      Custom URLs used to access the instances
                    items:
                      type: string
                    type: array
                  depends:
                    description: |-
                      (Block List) Dependent rules (see below for nested schema)
                      Dependent rules
                    items:
                      properties:
                        delayInSec:
                          description: |-
                            (Number) Number of seconds the rule should wait after warming up the dependent rule
                            Number of seconds the rule should wait after warming up the dependent rule
                          type: number
                        ruleId:
                          description: |-
                            (Number) Rule id of the dependent rule
                            Rule id of the dependent rule
                          type: number
                      type: object
                    type: array
                  dryRun:
                    description: |-
                      (Boolean) Boolean that indicates whether the AutoStopping rule should be created in DryRun mode
                      Boolean that indicates whether the AutoStopping rule should be created in DryRun mode
                    type: boolean
                  filter:
                    description: '(Block List, Min: 1, Max: 1) (see below for nested
                      schema)'
                    items:
                      properties:
                        regions:
                          description: |-
                            (List of String) Regions of instances that needs to be managed using the AutoStopping rules
                            Regions of instances that needs to be managed using the AutoStopping rules
                          items:
                            type: string
                          type: array
                        tags:
                          description: |-
                            (Block List) Tags of instances that needs to be managed using the AutoStopping rules (see below for nested schema)
                            Tags of instances that needs to be managed using the AutoStopping rules
                          items:
                            properties:
                              key:
                                description: (String)
                                type: string
                              value:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        vmIds:
                          description: |-
                            (List of String) Ids of instances that needs to be managed using the AutoStopping rules
                            Ids of instances that needs to be managed using the AutoStopping rules
                          items:
                            type: string
                          type: array
                        zones:
                          description: |-
                            (List of String) Zones of instances that needs to be managed using the AutoStopping rules
                            Zones of instances that needs to be managed using the AutoStopping rules
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  http:
                    description: |-
                      (Block List) Http routing configuration (see below for nested schema)
                      Http routing configuration
                    items:
                      properties:
                        health:
                          description: |-
                            (Block List) Health Check Details (see below for nested schema)
                            Health Check Details
                          items:
                            properties:
                              path:
                                description: |-
                                  (String) API path to use for health check
                                  API path to use for health check
                                type: string
                              port:
                                description: |-
                                  (Number) Health check port on the VM
                                  Health check port on the VM
                                type: number
                              protocol:
                                description: |-
                                  (String) Protocol can be http or https
                                  Protocol can be http or https
                                type: string
                              statusCodeFrom:
                                description: |-
                                  (Number) Lower limit for acceptable status code
                                  Lower limit for acceptable status code
                                type: number
                              statusCodeTo:
                                description: |-
                                  (Number) Upper limit for acceptable status code
                                  Upper limit for acceptable status code
                                type: number
                              timeout:
                                description: |-
                                  (Number) Health check timeout
                                  Health check timeout
                                type: number
                            type: object
                          type: array
                        proxyId:
                          description: |-
                            (String) Id of the proxy
                            Id of the proxy
                          type: string
                        routing:
                          description: |-
                            (Block List) Routing configuration used to access the instances (see below for nested schema)
                            Routing configuration used to access the instances
                          items:
                            properties:
                              action:
                                description: |-
                                  (String) Action to take for the routing rule
                                  Action to take for the routing rule
                                type: string
                              path:
                                description: |-
                                  (String) API path to use for health check
                                  Path to use for the proxy
                                type: string
                              sourcePort:
                                description: |-
                                  (Number) Port on the proxy
                                  Port on the proxy
                                type: number
                              sourceProtocol:
                                description: |-
                                  (String) Source protocol of the proxy can be http or https
                                  Source protocol of the proxy can be http or https
                                type: string
                              targetPort:
                                description: |-
                                  (Number) Port on the VM
                                  Port on the VM
                                type: number
                              targetProtocol:
                                description: |-
                                  (String) Target protocol of the instance can be http or https
                                  Target protocol of the instance can be http or https
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  idleTimeMins:
                    description: |-
                      (Number) Idle time in minutes. This is the time that the AutoStopping rule waits before stopping the idle instances.
                      Idle time in minutes. This is the time that the AutoStopping rule waits before stopping the idle instances.
                    type: number
                  name:
                    description: |-
                      (String) Name of the rule
                      Name of the rule
                    type: string
                  tcp:
                    description: |-
                      (Block List) TCP routing configuration (see below for nested schema)
                      TCP routing configuration
                    items:
                      properties:
                        forwardRule:
                          description: |-
                            (Block List) Additional tcp forwarding rules (see below for nested schema)
                            Additional tcp forwarding rules
                          items:
                            properties:
                              connectOn:
                                description: |-
                                  (Number) Port to listen on the proxy
                                  Port to listen on the proxy
                                type: number
                              port:
                                description: |-
                                  (Number) Health check port on the VM
                                  Port to listen on the vm
                                type: number
                            type: object
                          type: array
                        proxyId:
                          description: |-
                            (String) Id of the proxy
                            Id of the Proxy
                          type: string
                        rdp:
                          description: |-
                            (Block List) RDP configuration (see below for nested schema)
                            RDP configuration
                          items:
                            properties:
                              connectOn:
                                description: |-
                                  (Number) Port to listen on the proxy
                                  Port to listen on the proxy
                                type: number
                              port:
                                description: |-
                                  (Number) Health check port on the VM
                                  Port to listen on the vm
                                type: number
                            type: object
                          type: array
                        ssh:
                          description: |-
                            (Block List) SSH configuration (see below for nested schema)
                            SSH configuration
                          items:
                            properties:
                              connectOn:
                                description: |-
                                  (Number) Port to listen on the proxy
                                  Port to listen on the proxy
                                type: number
                              port:
                                description: |-
                                  (Number) Health check port on the VM
                                  Port to listen on the vm
                                type: number
                            type: object
                          type: array
                      type: object
                    type: array
                  useSpot:
                    description: |-
                      (Boolean) Boolean that indicates whether the selected instances should be converted to spot vm
                      Boolean that indicates whether the selected instances should be converted to spot vm
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.cloudConnectorId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.cloudConnectorId)
                || (has(self.initProvider) && has(self.initProvider.cloudConnectorId))'
            - message: spec.forProvider.filter is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.filter)
                || (has(self.initProvider) && has(self.initProvider.filter))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: RuleVmStatus defines the observed state of RuleVm.
            properties:
              atProvider:
                properties:
                  cloudConnectorId:
                    description: |-
                      (String) Id of the cloud connector
                      Id of the cloud connector
                    type: string
                  connect:
                    additionalProperties:
                      type: number
                    description: |-
                      (Map of Number) Connection information (source ports on the proxy). Keys: "ssh" and "rdp" for SSH/RDP; other keys are target port as string (e.g. "80") for forward_rule, value is the proxy source port.
                      Connection information (source ports on the proxy). Keys: "ssh" and "rdp" for SSH/RDP; other keys are target port as string (e.g. "80") for forward_rule, value is the proxy source port.
                    type: object
                    x-kubernetes-map-type: granular
                  customDomains:
                    description: |-
                      (List of String) Custom URLs used to access the instances
                      Custom URLs used to access the instances
                    items:
                      type: string
                    type: array
                  depends:
                    description: |-
                      (Block List) Dependent rules (see below for nested schema)
                      Dependent rules
                    items:
                      properties:
                        delayInSec:
                          description: |-
                            (Number) Number of seconds the rule should wait after warming up the dependent rule
                            Number of seconds the rule should wait after warming up the dependent rule
                          type: number
                        ruleId:
                          description: |-
                            (Number) Rule id of the dependent rule
                            Rule id of the dependent rule
                          type: number
                      type: object
                    type: array
                  dryRun:
                    description: |-
                      (Boolean) Boolean that indicates whether the AutoStopping rule should be created in DryRun mode
                      Boolean that indicates whether the AutoStopping rule should be created in DryRun mode
                    type: boolean
                  filter:
                    description: '(Block List, Min: 1, Max: 1) (see below for nested
                      schema)'
                    items:
                      properties:
                        regions:
                          description: |-
                            (List of String) Regions of instances that needs to be managed using the AutoStopping rules
                            Regions of instances that needs to be managed using the AutoStopping rules
                          items:
                            type: string
                          type: array
                        tags:
                          description: |-
                            (Block List) Tags of instances that needs to be managed using the AutoStopping rules (see below for nested schema)
                            Tags of instances that needs to be managed using the AutoStopping rules
                          items:
                            properties:
                              key:
                                description: (String)
                                type: string
                              value:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        vmIds:
                          description: |-
                            (List of String) Ids of instances that needs to be managed using the AutoStopping rules
                            Ids of instances that needs to be managed using the AutoStopping rules
                          items:
                            type: string
                          type: array
                        zones:
                          description: |-
                            (List of String) Zones of instances that needs to be managed using the AutoStopping rules
                            Zones of instances that needs to be managed using the AutoStopping rules
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  http:
                    description: |-
                      (Block List) Http routing configuration (see below for nested schema)
                      Http routing configuration
                    items:
                      properties:
                        health:
                          description: |-
                            (Block List) Health Check Details (see below for nested schema)
                            Health Check Details
                          items:
                            properties:
                              path:
                                description: |-
                                  (String) API path to use for health check
                                  API path to use for health check
                                type: string
                              port:
                                description: |-
                                  (Number) Health check port on the VM
                                  Health check port on the VM
                                type: number
                              protocol:
                                description: |-
                                  (String) Protocol can be http or https
                                  Protocol can be http or https
                                type: string
                              statusCodeFrom:
                                description: |-
                                  (Number) Lower limit for acceptable status code
                                  Lower limit for acceptable status code
                                type: number
                              statusCodeTo:
                                description: |-
                                  (Number) Upper limit for acceptable status code
                                  Upper limit for acceptable status code
                                type: number
                              timeout:
                                description: |-
                                  (Number) Health check timeout
                                  Health check timeout
                                type: number
                            type: object
                          type: array
                        proxyId:
                          description: |-
                            (String) Id of the proxy
                            Id of the proxy
                          type: string
                        routing:
                          description: |-
                            (Block List) Routing configuration used to access the instances (see below for nested schema)
                            Routing configuration used to access the instances
                          items:
                            properties:
                              action:
                                description: |-
                                  (String) Action to take for the routing rule
                                  Action to take for the routing rule
                                type: string
                              path:
                                description: |-
                                  (String) API path to use for health check
                                  Path to use for the proxy
                                type: string
                              sourcePort:
                                description: |-
                                  (Number) Port on the proxy
                                  Port on the proxy
                                type: number
                              sourceProtocol:
                                description: |-
                                  (String) Source protocol of the proxy can be http or https
                                  Source protocol of the proxy can be http or https
                                type: string
                              targetPort:
                                description: |-
                                  (Number) Port on the VM
                                  Port on the VM
                                type: number
                              targetProtocol:
                                description: |-
                                  (String) Target protocol of the instance can be http or https
                                  Target protocol of the instance can be http or https
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  identifier:
                    description: |-
                      (String) Unique identifier of the resource
                      Unique identifier of the resource
                    type: string
                  idleTimeMins:
                    description: |-
                      (Number) Idle time in minutes. This is the time that the AutoStopping rule waits before stopping the idle instances.
                      Idle time in minutes. This is the time that the AutoStopping rule waits before stopping the idle instances.
                    type: number
                  name:
                    description: |-
                      (String) Name of the rule
                      Name of the rule
                    type: string
                  tcp:
                    description: |-
                      (Block List) TCP routing configuration (see below for nested schema)
                      TCP routing configuration
                    items:
                      properties:
                        forwardRule:
                          description: |-
                            (Block List) Additional tcp forwarding rules (see below for nested schema)
                            Additional tcp forwarding rules
                          items:
                            properties:
                              connectOn:
                                description: |-
                                  (Number) Port to listen on the proxy
                                  Port to listen on the proxy
                                type: number
                              port:
                                description: |-
                                  (Number) Health check port on the VM
                                  Port to listen on the vm
                                type: number
                            type: object
                          type: array
                        proxyId:
                          description: |-
                            (String) Id of the proxy
                            Id of the Proxy
                          type: string
                        rdp:
                          description: |-
                            (Block List) RDP configuration (see below for nested schema)
                            RDP configuration
                          items:
                            properties:
                              connectOn:
                                description: |-
                                  (Number) Port to listen on the proxy
                                  Port to listen on the proxy
                                type: number
                              port:
                                description: |-
                                  (Number) Health check port on the VM
                                  Port to listen on the vm
                                type: number
                            type: object
                          type: array
                        ssh:
                          description: |-
                            (Block List) SSH configuration (see below for nested schema)
                            SSH configuration
                          items:
                            properties:
                              connectOn:
                                description: |-
                                  (Number) Port to listen on the proxy
                                  Port to listen on the proxy
                                type: number
                              port:
                                description: |-
                                  (Number) Health check port on the VM
                                  Port to listen on the vm
                                type: number
                            type: object
                          type: array
                      type: object
                    type: array
                  useSpot:
                    description: |-
                      (Boolean) Boolean that indicates whether the selected instances should be converted to spot vm
                      Boolean that indicates whether the selected instances should be converted to spot vm
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
