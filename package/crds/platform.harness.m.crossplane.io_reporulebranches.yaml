---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: reporulebranches.platform.harness.m.crossplane.io
spec:
  group: platform.harness.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - harness
    kind: RepoRuleBranch
    listKind: RepoRuleBranchList
    plural: reporulebranches
    singular: reporulebranch
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RepoRuleBranch is the Schema for the RepoRuleBranchs API. <no
          value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RepoRuleBranchSpec defines the desired state of RepoRuleBranch
            properties:
              forProvider:
                properties:
                  bypass:
                    description: List of users who can bypass this rule.
                    items:
                      properties:
                        repoOwners:
                          description: Allow users with repository edit permission
                            to bypass.
                          type: boolean
                        userIds:
                          description: List of user ids with who can bypass.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  description:
                    description: Description of the rule.
                    type: string
                  identifier:
                    description: Identifier of the rule.
                    type: string
                  orgId:
                    description: Unique identifier of the organization.
                    type: string
                  orgIdRef:
                    description: Reference to a Organization in platform to populate
                      orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  orgIdSelector:
                    description: Selector for a Organization in platform to populate
                      orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  pattern:
                    description: Pattern of branch to which rule will apply.
                    items:
                      properties:
                        defaultBranch:
                          description: Should rule apply to default branch of the
                            repository.
                          type: boolean
                        exclude:
                          description: Globstar branch patterns on which rules will
                            NOT be applied.
                          items:
                            type: string
                          type: array
                        include:
                          description: Globstar branch patterns on which rules will
                            be applied.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  policies:
                    description: Policies to be applied for this rule.
                    items:
                      properties:
                        allowMergeStrategies:
                          description: Limit which merge strategies are available
                            to merge a pull request(Any of squash, rebase, merge).
                          items:
                            type: string
                          type: array
                        blockBranchCreation:
                          description: Only allow users with bypass permission to
                            create matching branches.
                          type: boolean
                        blockBranchDeletion:
                          description: Only allow users with bypass permission to
                            delete matching branches.
                          type: boolean
                        deleteBranchOnMerge:
                          description: Automatically delete the source branch of a
                            pull request after it is merged.
                          type: boolean
                        requireCodeOwners:
                          description: Require approval on pull requests from one
                            reviewer for each codeowner rule.
                          type: boolean
                        requireLatestCommitApproval:
                          description: Require re-approval when there are new changes
                            in the pull request.
                          type: boolean
                        requireMinimumApprovalCount:
                          description: Require approval on pull requests from a minimum
                            number of reviewers.
                          type: number
                        requireNoChangeRequest:
                          description: Require all request for changes have been resolved.
                          type: boolean
                        requirePullRequest:
                          description: Do not allow any changes to matching branches
                            without a pull request.
                          type: boolean
                        requireResolveAllComments:
                          description: All comments on a pull request must be resolved
                            before it can be merged.
                          type: boolean
                        requireStatusChecks:
                          description: Selected status checks must pass before a pull
                            request can be merged.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  projectId:
                    description: Unique identifier of the project.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in platform to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in platform to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  repoIdentifier:
                    description: Repo identifier of the repository.
                    type: string
                  state:
                    description: State of the rule (active, disable, monitor).
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  bypass:
                    description: List of users who can bypass this rule.
                    items:
                      properties:
                        repoOwners:
                          description: Allow users with repository edit permission
                            to bypass.
                          type: boolean
                        userIds:
                          description: List of user ids with who can bypass.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  description:
                    description: Description of the rule.
                    type: string
                  identifier:
                    description: Identifier of the rule.
                    type: string
                  orgId:
                    description: Unique identifier of the organization.
                    type: string
                  orgIdRef:
                    description: Reference to a Organization in platform to populate
                      orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  orgIdSelector:
                    description: Selector for a Organization in platform to populate
                      orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  pattern:
                    description: Pattern of branch to which rule will apply.
                    items:
                      properties:
                        defaultBranch:
                          description: Should rule apply to default branch of the
                            repository.
                          type: boolean
                        exclude:
                          description: Globstar branch patterns on which rules will
                            NOT be applied.
                          items:
                            type: string
                          type: array
                        include:
                          description: Globstar branch patterns on which rules will
                            be applied.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  policies:
                    description: Policies to be applied for this rule.
                    items:
                      properties:
                        allowMergeStrategies:
                          description: Limit which merge strategies are available
                            to merge a pull request(Any of squash, rebase, merge).
                          items:
                            type: string
                          type: array
                        blockBranchCreation:
                          description: Only allow users with bypass permission to
                            create matching branches.
                          type: boolean
                        blockBranchDeletion:
                          description: Only allow users with bypass permission to
                            delete matching branches.
                          type: boolean
                        deleteBranchOnMerge:
                          description: Automatically delete the source branch of a
                            pull request after it is merged.
                          type: boolean
                        requireCodeOwners:
                          description: Require approval on pull requests from one
                            reviewer for each codeowner rule.
                          type: boolean
                        requireLatestCommitApproval:
                          description: Require re-approval when there are new changes
                            in the pull request.
                          type: boolean
                        requireMinimumApprovalCount:
                          description: Require approval on pull requests from a minimum
                            number of reviewers.
                          type: number
                        requireNoChangeRequest:
                          description: Require all request for changes have been resolved.
                          type: boolean
                        requirePullRequest:
                          description: Do not allow any changes to matching branches
                            without a pull request.
                          type: boolean
                        requireResolveAllComments:
                          description: All comments on a pull request must be resolved
                            before it can be merged.
                          type: boolean
                        requireStatusChecks:
                          description: Selected status checks must pass before a pull
                            request can be merged.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  projectId:
                    description: Unique identifier of the project.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in platform to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in platform to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  repoIdentifier:
                    description: Repo identifier of the repository.
                    type: string
                  state:
                    description: State of the rule (active, disable, monitor).
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.bypass is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.bypass)
                || (has(self.initProvider) && has(self.initProvider.bypass))'
            - message: spec.forProvider.identifier is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.identifier)
                || (has(self.initProvider) && has(self.initProvider.identifier))'
            - message: spec.forProvider.policies is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.policies)
                || (has(self.initProvider) && has(self.initProvider.policies))'
            - message: spec.forProvider.repoIdentifier is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.repoIdentifier)
                || (has(self.initProvider) && has(self.initProvider.repoIdentifier))'
            - message: spec.forProvider.state is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.state)
                || (has(self.initProvider) && has(self.initProvider.state))'
          status:
            description: RepoRuleBranchStatus defines the observed state of RepoRuleBranch.
            properties:
              atProvider:
                properties:
                  bypass:
                    description: List of users who can bypass this rule.
                    items:
                      properties:
                        repoOwners:
                          description: Allow users with repository edit permission
                            to bypass.
                          type: boolean
                        userIds:
                          description: List of user ids with who can bypass.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  created:
                    description: Timestamp when the rule was created.
                    type: number
                  createdBy:
                    description: ID of the user who created the rule.
                    type: number
                  description:
                    description: Description of the rule.
                    type: string
                  id:
                    type: string
                  identifier:
                    description: Identifier of the rule.
                    type: string
                  orgId:
                    description: Unique identifier of the organization.
                    type: string
                  pattern:
                    description: Pattern of branch to which rule will apply.
                    items:
                      properties:
                        defaultBranch:
                          description: Should rule apply to default branch of the
                            repository.
                          type: boolean
                        exclude:
                          description: Globstar branch patterns on which rules will
                            NOT be applied.
                          items:
                            type: string
                          type: array
                        include:
                          description: Globstar branch patterns on which rules will
                            be applied.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  policies:
                    description: Policies to be applied for this rule.
                    items:
                      properties:
                        allowMergeStrategies:
                          description: Limit which merge strategies are available
                            to merge a pull request(Any of squash, rebase, merge).
                          items:
                            type: string
                          type: array
                        blockBranchCreation:
                          description: Only allow users with bypass permission to
                            create matching branches.
                          type: boolean
                        blockBranchDeletion:
                          description: Only allow users with bypass permission to
                            delete matching branches.
                          type: boolean
                        deleteBranchOnMerge:
                          description: Automatically delete the source branch of a
                            pull request after it is merged.
                          type: boolean
                        requireCodeOwners:
                          description: Require approval on pull requests from one
                            reviewer for each codeowner rule.
                          type: boolean
                        requireLatestCommitApproval:
                          description: Require re-approval when there are new changes
                            in the pull request.
                          type: boolean
                        requireMinimumApprovalCount:
                          description: Require approval on pull requests from a minimum
                            number of reviewers.
                          type: number
                        requireNoChangeRequest:
                          description: Require all request for changes have been resolved.
                          type: boolean
                        requirePullRequest:
                          description: Do not allow any changes to matching branches
                            without a pull request.
                          type: boolean
                        requireResolveAllComments:
                          description: All comments on a pull request must be resolved
                            before it can be merged.
                          type: boolean
                        requireStatusChecks:
                          description: Selected status checks must pass before a pull
                            request can be merged.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  projectId:
                    description: Unique identifier of the project.
                    type: string
                  repoIdentifier:
                    description: Repo identifier of the repository.
                    type: string
                  state:
                    description: State of the rule (active, disable, monitor).
                    type: string
                  updated:
                    description: Timestamp when the rule was updated.
                    type: number
                  updatedBy:
                    description: ID of the user who updated the rule.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
