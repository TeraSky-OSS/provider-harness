---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: pipelinefilters.platform.harness.crossplane.io
spec:
  group: platform.harness.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - harness
    kind: PipelineFilters
    listKind: PipelineFiltersList
    plural: pipelinefilters
    singular: pipelinefilters
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PipelineFilters is the Schema for the PipelineFilterss API. Resource
          for creating Harness Pipeline Filters.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PipelineFiltersSpec defines the desired state of PipelineFilters
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  filterProperties:
                    description: |-
                      (Block List, Min: 1, Max: 1) Properties of the filters entity defined in Harness. (see below for nested schema)
                      Properties of the filters entity defined in Harness.
                    items:
                      properties:
                        description:
                          description: |-
                            (String) description of the pipline filter.
                            description of the pipline filter.
                          type: string
                        filterType:
                          description: |-
                            (String) Corresponding Entity of the filters. Currently supported types are {Connector, DelegateProfile, Delegate, PipelineSetup, PipelineExecution, Deployment, Audit, Template, EnvironmentGroup, FileStore, CCMRecommendation, Anomaly, Environment}.
                            Corresponding Entity of the filters. Currently supported types are {Connector, DelegateProfile, Delegate, PipelineSetup, PipelineExecution, Deployment, Audit, Template, EnvironmentGroup, FileStore, CCMRecommendation, Anomaly, Environment}.
                          type: string
                        moduleProperties:
                          description: |-
                            (Block List, Max: 1) module properties of the pipline filter. (see below for nested schema)
                            module properties of the pipline filter.
                          items:
                            properties:
                              cd:
                                description: |-
                                  (Block List, Max: 1) CD related properties to be filtered on. (see below for nested schema)
                                  CD related properties to be filtered on.
                                items:
                                  properties:
                                    artifactDisplayNames:
                                      description: |-
                                        (Set of String) Artifact display names of the CD pipeline.
                                        Artifact display names of the CD pipeline.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    deploymentTypes:
                                      description: |-
                                        (String) Deployment type of the CD pipeline, eg. Kubernetes
                                        Deployment type of the CD pipeline, eg. Kubernetes
                                      type: string
                                    environmentIdentifiers:
                                      description: |-
                                        (Set of String) Environment identifier of the CD pipeline.
                                        Environment identifier of the CD pipeline.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    environmentNames:
                                      description: |-
                                        (Set of String) Environment names of the CD pipeline.
                                        Environment names of the CD pipeline.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    serviceDefinitionTypes:
                                      description: |-
                                        (String) Deployment type of the CD pipeline, eg. Kubernetes
                                        Deployment type of the CD pipeline, eg. Kubernetes
                                      type: string
                                    serviceIdentifiers:
                                      description: |-
                                        (Set of String) Service identifiers of the CD pipeline.
                                        Service identifiers of the CD pipeline.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    serviceNames:
                                      description: |-
                                        (Set of String) Service names of the CD pipeline.
                                        Service names of the CD pipeline.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              ci:
                                description: |-
                                  (Block List, Max: 1) CI related properties to be filtered on. (see below for nested schema)
                                  CI related properties to be filtered on.
                                items:
                                  properties:
                                    branch:
                                      description: |-
                                        (String) Branch which was used while building.
                                        Branch which was used while building.
                                      type: string
                                    buildType:
                                      description: |-
                                        (String) Build type of the pipeline. Possible values: branch.
                                        Build type of the pipeline. Possible values: branch.
                                      type: string
                                    ciExecutionInfo:
                                      description: |-
                                        (Block List, Max: 1) CI execution info for the pipeline. (see below for nested schema)
                                        CI execution info for the pipeline.
                                      items:
                                        properties:
                                          event:
                                            description: |-
                                              (String) Event for the ci execution, Possible values: pullRequest.
                                              Event for the ci execution, Possible values: pullRequest.
                                            type: string
                                          pullRequest:
                                            description: |-
                                              (Block List, Max: 1) The pull request details of the CI pipeline. (see below for nested schema)
                                              The pull request details of the CI pipeline.
                                            items:
                                              properties:
                                                sourceBranch:
                                                  description: |-
                                                    (String) Source branch of the pull request.
                                                    Source branch of the pull request.
                                                  type: string
                                                targetBranch:
                                                  description: |-
                                                    (String) Target branch of the pull request.
                                                    Target branch of the pull request.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    repoNames:
                                      description: |-
                                        (String) name of the repository used in the pipeline.
                                        name of the repository used in the pipeline.
                                      type: string
                                    tag:
                                      description: |-
                                        (String) Tags to associate with the CI pipeline resource.
                                        Tags to associate with the CI pipeline resource.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        name:
                          description: |-
                            (String) Name of the pipeline filters.
                            Name of the pipeline filter.
                          type: string
                        pipelineIdentifiers:
                          description: |-
                            (List of String) Pipeline identifiers to filter on.
                            Pipeline identifiers to filter on.
                          items:
                            type: string
                          type: array
                        pipelineName:
                          description: |-
                            (String) Name of the pipeline execution filter.
                            Name of the pipeline execution filter.
                          type: string
                        pipelineTags:
                          description: |-
                            (List of Map of String) Tags to associate with the pipeline. tags should be in the form of {key:key1, value:key1value}
                            Tags to associate with the pipeline. tags should be in the form of `{key:key1, value:key1value}`
                          items:
                            additionalProperties:
                              type: string
                            type: object
                          type: array
                        tags:
                          description: |-
                            (Set of String) Tags to associate with the resource. Tags should be in the form name:value.
                            Tags to associate with the resource. Tags should be in the form `name:value`.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  filterVisibility:
                    description: |-
                      (String) This indicates visibility of filters. By default, everyone can view this filter.
                      This indicates visibility of filters. By default, everyone can view this filter.
                    type: string
                  identifier:
                    description: |-
                      (String) Unique identifier of the resource.
                      Unique identifier of the resource.
                    type: string
                  name:
                    description: |-
                      (String) Name of the pipeline filters.
                      Name of the pipeline filters.
                    type: string
                  orgId:
                    description: |-
                      (String) Organization Identifier for the Entity.
                      Organization Identifier for the Entity.
                    type: string
                  orgIdRef:
                    description: Reference to a Organization in platform to populate
                      orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  orgIdSelector:
                    description: Selector for a Organization in platform to populate
                      orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  projectId:
                    description: |-
                      (String) Project Identifier for the Entity.
                      Project Identifier for the Entity.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in platform to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in platform to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  type:
                    description: |-
                      (String) Type of pipeline filters. Currently supported types are {PipelineSetup, PipelineExecution, Deployment, Template, EnvironmentGroup, Environment}
                      Type of pipeline filters. Currently supported types are {PipelineSetup, PipelineExecution, Deployment, Template, EnvironmentGroup, Environment}
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  filterProperties:
                    description: |-
                      (Block List, Min: 1, Max: 1) Properties of the filters entity defined in Harness. (see below for nested schema)
                      Properties of the filters entity defined in Harness.
                    items:
                      properties:
                        description:
                          description: |-
                            (String) description of the pipline filter.
                            description of the pipline filter.
                          type: string
                        filterType:
                          description: |-
                            (String) Corresponding Entity of the filters. Currently supported types are {Connector, DelegateProfile, Delegate, PipelineSetup, PipelineExecution, Deployment, Audit, Template, EnvironmentGroup, FileStore, CCMRecommendation, Anomaly, Environment}.
                            Corresponding Entity of the filters. Currently supported types are {Connector, DelegateProfile, Delegate, PipelineSetup, PipelineExecution, Deployment, Audit, Template, EnvironmentGroup, FileStore, CCMRecommendation, Anomaly, Environment}.
                          type: string
                        moduleProperties:
                          description: |-
                            (Block List, Max: 1) module properties of the pipline filter. (see below for nested schema)
                            module properties of the pipline filter.
                          items:
                            properties:
                              cd:
                                description: |-
                                  (Block List, Max: 1) CD related properties to be filtered on. (see below for nested schema)
                                  CD related properties to be filtered on.
                                items:
                                  properties:
                                    artifactDisplayNames:
                                      description: |-
                                        (Set of String) Artifact display names of the CD pipeline.
                                        Artifact display names of the CD pipeline.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    deploymentTypes:
                                      description: |-
                                        (String) Deployment type of the CD pipeline, eg. Kubernetes
                                        Deployment type of the CD pipeline, eg. Kubernetes
                                      type: string
                                    environmentIdentifiers:
                                      description: |-
                                        (Set of String) Environment identifier of the CD pipeline.
                                        Environment identifier of the CD pipeline.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    environmentNames:
                                      description: |-
                                        (Set of String) Environment names of the CD pipeline.
                                        Environment names of the CD pipeline.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    serviceDefinitionTypes:
                                      description: |-
                                        (String) Deployment type of the CD pipeline, eg. Kubernetes
                                        Deployment type of the CD pipeline, eg. Kubernetes
                                      type: string
                                    serviceIdentifiers:
                                      description: |-
                                        (Set of String) Service identifiers of the CD pipeline.
                                        Service identifiers of the CD pipeline.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    serviceNames:
                                      description: |-
                                        (Set of String) Service names of the CD pipeline.
                                        Service names of the CD pipeline.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              ci:
                                description: |-
                                  (Block List, Max: 1) CI related properties to be filtered on. (see below for nested schema)
                                  CI related properties to be filtered on.
                                items:
                                  properties:
                                    branch:
                                      description: |-
                                        (String) Branch which was used while building.
                                        Branch which was used while building.
                                      type: string
                                    buildType:
                                      description: |-
                                        (String) Build type of the pipeline. Possible values: branch.
                                        Build type of the pipeline. Possible values: branch.
                                      type: string
                                    ciExecutionInfo:
                                      description: |-
                                        (Block List, Max: 1) CI execution info for the pipeline. (see below for nested schema)
                                        CI execution info for the pipeline.
                                      items:
                                        properties:
                                          event:
                                            description: |-
                                              (String) Event for the ci execution, Possible values: pullRequest.
                                              Event for the ci execution, Possible values: pullRequest.
                                            type: string
                                          pullRequest:
                                            description: |-
                                              (Block List, Max: 1) The pull request details of the CI pipeline. (see below for nested schema)
                                              The pull request details of the CI pipeline.
                                            items:
                                              properties:
                                                sourceBranch:
                                                  description: |-
                                                    (String) Source branch of the pull request.
                                                    Source branch of the pull request.
                                                  type: string
                                                targetBranch:
                                                  description: |-
                                                    (String) Target branch of the pull request.
                                                    Target branch of the pull request.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    repoNames:
                                      description: |-
                                        (String) name of the repository used in the pipeline.
                                        name of the repository used in the pipeline.
                                      type: string
                                    tag:
                                      description: |-
                                        (String) Tags to associate with the CI pipeline resource.
                                        Tags to associate with the CI pipeline resource.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        name:
                          description: |-
                            (String) Name of the pipeline filters.
                            Name of the pipeline filter.
                          type: string
                        pipelineIdentifiers:
                          description: |-
                            (List of String) Pipeline identifiers to filter on.
                            Pipeline identifiers to filter on.
                          items:
                            type: string
                          type: array
                        pipelineName:
                          description: |-
                            (String) Name of the pipeline execution filter.
                            Name of the pipeline execution filter.
                          type: string
                        pipelineTags:
                          description: |-
                            (List of Map of String) Tags to associate with the pipeline. tags should be in the form of {key:key1, value:key1value}
                            Tags to associate with the pipeline. tags should be in the form of `{key:key1, value:key1value}`
                          items:
                            additionalProperties:
                              type: string
                            type: object
                          type: array
                        tags:
                          description: |-
                            (Set of String) Tags to associate with the resource. Tags should be in the form name:value.
                            Tags to associate with the resource. Tags should be in the form `name:value`.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  filterVisibility:
                    description: |-
                      (String) This indicates visibility of filters. By default, everyone can view this filter.
                      This indicates visibility of filters. By default, everyone can view this filter.
                    type: string
                  identifier:
                    description: |-
                      (String) Unique identifier of the resource.
                      Unique identifier of the resource.
                    type: string
                  name:
                    description: |-
                      (String) Name of the pipeline filters.
                      Name of the pipeline filters.
                    type: string
                  orgId:
                    description: |-
                      (String) Organization Identifier for the Entity.
                      Organization Identifier for the Entity.
                    type: string
                  orgIdRef:
                    description: Reference to a Organization in platform to populate
                      orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  orgIdSelector:
                    description: Selector for a Organization in platform to populate
                      orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  projectId:
                    description: |-
                      (String) Project Identifier for the Entity.
                      Project Identifier for the Entity.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in platform to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in platform to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  type:
                    description: |-
                      (String) Type of pipeline filters. Currently supported types are {PipelineSetup, PipelineExecution, Deployment, Template, EnvironmentGroup, Environment}
                      Type of pipeline filters. Currently supported types are {PipelineSetup, PipelineExecution, Deployment, Template, EnvironmentGroup, Environment}
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.filterProperties is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.filterProperties)
                || (has(self.initProvider) && has(self.initProvider.filterProperties))'
            - message: spec.forProvider.identifier is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.identifier)
                || (has(self.initProvider) && has(self.initProvider.identifier))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: PipelineFiltersStatus defines the observed state of PipelineFilters.
            properties:
              atProvider:
                properties:
                  filterProperties:
                    description: |-
                      (Block List, Min: 1, Max: 1) Properties of the filters entity defined in Harness. (see below for nested schema)
                      Properties of the filters entity defined in Harness.
                    items:
                      properties:
                        description:
                          description: |-
                            (String) description of the pipline filter.
                            description of the pipline filter.
                          type: string
                        filterType:
                          description: |-
                            (String) Corresponding Entity of the filters. Currently supported types are {Connector, DelegateProfile, Delegate, PipelineSetup, PipelineExecution, Deployment, Audit, Template, EnvironmentGroup, FileStore, CCMRecommendation, Anomaly, Environment}.
                            Corresponding Entity of the filters. Currently supported types are {Connector, DelegateProfile, Delegate, PipelineSetup, PipelineExecution, Deployment, Audit, Template, EnvironmentGroup, FileStore, CCMRecommendation, Anomaly, Environment}.
                          type: string
                        moduleProperties:
                          description: |-
                            (Block List, Max: 1) module properties of the pipline filter. (see below for nested schema)
                            module properties of the pipline filter.
                          items:
                            properties:
                              cd:
                                description: |-
                                  (Block List, Max: 1) CD related properties to be filtered on. (see below for nested schema)
                                  CD related properties to be filtered on.
                                items:
                                  properties:
                                    artifactDisplayNames:
                                      description: |-
                                        (Set of String) Artifact display names of the CD pipeline.
                                        Artifact display names of the CD pipeline.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    deploymentTypes:
                                      description: |-
                                        (String) Deployment type of the CD pipeline, eg. Kubernetes
                                        Deployment type of the CD pipeline, eg. Kubernetes
                                      type: string
                                    environmentIdentifiers:
                                      description: |-
                                        (Set of String) Environment identifier of the CD pipeline.
                                        Environment identifier of the CD pipeline.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    environmentNames:
                                      description: |-
                                        (Set of String) Environment names of the CD pipeline.
                                        Environment names of the CD pipeline.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    serviceDefinitionTypes:
                                      description: |-
                                        (String) Deployment type of the CD pipeline, eg. Kubernetes
                                        Deployment type of the CD pipeline, eg. Kubernetes
                                      type: string
                                    serviceIdentifiers:
                                      description: |-
                                        (Set of String) Service identifiers of the CD pipeline.
                                        Service identifiers of the CD pipeline.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    serviceNames:
                                      description: |-
                                        (Set of String) Service names of the CD pipeline.
                                        Service names of the CD pipeline.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              ci:
                                description: |-
                                  (Block List, Max: 1) CI related properties to be filtered on. (see below for nested schema)
                                  CI related properties to be filtered on.
                                items:
                                  properties:
                                    branch:
                                      description: |-
                                        (String) Branch which was used while building.
                                        Branch which was used while building.
                                      type: string
                                    buildType:
                                      description: |-
                                        (String) Build type of the pipeline. Possible values: branch.
                                        Build type of the pipeline. Possible values: branch.
                                      type: string
                                    ciExecutionInfo:
                                      description: |-
                                        (Block List, Max: 1) CI execution info for the pipeline. (see below for nested schema)
                                        CI execution info for the pipeline.
                                      items:
                                        properties:
                                          event:
                                            description: |-
                                              (String) Event for the ci execution, Possible values: pullRequest.
                                              Event for the ci execution, Possible values: pullRequest.
                                            type: string
                                          pullRequest:
                                            description: |-
                                              (Block List, Max: 1) The pull request details of the CI pipeline. (see below for nested schema)
                                              The pull request details of the CI pipeline.
                                            items:
                                              properties:
                                                sourceBranch:
                                                  description: |-
                                                    (String) Source branch of the pull request.
                                                    Source branch of the pull request.
                                                  type: string
                                                targetBranch:
                                                  description: |-
                                                    (String) Target branch of the pull request.
                                                    Target branch of the pull request.
                                                  type: string
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                    repoNames:
                                      description: |-
                                        (String) name of the repository used in the pipeline.
                                        name of the repository used in the pipeline.
                                      type: string
                                    tag:
                                      description: |-
                                        (String) Tags to associate with the CI pipeline resource.
                                        Tags to associate with the CI pipeline resource.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        name:
                          description: |-
                            (String) Name of the pipeline filters.
                            Name of the pipeline filter.
                          type: string
                        pipelineIdentifiers:
                          description: |-
                            (List of String) Pipeline identifiers to filter on.
                            Pipeline identifiers to filter on.
                          items:
                            type: string
                          type: array
                        pipelineName:
                          description: |-
                            (String) Name of the pipeline execution filter.
                            Name of the pipeline execution filter.
                          type: string
                        pipelineTags:
                          description: |-
                            (List of Map of String) Tags to associate with the pipeline. tags should be in the form of {key:key1, value:key1value}
                            Tags to associate with the pipeline. tags should be in the form of `{key:key1, value:key1value}`
                          items:
                            additionalProperties:
                              type: string
                            type: object
                          type: array
                        tags:
                          description: |-
                            (Set of String) Tags to associate with the resource. Tags should be in the form name:value.
                            Tags to associate with the resource. Tags should be in the form `name:value`.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  filterVisibility:
                    description: |-
                      (String) This indicates visibility of filters. By default, everyone can view this filter.
                      This indicates visibility of filters. By default, everyone can view this filter.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  identifier:
                    description: |-
                      (String) Unique identifier of the resource.
                      Unique identifier of the resource.
                    type: string
                  name:
                    description: |-
                      (String) Name of the pipeline filters.
                      Name of the pipeline filters.
                    type: string
                  orgId:
                    description: |-
                      (String) Organization Identifier for the Entity.
                      Organization Identifier for the Entity.
                    type: string
                  projectId:
                    description: |-
                      (String) Project Identifier for the Entity.
                      Project Identifier for the Entity.
                    type: string
                  type:
                    description: |-
                      (String) Type of pipeline filters. Currently supported types are {PipelineSetup, PipelineExecution, Deployment, Template, EnvironmentGroup, Environment}
                      Type of pipeline filters. Currently supported types are {PipelineSetup, PipelineExecution, Deployment, Template, EnvironmentGroup, Environment}
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
