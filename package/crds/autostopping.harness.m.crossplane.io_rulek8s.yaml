---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: rulek8s.autostopping.harness.m.crossplane.io
spec:
  group: autostopping.harness.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - harness
    kind: RuleK8s
    listKind: RuleK8sList
    plural: rulek8s
    singular: rulek8s
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RuleK8s is the Schema for the RuleK8ss API. Data source for retrieving
          a Harness AutoStopping rule for K8s services.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RuleK8sSpec defines the desired state of RuleK8s
            properties:
              forProvider:
                properties:
                  cloudConnectorId:
                    description: |-
                      (String) Id of the cloud connector
                      Id of the cloud connector
                    type: string
                  depends:
                    description: |-
                      (Block List) Dependent rules (see below for nested schema)
                      Dependent rules
                    items:
                      properties:
                        delayInSec:
                          description: |-
                            (Number) Number of seconds the rule should wait after warming up the dependent rule
                            Number of seconds the rule should wait after warming up the dependent rule
                          type: number
                        ruleId:
                          description: |-
                            (Number) Rule id of the dependent rule
                            Rule id of the dependent rule
                          type: number
                      type: object
                    type: array
                  dryRun:
                    description: |-
                      (Boolean) Boolean that indicates whether the AutoStopping rule should be created in DryRun mode
                      Boolean that indicates whether the AutoStopping rule should be created in DryRun mode
                    type: boolean
                  idleTimeMins:
                    description: |-
                      (Number) Idle time in minutes. This is the time that the AutoStopping rule waits before stopping the idle instances.
                      Idle time in minutes. This is the time that the AutoStopping rule waits before stopping the idle instances.
                    type: number
                  k8sConnectorId:
                    description: |-
                      (String) Id of the K8s connector
                      Id of the K8s connector
                    type: string
                  k8sNamespace:
                    description: |-
                      (String) Namespace of the cluster
                      Namespace of the cluster
                    type: string
                  name:
                    description: |-
                      (String) Name of the rule
                      Name of the rule
                    type: string
                  ruleYaml:
                    description: |-
                      (String) YAML definition of the K8s AutoStopping rule (workload selector, ingress, etc.).
                      YAML definition of the K8s AutoStopping rule (workload selector, ingress, etc.).
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  cloudConnectorId:
                    description: |-
                      (String) Id of the cloud connector
                      Id of the cloud connector
                    type: string
                  depends:
                    description: |-
                      (Block List) Dependent rules (see below for nested schema)
                      Dependent rules
                    items:
                      properties:
                        delayInSec:
                          description: |-
                            (Number) Number of seconds the rule should wait after warming up the dependent rule
                            Number of seconds the rule should wait after warming up the dependent rule
                          type: number
                        ruleId:
                          description: |-
                            (Number) Rule id of the dependent rule
                            Rule id of the dependent rule
                          type: number
                      type: object
                    type: array
                  dryRun:
                    description: |-
                      (Boolean) Boolean that indicates whether the AutoStopping rule should be created in DryRun mode
                      Boolean that indicates whether the AutoStopping rule should be created in DryRun mode
                    type: boolean
                  idleTimeMins:
                    description: |-
                      (Number) Idle time in minutes. This is the time that the AutoStopping rule waits before stopping the idle instances.
                      Idle time in minutes. This is the time that the AutoStopping rule waits before stopping the idle instances.
                    type: number
                  k8sConnectorId:
                    description: |-
                      (String) Id of the K8s connector
                      Id of the K8s connector
                    type: string
                  k8sNamespace:
                    description: |-
                      (String) Namespace of the cluster
                      Namespace of the cluster
                    type: string
                  name:
                    description: |-
                      (String) Name of the rule
                      Name of the rule
                    type: string
                  ruleYaml:
                    description: |-
                      (String) YAML definition of the K8s AutoStopping rule (workload selector, ingress, etc.).
                      YAML definition of the K8s AutoStopping rule (workload selector, ingress, etc.).
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.cloudConnectorId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.cloudConnectorId)
                || (has(self.initProvider) && has(self.initProvider.cloudConnectorId))'
            - message: spec.forProvider.k8sConnectorId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.k8sConnectorId)
                || (has(self.initProvider) && has(self.initProvider.k8sConnectorId))'
            - message: spec.forProvider.k8sNamespace is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.k8sNamespace)
                || (has(self.initProvider) && has(self.initProvider.k8sNamespace))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.ruleYaml is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.ruleYaml)
                || (has(self.initProvider) && has(self.initProvider.ruleYaml))'
          status:
            description: RuleK8sStatus defines the observed state of RuleK8s.
            properties:
              atProvider:
                properties:
                  cloudConnectorId:
                    description: |-
                      (String) Id of the cloud connector
                      Id of the cloud connector
                    type: string
                  depends:
                    description: |-
                      (Block List) Dependent rules (see below for nested schema)
                      Dependent rules
                    items:
                      properties:
                        delayInSec:
                          description: |-
                            (Number) Number of seconds the rule should wait after warming up the dependent rule
                            Number of seconds the rule should wait after warming up the dependent rule
                          type: number
                        ruleId:
                          description: |-
                            (Number) Rule id of the dependent rule
                            Rule id of the dependent rule
                          type: number
                      type: object
                    type: array
                  dryRun:
                    description: |-
                      (Boolean) Boolean that indicates whether the AutoStopping rule should be created in DryRun mode
                      Boolean that indicates whether the AutoStopping rule should be created in DryRun mode
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  identifier:
                    description: |-
                      (String) Unique identifier of the resource
                      Unique identifier of the resource
                    type: string
                  idleTimeMins:
                    description: |-
                      (Number) Idle time in minutes. This is the time that the AutoStopping rule waits before stopping the idle instances.
                      Idle time in minutes. This is the time that the AutoStopping rule waits before stopping the idle instances.
                    type: number
                  k8sConnectorId:
                    description: |-
                      (String) Id of the K8s connector
                      Id of the K8s connector
                    type: string
                  k8sNamespace:
                    description: |-
                      (String) Namespace of the cluster
                      Namespace of the cluster
                    type: string
                  name:
                    description: |-
                      (String) Name of the rule
                      Name of the rule
                    type: string
                  ruleYaml:
                    description: |-
                      (String) YAML definition of the K8s AutoStopping rule (workload selector, ingress, etc.).
                      YAML definition of the K8s AutoStopping rule (workload selector, ingress, etc.).
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
