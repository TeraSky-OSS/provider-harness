---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: gcpproxies.autostopping.harness.crossplane.io
spec:
  group: autostopping.harness.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - harness
    kind: GcpProxy
    listKind: GcpProxyList
    plural: gcpproxies
    singular: gcpproxy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GcpProxy is the Schema for the GcpProxys API. Resource for creating
          an GCP Autostopping proxy
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GcpProxySpec defines the desired state of GcpProxy
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  allocateStaticIp:
                    description: |-
                      (Boolean) Boolean value to indicate if proxy vm needs to have static IP
                      Boolean value to indicate if proxy vm needs to have static IP
                    type: boolean
                  apiKeySecretRef:
                    description: |-
                      (String, Sensitive) Harness NG API key
                      Harness NG API key
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  certificates:
                    description: '(Block List, Max: 1) (see below for nested schema)'
                    items:
                      properties:
                        certSecretId:
                          description: |-
                            (String) Certificate secret ID
                            Certificate secret ID
                          type: string
                        keySecretId:
                          description: |-
                            (String) Private key secret ID
                            Private key secret ID
                          type: string
                      type: object
                    type: array
                  cloudConnectorId:
                    description: |-
                      (String) Id of the cloud connector
                      Id of the cloud connector
                    type: string
                  deleteCloudResourcesOnDestroy:
                    description: When set to true, the associated VM will be deleted
                      permanently from GCP account. Be fully aware of the consequneces
                      of settting this to true, as the action is irreversible. When
                      set to false, solely the Harness LB representation will be deleted,
                      which leaves the proxy VM in GCP account itself. When set to
                      true, the associated VM will be deleted permanently from GCP
                      account. Be fully aware of the consequneces of settting this
                      to true, as the action is irreversible. When set to false, solely
                      the Harness LB representation will be deleted, which leaves
                      the proxy VM in GCP account itself.
                    type: boolean
                  machineType:
                    description: |-
                      (String) Machine instance type
                      Machine instance type
                    type: string
                  name:
                    description: |-
                      (String) Name of the proxy
                      Name of the proxy
                    type: string
                  region:
                    description: |-
                      (String) Region in which cloud resources are hosted
                      Region in which cloud resources are hosted
                    type: string
                  securityGroups:
                    description: |-
                      (List of String) Security Group to define the security rules that determine the inbound and outbound traffic
                      Security Group to define the security rules that determine the inbound and outbound traffic
                    items:
                      type: string
                    type: array
                  subnetId:
                    description: |-
                      (String) VPC in which cloud resources are hosted
                      VPC in which cloud resources are hosted
                    type: string
                  vpc:
                    description: |-
                      (String) VPC in which cloud resources are hosted
                      VPC in which cloud resources are hosted
                    type: string
                  zone:
                    description: |-
                      (String) Zone in which cloud resources are hosted
                      Zone in which cloud resources are hosted
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  allocateStaticIp:
                    description: |-
                      (Boolean) Boolean value to indicate if proxy vm needs to have static IP
                      Boolean value to indicate if proxy vm needs to have static IP
                    type: boolean
                  apiKeySecretRef:
                    description: |-
                      (String, Sensitive) Harness NG API key
                      Harness NG API key
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  certificates:
                    description: '(Block List, Max: 1) (see below for nested schema)'
                    items:
                      properties:
                        certSecretId:
                          description: |-
                            (String) Certificate secret ID
                            Certificate secret ID
                          type: string
                        keySecretId:
                          description: |-
                            (String) Private key secret ID
                            Private key secret ID
                          type: string
                      type: object
                    type: array
                  cloudConnectorId:
                    description: |-
                      (String) Id of the cloud connector
                      Id of the cloud connector
                    type: string
                  deleteCloudResourcesOnDestroy:
                    description: When set to true, the associated VM will be deleted
                      permanently from GCP account. Be fully aware of the consequneces
                      of settting this to true, as the action is irreversible. When
                      set to false, solely the Harness LB representation will be deleted,
                      which leaves the proxy VM in GCP account itself. When set to
                      true, the associated VM will be deleted permanently from GCP
                      account. Be fully aware of the consequneces of settting this
                      to true, as the action is irreversible. When set to false, solely
                      the Harness LB representation will be deleted, which leaves
                      the proxy VM in GCP account itself.
                    type: boolean
                  machineType:
                    description: |-
                      (String) Machine instance type
                      Machine instance type
                    type: string
                  name:
                    description: |-
                      (String) Name of the proxy
                      Name of the proxy
                    type: string
                  region:
                    description: |-
                      (String) Region in which cloud resources are hosted
                      Region in which cloud resources are hosted
                    type: string
                  securityGroups:
                    description: |-
                      (List of String) Security Group to define the security rules that determine the inbound and outbound traffic
                      Security Group to define the security rules that determine the inbound and outbound traffic
                    items:
                      type: string
                    type: array
                  subnetId:
                    description: |-
                      (String) VPC in which cloud resources are hosted
                      VPC in which cloud resources are hosted
                    type: string
                  vpc:
                    description: |-
                      (String) VPC in which cloud resources are hosted
                      VPC in which cloud resources are hosted
                    type: string
                  zone:
                    description: |-
                      (String) Zone in which cloud resources are hosted
                      Zone in which cloud resources are hosted
                    type: string
                required:
                - apiKeySecretRef
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.apiKeySecretRef is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.apiKeySecretRef)'
            - message: spec.forProvider.cloudConnectorId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.cloudConnectorId)
                || (has(self.initProvider) && has(self.initProvider.cloudConnectorId))'
            - message: spec.forProvider.deleteCloudResourcesOnDestroy is a required
                parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.deleteCloudResourcesOnDestroy)
                || (has(self.initProvider) && has(self.initProvider.deleteCloudResourcesOnDestroy))'
            - message: spec.forProvider.machineType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.machineType)
                || (has(self.initProvider) && has(self.initProvider.machineType))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.region is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.region)
                || (has(self.initProvider) && has(self.initProvider.region))'
            - message: spec.forProvider.subnetId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.subnetId)
                || (has(self.initProvider) && has(self.initProvider.subnetId))'
            - message: spec.forProvider.vpc is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.vpc)
                || (has(self.initProvider) && has(self.initProvider.vpc))'
            - message: spec.forProvider.zone is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.zone)
                || (has(self.initProvider) && has(self.initProvider.zone))'
          status:
            description: GcpProxyStatus defines the observed state of GcpProxy.
            properties:
              atProvider:
                properties:
                  allocateStaticIp:
                    description: |-
                      (Boolean) Boolean value to indicate if proxy vm needs to have static IP
                      Boolean value to indicate if proxy vm needs to have static IP
                    type: boolean
                  certificates:
                    description: '(Block List, Max: 1) (see below for nested schema)'
                    items:
                      properties:
                        certSecretId:
                          description: |-
                            (String) Certificate secret ID
                            Certificate secret ID
                          type: string
                        keySecretId:
                          description: |-
                            (String) Private key secret ID
                            Private key secret ID
                          type: string
                      type: object
                    type: array
                  cloudConnectorId:
                    description: |-
                      (String) Id of the cloud connector
                      Id of the cloud connector
                    type: string
                  deleteCloudResourcesOnDestroy:
                    description: When set to true, the associated VM will be deleted
                      permanently from GCP account. Be fully aware of the consequneces
                      of settting this to true, as the action is irreversible. When
                      set to false, solely the Harness LB representation will be deleted,
                      which leaves the proxy VM in GCP account itself. When set to
                      true, the associated VM will be deleted permanently from GCP
                      account. Be fully aware of the consequneces of settting this
                      to true, as the action is irreversible. When set to false, solely
                      the Harness LB representation will be deleted, which leaves
                      the proxy VM in GCP account itself.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  identifier:
                    description: |-
                      (String) Unique identifier of the resource
                      Unique identifier of the resource
                    type: string
                  machineType:
                    description: |-
                      (String) Machine instance type
                      Machine instance type
                    type: string
                  name:
                    description: |-
                      (String) Name of the proxy
                      Name of the proxy
                    type: string
                  privateIp:
                    description: |-
                      (String) Private IP address of the proxy
                      Private IP address of the proxy
                    type: string
                  publicIp:
                    description: |-
                      (String) Public IP address of the proxy
                      Public IP address of the proxy
                    type: string
                  region:
                    description: |-
                      (String) Region in which cloud resources are hosted
                      Region in which cloud resources are hosted
                    type: string
                  securityGroups:
                    description: |-
                      (List of String) Security Group to define the security rules that determine the inbound and outbound traffic
                      Security Group to define the security rules that determine the inbound and outbound traffic
                    items:
                      type: string
                    type: array
                  subnetId:
                    description: |-
                      (String) VPC in which cloud resources are hosted
                      VPC in which cloud resources are hosted
                    type: string
                  vpc:
                    description: |-
                      (String) VPC in which cloud resources are hosted
                      VPC in which cloud resources are hosted
                    type: string
                  zone:
                    description: |-
                      (String) Zone in which cloud resources are hosted
                      Zone in which cloud resources are hosted
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
