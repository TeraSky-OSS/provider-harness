---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: vaults.connector.harness.m.crossplane.io
spec:
  group: connector.harness.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - harness
    kind: Vault
    listKind: VaultList
    plural: vaults
    singular: vault
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Vault is the Schema for the Vaults API. Resource for creating
          a HashiCorp Vault Secret Manager connector.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VaultSpec defines the desired state of Vault
            properties:
              forProvider:
                properties:
                  accessType:
                    description: |-
                      (String) Access type.
                      Access type.
                    type: string
                  appRoleId:
                    description: |-
                      (String) ID of App Role.
                      ID of App Role.
                    type: string
                  authToken:
                    description: |-
                      (String) Authentication token for Vault.
                      Authentication token for Vault.
                    type: string
                  awsRegion:
                    description: |-
                      (String) AWS region where the AWS IAM authentication will happen.
                      AWS region where the AWS IAM authentication will happen.
                    type: string
                  basePath:
                    description: |-
                      (String) Location of the Vault directory where the secret will be stored.
                      Location of the Vault directory where the secret will be stored.
                    type: string
                  default:
                    description: |-
                      (Boolean) Is default or not.
                      Is default or not.
                    type: boolean
                  delegateSelectors:
                    description: |-
                      (Set of String) List of Delegate Selectors that belong to the same Delegate and are used to connect to the Secret Manager.
                      List of Delegate Selectors that belong to the same Delegate and are used to connect to the Secret Manager.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  description:
                    description: |-
                      (String) Description of the resource.
                      Description of the resource.
                    type: string
                  executeOnDelegate:
                    description: |-
                      (Boolean) Execute on delegate or not.
                      Execute on delegate or not.
                    type: boolean
                  identifier:
                    description: |-
                      (String) Unique identifier of the resource.
                      Unique identifier of the resource.
                    type: string
                  isDefault:
                    description: |-
                      (Boolean) Is default or not.
                      Is default or not.
                    type: boolean
                  isReadOnly:
                    description: |-
                      (Boolean) Read only or not.
                      Read only or not.
                    type: boolean
                  k8sAuthEndpoint:
                    description: |-
                      (String) The path where Kubernetes Auth is enabled in Vault.
                      The path where Kubernetes Auth is enabled in Vault.
                    type: string
                  name:
                    description: |-
                      (String) Name of the resource.
                      Name of the resource.
                    type: string
                  namespace:
                    description: |-
                      (String) Vault namespace where the Secret will be created.
                      Vault namespace where the Secret will be created.
                    type: string
                  orgId:
                    description: |-
                      (String) Unique identifier of the organization.
                      Unique identifier of the organization.
                    type: string
                  orgIdRef:
                    description: Reference to a Organization in platform to populate
                      orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  orgIdSelector:
                    description: Selector for a Organization in platform to populate
                      orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  projectId:
                    description: |-
                      (String) Unique identifier of the project.
                      Unique identifier of the project.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in platform to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in platform to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  readOnly:
                    description: |-
                      (Boolean) Read only.
                      Read only.
                    type: boolean
                  renewAppRoleToken:
                    description: |-
                      (Boolean) Boolean value to indicate if AppRole token renewal is enabled or not.
                      Boolean value to indicate if AppRole token renewal is enabled or not.
                    type: boolean
                  renewalIntervalMinutes:
                    description: |-
                      (Number) The time interval for the token renewal.
                      The time interval for the token renewal.
                    type: number
                  secretEngineManuallyConfigured:
                    description: |-
                      (Boolean) Manually entered Secret Engine.
                      Manually entered Secret Engine.
                    type: boolean
                  secretEngineName:
                    description: |-
                      (String) Name of the Secret Engine.
                      Name of the Secret Engine.
                    type: string
                  secretEngineVersion:
                    description: |-
                      (Number) Version of Secret Engine.
                      Version of Secret Engine.
                    type: number
                  secretId:
                    description: |-
                      (String) ID of the Secret.
                      ID of the Secret.
                    type: string
                  serviceAccountTokenPath:
                    description: |-
                      (String) The Service Account token path in the K8s pod where the token is mounted.
                      The Service Account token path in the K8s pod where the token is mounted.
                    type: string
                  sinkPath:
                    description: |-
                      (String) The location from which the authentication token should be read.
                      The location from which the authentication token should be read.
                    type: string
                  tags:
                    description: |-
                      (Set of String) Tags to associate with the resource.
                      Tags to associate with the resource.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  useAwsIam:
                    description: |-
                      (Boolean) Boolean value to indicate if AWS IAM is used for authentication.
                      Boolean value to indicate if AWS IAM is used for authentication.
                    type: boolean
                  useJwtAuth:
                    description: |-
                      (Boolean) Boolean value to indicate if JWT is used for authentication.
                      Boolean value to indicate if JWT is used for authentication.
                    type: boolean
                  useK8SAuth:
                    description: |-
                      (Boolean) Boolean value to indicate if K8s Auth is used for authentication.
                      Boolean value to indicate if K8s Auth is used for authentication.
                    type: boolean
                  useVaultAgent:
                    description: |-
                      (Boolean) Boolean value to indicate if Vault Agent is used for authentication.
                      Boolean value to indicate if Vault Agent is used for authentication.
                    type: boolean
                  vaultAwsIamRole:
                    description: |-
                      (String) The Vault role defined to bind to aws iam account/role being accessed.
                      The Vault role defined to bind to aws iam account/role being accessed.
                    type: string
                  vaultJwtAuthPath:
                    description: |-
                      (String) Custom path at with JWT auth in enabled for Vault
                      Custom path at with JWT auth in enabled for Vault
                    type: string
                  vaultJwtAuthRole:
                    description: |-
                      (String) The Vault role defined with JWT auth type for accessing Vault as per policies binded.
                      The Vault role defined with JWT auth type for accessing Vault as per policies binded.
                    type: string
                  vaultK8SAuthRole:
                    description: |-
                      (String) The role where K8s Auth will happen.
                      The role where K8s Auth will happen.
                    type: string
                  vaultUrl:
                    description: |-
                      (String) URL of the HashiCorp Vault.
                      URL of the HashiCorp Vault.
                    type: string
                  xvaultAwsIamServerId:
                    description: |-
                      (String) The AWS IAM Header Server ID that has been configured for this AWS IAM instance.
                      The AWS IAM Header Server ID that has been configured for this AWS IAM instance.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accessType:
                    description: |-
                      (String) Access type.
                      Access type.
                    type: string
                  appRoleId:
                    description: |-
                      (String) ID of App Role.
                      ID of App Role.
                    type: string
                  authToken:
                    description: |-
                      (String) Authentication token for Vault.
                      Authentication token for Vault.
                    type: string
                  awsRegion:
                    description: |-
                      (String) AWS region where the AWS IAM authentication will happen.
                      AWS region where the AWS IAM authentication will happen.
                    type: string
                  basePath:
                    description: |-
                      (String) Location of the Vault directory where the secret will be stored.
                      Location of the Vault directory where the secret will be stored.
                    type: string
                  default:
                    description: |-
                      (Boolean) Is default or not.
                      Is default or not.
                    type: boolean
                  delegateSelectors:
                    description: |-
                      (Set of String) List of Delegate Selectors that belong to the same Delegate and are used to connect to the Secret Manager.
                      List of Delegate Selectors that belong to the same Delegate and are used to connect to the Secret Manager.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  description:
                    description: |-
                      (String) Description of the resource.
                      Description of the resource.
                    type: string
                  executeOnDelegate:
                    description: |-
                      (Boolean) Execute on delegate or not.
                      Execute on delegate or not.
                    type: boolean
                  identifier:
                    description: |-
                      (String) Unique identifier of the resource.
                      Unique identifier of the resource.
                    type: string
                  isDefault:
                    description: |-
                      (Boolean) Is default or not.
                      Is default or not.
                    type: boolean
                  isReadOnly:
                    description: |-
                      (Boolean) Read only or not.
                      Read only or not.
                    type: boolean
                  k8sAuthEndpoint:
                    description: |-
                      (String) The path where Kubernetes Auth is enabled in Vault.
                      The path where Kubernetes Auth is enabled in Vault.
                    type: string
                  name:
                    description: |-
                      (String) Name of the resource.
                      Name of the resource.
                    type: string
                  namespace:
                    description: |-
                      (String) Vault namespace where the Secret will be created.
                      Vault namespace where the Secret will be created.
                    type: string
                  orgId:
                    description: |-
                      (String) Unique identifier of the organization.
                      Unique identifier of the organization.
                    type: string
                  orgIdRef:
                    description: Reference to a Organization in platform to populate
                      orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  orgIdSelector:
                    description: Selector for a Organization in platform to populate
                      orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  projectId:
                    description: |-
                      (String) Unique identifier of the project.
                      Unique identifier of the project.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in platform to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in platform to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  readOnly:
                    description: |-
                      (Boolean) Read only.
                      Read only.
                    type: boolean
                  renewAppRoleToken:
                    description: |-
                      (Boolean) Boolean value to indicate if AppRole token renewal is enabled or not.
                      Boolean value to indicate if AppRole token renewal is enabled or not.
                    type: boolean
                  renewalIntervalMinutes:
                    description: |-
                      (Number) The time interval for the token renewal.
                      The time interval for the token renewal.
                    type: number
                  secretEngineManuallyConfigured:
                    description: |-
                      (Boolean) Manually entered Secret Engine.
                      Manually entered Secret Engine.
                    type: boolean
                  secretEngineName:
                    description: |-
                      (String) Name of the Secret Engine.
                      Name of the Secret Engine.
                    type: string
                  secretEngineVersion:
                    description: |-
                      (Number) Version of Secret Engine.
                      Version of Secret Engine.
                    type: number
                  secretId:
                    description: |-
                      (String) ID of the Secret.
                      ID of the Secret.
                    type: string
                  serviceAccountTokenPath:
                    description: |-
                      (String) The Service Account token path in the K8s pod where the token is mounted.
                      The Service Account token path in the K8s pod where the token is mounted.
                    type: string
                  sinkPath:
                    description: |-
                      (String) The location from which the authentication token should be read.
                      The location from which the authentication token should be read.
                    type: string
                  tags:
                    description: |-
                      (Set of String) Tags to associate with the resource.
                      Tags to associate with the resource.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  useAwsIam:
                    description: |-
                      (Boolean) Boolean value to indicate if AWS IAM is used for authentication.
                      Boolean value to indicate if AWS IAM is used for authentication.
                    type: boolean
                  useJwtAuth:
                    description: |-
                      (Boolean) Boolean value to indicate if JWT is used for authentication.
                      Boolean value to indicate if JWT is used for authentication.
                    type: boolean
                  useK8SAuth:
                    description: |-
                      (Boolean) Boolean value to indicate if K8s Auth is used for authentication.
                      Boolean value to indicate if K8s Auth is used for authentication.
                    type: boolean
                  useVaultAgent:
                    description: |-
                      (Boolean) Boolean value to indicate if Vault Agent is used for authentication.
                      Boolean value to indicate if Vault Agent is used for authentication.
                    type: boolean
                  vaultAwsIamRole:
                    description: |-
                      (String) The Vault role defined to bind to aws iam account/role being accessed.
                      The Vault role defined to bind to aws iam account/role being accessed.
                    type: string
                  vaultJwtAuthPath:
                    description: |-
                      (String) Custom path at with JWT auth in enabled for Vault
                      Custom path at with JWT auth in enabled for Vault
                    type: string
                  vaultJwtAuthRole:
                    description: |-
                      (String) The Vault role defined with JWT auth type for accessing Vault as per policies binded.
                      The Vault role defined with JWT auth type for accessing Vault as per policies binded.
                    type: string
                  vaultK8SAuthRole:
                    description: |-
                      (String) The role where K8s Auth will happen.
                      The role where K8s Auth will happen.
                    type: string
                  vaultUrl:
                    description: |-
                      (String) URL of the HashiCorp Vault.
                      URL of the HashiCorp Vault.
                    type: string
                  xvaultAwsIamServerId:
                    description: |-
                      (String) The AWS IAM Header Server ID that has been configured for this AWS IAM instance.
                      The AWS IAM Header Server ID that has been configured for this AWS IAM instance.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.identifier is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.identifier)
                || (has(self.initProvider) && has(self.initProvider.identifier))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.renewalIntervalMinutes is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.renewalIntervalMinutes)
                || (has(self.initProvider) && has(self.initProvider.renewalIntervalMinutes))'
            - message: spec.forProvider.vaultUrl is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.vaultUrl)
                || (has(self.initProvider) && has(self.initProvider.vaultUrl))'
          status:
            description: VaultStatus defines the observed state of Vault.
            properties:
              atProvider:
                properties:
                  accessType:
                    description: |-
                      (String) Access type.
                      Access type.
                    type: string
                  appRoleId:
                    description: |-
                      (String) ID of App Role.
                      ID of App Role.
                    type: string
                  authToken:
                    description: |-
                      (String) Authentication token for Vault.
                      Authentication token for Vault.
                    type: string
                  awsRegion:
                    description: |-
                      (String) AWS region where the AWS IAM authentication will happen.
                      AWS region where the AWS IAM authentication will happen.
                    type: string
                  basePath:
                    description: |-
                      (String) Location of the Vault directory where the secret will be stored.
                      Location of the Vault directory where the secret will be stored.
                    type: string
                  default:
                    description: |-
                      (Boolean) Is default or not.
                      Is default or not.
                    type: boolean
                  delegateSelectors:
                    description: |-
                      (Set of String) List of Delegate Selectors that belong to the same Delegate and are used to connect to the Secret Manager.
                      List of Delegate Selectors that belong to the same Delegate and are used to connect to the Secret Manager.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  description:
                    description: |-
                      (String) Description of the resource.
                      Description of the resource.
                    type: string
                  executeOnDelegate:
                    description: |-
                      (Boolean) Execute on delegate or not.
                      Execute on delegate or not.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  identifier:
                    description: |-
                      (String) Unique identifier of the resource.
                      Unique identifier of the resource.
                    type: string
                  isDefault:
                    description: |-
                      (Boolean) Is default or not.
                      Is default or not.
                    type: boolean
                  isReadOnly:
                    description: |-
                      (Boolean) Read only or not.
                      Read only or not.
                    type: boolean
                  k8sAuthEndpoint:
                    description: |-
                      (String) The path where Kubernetes Auth is enabled in Vault.
                      The path where Kubernetes Auth is enabled in Vault.
                    type: string
                  name:
                    description: |-
                      (String) Name of the resource.
                      Name of the resource.
                    type: string
                  namespace:
                    description: |-
                      (String) Vault namespace where the Secret will be created.
                      Vault namespace where the Secret will be created.
                    type: string
                  orgId:
                    description: |-
                      (String) Unique identifier of the organization.
                      Unique identifier of the organization.
                    type: string
                  projectId:
                    description: |-
                      (String) Unique identifier of the project.
                      Unique identifier of the project.
                    type: string
                  readOnly:
                    description: |-
                      (Boolean) Read only.
                      Read only.
                    type: boolean
                  renewAppRoleToken:
                    description: |-
                      (Boolean) Boolean value to indicate if AppRole token renewal is enabled or not.
                      Boolean value to indicate if AppRole token renewal is enabled or not.
                    type: boolean
                  renewalIntervalMinutes:
                    description: |-
                      (Number) The time interval for the token renewal.
                      The time interval for the token renewal.
                    type: number
                  secretEngineManuallyConfigured:
                    description: |-
                      (Boolean) Manually entered Secret Engine.
                      Manually entered Secret Engine.
                    type: boolean
                  secretEngineName:
                    description: |-
                      (String) Name of the Secret Engine.
                      Name of the Secret Engine.
                    type: string
                  secretEngineVersion:
                    description: |-
                      (Number) Version of Secret Engine.
                      Version of Secret Engine.
                    type: number
                  secretId:
                    description: |-
                      (String) ID of the Secret.
                      ID of the Secret.
                    type: string
                  serviceAccountTokenPath:
                    description: |-
                      (String) The Service Account token path in the K8s pod where the token is mounted.
                      The Service Account token path in the K8s pod where the token is mounted.
                    type: string
                  sinkPath:
                    description: |-
                      (String) The location from which the authentication token should be read.
                      The location from which the authentication token should be read.
                    type: string
                  tags:
                    description: |-
                      (Set of String) Tags to associate with the resource.
                      Tags to associate with the resource.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  useAwsIam:
                    description: |-
                      (Boolean) Boolean value to indicate if AWS IAM is used for authentication.
                      Boolean value to indicate if AWS IAM is used for authentication.
                    type: boolean
                  useJwtAuth:
                    description: |-
                      (Boolean) Boolean value to indicate if JWT is used for authentication.
                      Boolean value to indicate if JWT is used for authentication.
                    type: boolean
                  useK8SAuth:
                    description: |-
                      (Boolean) Boolean value to indicate if K8s Auth is used for authentication.
                      Boolean value to indicate if K8s Auth is used for authentication.
                    type: boolean
                  useVaultAgent:
                    description: |-
                      (Boolean) Boolean value to indicate if Vault Agent is used for authentication.
                      Boolean value to indicate if Vault Agent is used for authentication.
                    type: boolean
                  vaultAwsIamRole:
                    description: |-
                      (String) The Vault role defined to bind to aws iam account/role being accessed.
                      The Vault role defined to bind to aws iam account/role being accessed.
                    type: string
                  vaultJwtAuthPath:
                    description: |-
                      (String) Custom path at with JWT auth in enabled for Vault
                      Custom path at with JWT auth in enabled for Vault
                    type: string
                  vaultJwtAuthRole:
                    description: |-
                      (String) The Vault role defined with JWT auth type for accessing Vault as per policies binded.
                      The Vault role defined with JWT auth type for accessing Vault as per policies binded.
                    type: string
                  vaultK8SAuthRole:
                    description: |-
                      (String) The role where K8s Auth will happen.
                      The role where K8s Auth will happen.
                    type: string
                  vaultUrl:
                    description: |-
                      (String) URL of the HashiCorp Vault.
                      URL of the HashiCorp Vault.
                    type: string
                  xvaultAwsIamServerId:
                    description: |-
                      (String) The AWS IAM Header Server ID that has been configured for this AWS IAM instance.
                      The AWS IAM Header Server ID that has been configured for this AWS IAM instance.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
