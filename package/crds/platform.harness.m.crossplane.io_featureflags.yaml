---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: featureflags.platform.harness.m.crossplane.io
spec:
  group: platform.harness.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - harness
    kind: FeatureFlag
    listKind: FeatureFlagList
    plural: featureflags
    singular: featureflag
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FeatureFlag is the Schema for the FeatureFlags API. Resource
          for managing Feature Flags.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FeatureFlagSpec defines the desired state of FeatureFlag
            properties:
              forProvider:
                properties:
                  archived:
                    description: |-
                      (Boolean) Whether or not the flag is archived.
                      Whether or not the flag is archived.
                    type: boolean
                  defaultOffVariation:
                    description: |-
                      (String) Which of the variations to use when the flag is toggled to off state
                      Which of the variations to use when the flag is toggled to off state
                    type: string
                  defaultOnVariation:
                    description: |-
                      (String) Which of the variations to use when the flag is toggled to on state
                      Which of the variations to use when the flag is toggled to on state
                    type: string
                  description:
                    description: |-
                      (String) Description of the Feature Flag
                      Description of the Feature Flag
                    type: string
                  environment:
                    description: |-
                      (Block List) Environment Identifier (see below for nested schema)
                      Environment Identifier
                    items:
                      properties:
                        addTargetRule:
                          description: |-
                            (Block List) The targeting rules for the flag (see below for nested schema)
                            The targeting rules for the flag
                          items:
                            properties:
                              targets:
                                description: |-
                                  (List of String) The targets that should be served this variation
                                  The targets that should be served this variation
                                items:
                                  type: string
                                type: array
                              variation:
                                description: |-
                                  (Block List, Min: 2) The options available for your flag (see below for nested schema)
                                  The identifier of the variation
                                type: string
                            type: object
                          type: array
                        defaultOffVariation:
                          description: |-
                            (String) Which of the variations to use when the flag is toggled to off state
                            Default variation to be served when flag is 'off'
                          type: string
                        defaultOnVariation:
                          description: |-
                            (String) Which of the variations to use when the flag is toggled to on state
                            Default variation to be served when flag is 'on'
                          type: string
                        identifier:
                          description: |-
                            (String) Identifier of the Feature Flag
                            Identifier of the Environment
                          type: string
                        state:
                          description: |-
                            (String) State of the flag in this environment. Possible values are 'on' and 'off'
                            State of the flag in this environment. Possible values are 'on' and 'off'
                          type: string
                      type: object
                    type: array
                  identifier:
                    description: |-
                      (String) Identifier of the Feature Flag
                      Identifier of the Feature Flag
                    type: string
                  kind:
                    description: |-
                      (String) The type of data the flag represents. Valid values are boolean, int, string, json
                      The type of data the flag represents. Valid values are `boolean`, `int`, `string`, `json`
                    type: string
                  name:
                    description: |-
                      (String) Name of the Feature Flag
                      Name of the Feature Flag
                    type: string
                  orgId:
                    description: |-
                      (String) Organization Identifier
                      Organization Identifier
                    type: string
                  orgIdRef:
                    description: Reference to a Organization in platform to populate
                      orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  orgIdSelector:
                    description: Selector for a Organization in platform to populate
                      orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  owner:
                    description: |-
                      (String) The owner of the flag
                      The owner of the flag
                    type: string
                  permanent:
                    description: |-
                      (Boolean) Whether or not the flag is permanent. If it is, it will never be flagged as stale
                      Whether or not the flag is permanent. If it is, it will never be flagged as stale
                    type: boolean
                  projectId:
                    description: |-
                      (String) Project Identifier
                      Project Identifier
                    type: string
                  projectIdRef:
                    description: Reference to a Project in platform to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in platform to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  tags:
                    description: |-
                      (Block List) The tags for the flag (see below for nested schema)
                      The tags for the flag
                    items:
                      properties:
                        identifier:
                          description: |-
                            (String) Identifier of the Feature Flag
                            The identifier of the tag
                          type: string
                      type: object
                    type: array
                  variation:
                    description: |-
                      (Block List, Min: 2) The options available for your flag (see below for nested schema)
                      The options available for your flag
                    items:
                      properties:
                        description:
                          description: |-
                            (String) Description of the Feature Flag
                            The description of the variation
                          type: string
                        identifier:
                          description: |-
                            (String) Identifier of the Feature Flag
                            The identifier of the variation
                          type: string
                        name:
                          description: |-
                            (String) Name of the Feature Flag
                            The user friendly name of the variation
                          type: string
                        value:
                          description: |-
                            (String) The value of the variation
                            The value of the variation
                          type: string
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  archived:
                    description: |-
                      (Boolean) Whether or not the flag is archived.
                      Whether or not the flag is archived.
                    type: boolean
                  defaultOffVariation:
                    description: |-
                      (String) Which of the variations to use when the flag is toggled to off state
                      Which of the variations to use when the flag is toggled to off state
                    type: string
                  defaultOnVariation:
                    description: |-
                      (String) Which of the variations to use when the flag is toggled to on state
                      Which of the variations to use when the flag is toggled to on state
                    type: string
                  description:
                    description: |-
                      (String) Description of the Feature Flag
                      Description of the Feature Flag
                    type: string
                  environment:
                    description: |-
                      (Block List) Environment Identifier (see below for nested schema)
                      Environment Identifier
                    items:
                      properties:
                        addTargetRule:
                          description: |-
                            (Block List) The targeting rules for the flag (see below for nested schema)
                            The targeting rules for the flag
                          items:
                            properties:
                              targets:
                                description: |-
                                  (List of String) The targets that should be served this variation
                                  The targets that should be served this variation
                                items:
                                  type: string
                                type: array
                              variation:
                                description: |-
                                  (Block List, Min: 2) The options available for your flag (see below for nested schema)
                                  The identifier of the variation
                                type: string
                            type: object
                          type: array
                        defaultOffVariation:
                          description: |-
                            (String) Which of the variations to use when the flag is toggled to off state
                            Default variation to be served when flag is 'off'
                          type: string
                        defaultOnVariation:
                          description: |-
                            (String) Which of the variations to use when the flag is toggled to on state
                            Default variation to be served when flag is 'on'
                          type: string
                        identifier:
                          description: |-
                            (String) Identifier of the Feature Flag
                            Identifier of the Environment
                          type: string
                        state:
                          description: |-
                            (String) State of the flag in this environment. Possible values are 'on' and 'off'
                            State of the flag in this environment. Possible values are 'on' and 'off'
                          type: string
                      type: object
                    type: array
                  identifier:
                    description: |-
                      (String) Identifier of the Feature Flag
                      Identifier of the Feature Flag
                    type: string
                  kind:
                    description: |-
                      (String) The type of data the flag represents. Valid values are boolean, int, string, json
                      The type of data the flag represents. Valid values are `boolean`, `int`, `string`, `json`
                    type: string
                  name:
                    description: |-
                      (String) Name of the Feature Flag
                      Name of the Feature Flag
                    type: string
                  orgId:
                    description: |-
                      (String) Organization Identifier
                      Organization Identifier
                    type: string
                  orgIdRef:
                    description: Reference to a Organization in platform to populate
                      orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  orgIdSelector:
                    description: Selector for a Organization in platform to populate
                      orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  owner:
                    description: |-
                      (String) The owner of the flag
                      The owner of the flag
                    type: string
                  permanent:
                    description: |-
                      (Boolean) Whether or not the flag is permanent. If it is, it will never be flagged as stale
                      Whether or not the flag is permanent. If it is, it will never be flagged as stale
                    type: boolean
                  projectId:
                    description: |-
                      (String) Project Identifier
                      Project Identifier
                    type: string
                  projectIdRef:
                    description: Reference to a Project in platform to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in platform to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  tags:
                    description: |-
                      (Block List) The tags for the flag (see below for nested schema)
                      The tags for the flag
                    items:
                      properties:
                        identifier:
                          description: |-
                            (String) Identifier of the Feature Flag
                            The identifier of the tag
                          type: string
                      type: object
                    type: array
                  variation:
                    description: |-
                      (Block List, Min: 2) The options available for your flag (see below for nested schema)
                      The options available for your flag
                    items:
                      properties:
                        description:
                          description: |-
                            (String) Description of the Feature Flag
                            The description of the variation
                          type: string
                        identifier:
                          description: |-
                            (String) Identifier of the Feature Flag
                            The identifier of the variation
                          type: string
                        name:
                          description: |-
                            (String) Name of the Feature Flag
                            The user friendly name of the variation
                          type: string
                        value:
                          description: |-
                            (String) The value of the variation
                            The value of the variation
                          type: string
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.defaultOffVariation is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.defaultOffVariation)
                || (has(self.initProvider) && has(self.initProvider.defaultOffVariation))'
            - message: spec.forProvider.defaultOnVariation is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.defaultOnVariation)
                || (has(self.initProvider) && has(self.initProvider.defaultOnVariation))'
            - message: spec.forProvider.identifier is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.identifier)
                || (has(self.initProvider) && has(self.initProvider.identifier))'
            - message: spec.forProvider.kind is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.kind)
                || (has(self.initProvider) && has(self.initProvider.kind))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.permanent is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.permanent)
                || (has(self.initProvider) && has(self.initProvider.permanent))'
            - message: spec.forProvider.variation is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.variation)
                || (has(self.initProvider) && has(self.initProvider.variation))'
          status:
            description: FeatureFlagStatus defines the observed state of FeatureFlag.
            properties:
              atProvider:
                properties:
                  archived:
                    description: |-
                      (Boolean) Whether or not the flag is archived.
                      Whether or not the flag is archived.
                    type: boolean
                  defaultOffVariation:
                    description: |-
                      (String) Which of the variations to use when the flag is toggled to off state
                      Which of the variations to use when the flag is toggled to off state
                    type: string
                  defaultOnVariation:
                    description: |-
                      (String) Which of the variations to use when the flag is toggled to on state
                      Which of the variations to use when the flag is toggled to on state
                    type: string
                  description:
                    description: |-
                      (String) Description of the Feature Flag
                      Description of the Feature Flag
                    type: string
                  environment:
                    description: |-
                      (Block List) Environment Identifier (see below for nested schema)
                      Environment Identifier
                    items:
                      properties:
                        addTargetRule:
                          description: |-
                            (Block List) The targeting rules for the flag (see below for nested schema)
                            The targeting rules for the flag
                          items:
                            properties:
                              targets:
                                description: |-
                                  (List of String) The targets that should be served this variation
                                  The targets that should be served this variation
                                items:
                                  type: string
                                type: array
                              variation:
                                description: |-
                                  (Block List, Min: 2) The options available for your flag (see below for nested schema)
                                  The identifier of the variation
                                type: string
                            type: object
                          type: array
                        defaultOffVariation:
                          description: |-
                            (String) Which of the variations to use when the flag is toggled to off state
                            Default variation to be served when flag is 'off'
                          type: string
                        defaultOnVariation:
                          description: |-
                            (String) Which of the variations to use when the flag is toggled to on state
                            Default variation to be served when flag is 'on'
                          type: string
                        identifier:
                          description: |-
                            (String) Identifier of the Feature Flag
                            Identifier of the Environment
                          type: string
                        state:
                          description: |-
                            (String) State of the flag in this environment. Possible values are 'on' and 'off'
                            State of the flag in this environment. Possible values are 'on' and 'off'
                          type: string
                      type: object
                    type: array
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  identifier:
                    description: |-
                      (String) Identifier of the Feature Flag
                      Identifier of the Feature Flag
                    type: string
                  kind:
                    description: |-
                      (String) The type of data the flag represents. Valid values are boolean, int, string, json
                      The type of data the flag represents. Valid values are `boolean`, `int`, `string`, `json`
                    type: string
                  name:
                    description: |-
                      (String) Name of the Feature Flag
                      Name of the Feature Flag
                    type: string
                  orgId:
                    description: |-
                      (String) Organization Identifier
                      Organization Identifier
                    type: string
                  owner:
                    description: |-
                      (String) The owner of the flag
                      The owner of the flag
                    type: string
                  permanent:
                    description: |-
                      (Boolean) Whether or not the flag is permanent. If it is, it will never be flagged as stale
                      Whether or not the flag is permanent. If it is, it will never be flagged as stale
                    type: boolean
                  projectId:
                    description: |-
                      (String) Project Identifier
                      Project Identifier
                    type: string
                  tags:
                    description: |-
                      (Block List) The tags for the flag (see below for nested schema)
                      The tags for the flag
                    items:
                      properties:
                        identifier:
                          description: |-
                            (String) Identifier of the Feature Flag
                            The identifier of the tag
                          type: string
                      type: object
                    type: array
                  variation:
                    description: |-
                      (Block List, Min: 2) The options available for your flag (see below for nested schema)
                      The options available for your flag
                    items:
                      properties:
                        description:
                          description: |-
                            (String) Description of the Feature Flag
                            The description of the variation
                          type: string
                        identifier:
                          description: |-
                            (String) Identifier of the Feature Flag
                            The identifier of the variation
                          type: string
                        name:
                          description: |-
                            (String) Name of the Feature Flag
                            The user friendly name of the variation
                          type: string
                        value:
                          description: |-
                            (String) The value of the variation
                            The value of the variation
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
