---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: usergroups.harness.harness.m.crossplane.io
spec:
  group: harness.harness.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - harness
    kind: UserGroup
    listKind: UserGroupList
    plural: usergroups
    singular: usergroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UserGroup is the Schema for the UserGroups API. Resource for
          creating a Harness user group
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UserGroupSpec defines the desired state of UserGroup
            properties:
              forProvider:
                properties:
                  description:
                    description: |-
                      (String) The description of the user group.
                      The description of the user group.
                    type: string
                  ldapSettings:
                    description: |-
                      (Block List, Max: 1) The LDAP settings for the user group. (see below for nested schema)
                      The LDAP settings for the user group.
                    items:
                      properties:
                        groupDn:
                          description: |-
                            (String) The group DN of the LDAP user group.
                            The group DN of the LDAP user group.
                          type: string
                        groupName:
                          description: |-
                            (String) The group name of the LDAP user group.
                            The group name of the LDAP user group.
                          type: string
                        ssoProviderId:
                          description: |-
                            (String) The ID of the SSO provider.
                            The ID of the SSO provider.
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the user group.
                      The name of the user group.
                    type: string
                  notificationSettings:
                    description: |-
                      (Block List, Max: 1) The notification settings of the user group. (see below for nested schema)
                      The notification settings of the user group.
                    items:
                      properties:
                        groupEmailAddresses:
                          description: |-
                            (List of String) The email addresses of the user group.
                            The email addresses of the user group.
                          items:
                            type: string
                          type: array
                        microsoftTeamsWebhookUrl:
                          description: |-
                            (String) The Microsoft Teams webhook URL of the user group.
                            The Microsoft Teams webhook URL of the user group.
                          type: string
                        sendMailToNewMembers:
                          description: |-
                            (Boolean) Indicates whether an email is sent when a new user is added to the group.
                            Indicates whether an email is sent when a new user is added to the group.
                          type: boolean
                        sendNotificationsToMembers:
                          description: |-
                            (Boolean) Enable this setting to have notifications sent to the members of this group.
                            Enable this setting to have notifications sent to the members of this group.
                          type: boolean
                        slackChannel:
                          description: |-
                            (String) The Slack channel to send notifications to.
                            The Slack channel to send notifications to.
                          type: string
                        slackWebhookUrl:
                          description: |-
                            (String) The Slack webhook URL to send notifications to.
                            The Slack webhook URL to send notifications to.
                          type: string
                      type: object
                    type: array
                  permissions:
                    description: |-
                      (Block List, Max: 1) The permissions of the user group. (see below for nested schema)
                      The permissions of the user group.
                    items:
                      properties:
                        accountPermissions:
                          description: |-
                            (Set of String) The account permissions of the user group. Valid options are ADMINISTER_OTHER_ACCOUNT_FUNCTIONS, CREATE_AND_DELETE_APPLICATION, CREATE_CUSTOM_DASHBOARDS, MANAGE_ALERT_NOTIFICATION_RULES, MANAGE_API_KEYS, MANAGE_APPLICATION_STACKS, MANAGE_AUTHENTICATION_SETTINGS, MANAGE_CLOUD_PROVIDERS, MANAGE_CONFIG_AS_CODE, MANAGE_CONNECTORS, MANAGE_CUSTOM_DASHBOARDS, MANAGE_DELEGATE_PROFILES, MANAGE_DELEGATES, MANAGE_DEPLOYMENT_FREEZES, MANAGE_IP_WHITELIST, MANAGE_PIPELINE_GOVERNANCE_STANDARDS, MANAGE_RESTRICTED_ACCESS, MANAGE_SECRET_MANAGERS, MANAGE_SECRETS, MANAGE_SSH_AND_WINRM, MANAGE_TAGS, MANAGE_TEMPLATE_LIBRARY, MANAGE_USER_AND_USER_GROUPS_AND_API_KEYS, MANAGE_USERS_AND_GROUPS, READ_USERS_AND_GROUPS, VIEW_AUDITS, VIEW_USER_AND_USER_GROUPS_AND_API_KEYS
                            The account permissions of the user group. Valid options are ADMINISTER_OTHER_ACCOUNT_FUNCTIONS, CREATE_AND_DELETE_APPLICATION, CREATE_CUSTOM_DASHBOARDS, MANAGE_ALERT_NOTIFICATION_RULES, MANAGE_API_KEYS, MANAGE_APPLICATION_STACKS, MANAGE_AUTHENTICATION_SETTINGS, MANAGE_CLOUD_PROVIDERS, MANAGE_CONFIG_AS_CODE, MANAGE_CONNECTORS, MANAGE_CUSTOM_DASHBOARDS, MANAGE_DELEGATE_PROFILES, MANAGE_DELEGATES, MANAGE_DEPLOYMENT_FREEZES, MANAGE_IP_WHITELIST, MANAGE_PIPELINE_GOVERNANCE_STANDARDS, MANAGE_RESTRICTED_ACCESS, MANAGE_SECRET_MANAGERS, MANAGE_SECRETS, MANAGE_SSH_AND_WINRM, MANAGE_TAGS, MANAGE_TEMPLATE_LIBRARY, MANAGE_USER_AND_USER_GROUPS_AND_API_KEYS, MANAGE_USERS_AND_GROUPS, READ_USERS_AND_GROUPS, VIEW_AUDITS, VIEW_USER_AND_USER_GROUPS_AND_API_KEYS
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        appPermissions:
                          description: |-
                            (Block List, Max: 1) Application specific permissions (see below for nested schema)
                            Application specific permissions
                          items:
                            properties:
                              all:
                                description: |-
                                  (Block Set) The permission to perform actions against all resources. (see below for nested schema)
                                  The permission to perform actions against all resources.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              deployment:
                                description: |-
                                  (Block Set) Permission configuration to perform actions against deployments. (see below for nested schema)
                                  Permission configuration to perform actions against deployments.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are READ, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW, ABORT_WORKFLOW
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    envIds:
                                      description: |-
                                        (Set of String) The environment IDs to which the permission applies. Leave empty to apply to all environments.
                                        The environment IDs to which the permission applies. Leave empty to apply to all environments.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    filters:
                                      description: |-
                                        (Set of String) The filters to apply to the action. Valid options are: NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                                        The filters to apply to the action. Valid options are: NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              environment:
                                description: |-
                                  (Block Set) Permission configuration to perform actions against workflows. (see below for nested schema)
                                  Permission configuration to perform actions against workflows.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    envIds:
                                      description: |-
                                        (Set of String) The environment IDs to which the permission applies. Leave empty to apply to all environments.
                                        The environment IDs to which the permission applies. Leave empty to apply to all environments.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    filters:
                                      description: |-
                                        (Set of String) The filters to apply to the action. Valid options are: NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                                        The filters to apply to the action. Valid options are: NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              pipeline:
                                description: |-
                                  (Block Set) Permission configuration to perform actions against pipelines. (see below for nested schema)
                                  Permission configuration to perform actions against pipelines.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are [CREATE READ UPDATE DELETE]
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    envIds:
                                      description: |-
                                        (Set of String) The environment IDs to which the permission applies. Leave empty to apply to all environments.
                                        The environment IDs to which the permission applies. Leave empty to apply to all environments.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    filters:
                                      description: |-
                                        (Set of String) The filters to apply to the action. Valid options are: NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                                        The filters to apply to the action. Valid options are: NON_PRODUCTION_PIPELINES, PRODUCTION_PIPELINES.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              provisioner:
                                description: |-
                                  (Block Set) Permission configuration to perform actions against provisioners. (see below for nested schema)
                                  Permission configuration to perform actions against provisioners.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    provisionerIds:
                                      description: |-
                                        (Set of String) The provisioner IDs to which the permission applies. Leave empty to apply to all provisioners.
                                        The provisioner IDs to which the permission applies. Leave empty to apply to all provisioners.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              service:
                                description: |-
                                  (Block Set) Permission configuration to perform actions against services. (see below for nested schema)
                                  Permission configuration to perform actions against services.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    serviceIds:
                                      description: |-
                                        (Set of String) The service IDs to which the permission applies. Leave empty to apply to all services.
                                        The service IDs to which the permission applies. Leave empty to apply to all services.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              template:
                                description: |-
                                  (Block Set) Permission configuration to perform actions against templates. (see below for nested schema)
                                  Permission configuration to perform actions against templates.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    templateIds:
                                      description: |-
                                        (Set of String) The template IDs to which the permission applies. Leave empty to apply to all environments.
                                        The template IDs to which the permission applies. Leave empty to apply to all environments.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              workflow:
                                description: |-
                                  (Block Set) Permission configuration to perform actions against workflows. (see below for nested schema)
                                  Permission configuration to perform actions against workflows.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    filters:
                                      description: |-
                                        (Set of String) The filters to apply to the action. Valid options are: NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                                        The filters to apply to the action. Valid options are: NON_PRODUCTION_WORKFLOWS, PRODUCTION_WORKFLOWS, WORKFLOW_TEMPLATES.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  samlSettings:
                    description: |-
                      (Block List, Max: 1) The SAML settings for the user group. (see below for nested schema)
                      The SAML settings for the user group.
                    items:
                      properties:
                        groupName:
                          description: |-
                            (String) The group name of the LDAP user group.
                            The group name of the SAML user group.
                          type: string
                        ssoProviderId:
                          description: |-
                            (String) The ID of the SSO provider.
                            The ID of the SSO provider.
                          type: string
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  description:
                    description: |-
                      (String) The description of the user group.
                      The description of the user group.
                    type: string
                  ldapSettings:
                    description: |-
                      (Block List, Max: 1) The LDAP settings for the user group. (see below for nested schema)
                      The LDAP settings for the user group.
                    items:
                      properties:
                        groupDn:
                          description: |-
                            (String) The group DN of the LDAP user group.
                            The group DN of the LDAP user group.
                          type: string
                        groupName:
                          description: |-
                            (String) The group name of the LDAP user group.
                            The group name of the LDAP user group.
                          type: string
                        ssoProviderId:
                          description: |-
                            (String) The ID of the SSO provider.
                            The ID of the SSO provider.
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the user group.
                      The name of the user group.
                    type: string
                  notificationSettings:
                    description: |-
                      (Block List, Max: 1) The notification settings of the user group. (see below for nested schema)
                      The notification settings of the user group.
                    items:
                      properties:
                        groupEmailAddresses:
                          description: |-
                            (List of String) The email addresses of the user group.
                            The email addresses of the user group.
                          items:
                            type: string
                          type: array
                        microsoftTeamsWebhookUrl:
                          description: |-
                            (String) The Microsoft Teams webhook URL of the user group.
                            The Microsoft Teams webhook URL of the user group.
                          type: string
                        sendMailToNewMembers:
                          description: |-
                            (Boolean) Indicates whether an email is sent when a new user is added to the group.
                            Indicates whether an email is sent when a new user is added to the group.
                          type: boolean
                        sendNotificationsToMembers:
                          description: |-
                            (Boolean) Enable this setting to have notifications sent to the members of this group.
                            Enable this setting to have notifications sent to the members of this group.
                          type: boolean
                        slackChannel:
                          description: |-
                            (String) The Slack channel to send notifications to.
                            The Slack channel to send notifications to.
                          type: string
                        slackWebhookUrl:
                          description: |-
                            (String) The Slack webhook URL to send notifications to.
                            The Slack webhook URL to send notifications to.
                          type: string
                      type: object
                    type: array
                  permissions:
                    description: |-
                      (Block List, Max: 1) The permissions of the user group. (see below for nested schema)
                      The permissions of the user group.
                    items:
                      properties:
                        accountPermissions:
                          description: |-
                            (Set of String) The account permissions of the user group. Valid options are ADMINISTER_OTHER_ACCOUNT_FUNCTIONS, CREATE_AND_DELETE_APPLICATION, CREATE_CUSTOM_DASHBOARDS, MANAGE_ALERT_NOTIFICATION_RULES, MANAGE_API_KEYS, MANAGE_APPLICATION_STACKS, MANAGE_AUTHENTICATION_SETTINGS, MANAGE_CLOUD_PROVIDERS, MANAGE_CONFIG_AS_CODE, MANAGE_CONNECTORS, MANAGE_CUSTOM_DASHBOARDS, MANAGE_DELEGATE_PROFILES, MANAGE_DELEGATES, MANAGE_DEPLOYMENT_FREEZES, MANAGE_IP_WHITELIST, MANAGE_PIPELINE_GOVERNANCE_STANDARDS, MANAGE_RESTRICTED_ACCESS, MANAGE_SECRET_MANAGERS, MANAGE_SECRETS, MANAGE_SSH_AND_WINRM, MANAGE_TAGS, MANAGE_TEMPLATE_LIBRARY, MANAGE_USER_AND_USER_GROUPS_AND_API_KEYS, MANAGE_USERS_AND_GROUPS, READ_USERS_AND_GROUPS, VIEW_AUDITS, VIEW_USER_AND_USER_GROUPS_AND_API_KEYS
                            The account permissions of the user group. Valid options are ADMINISTER_OTHER_ACCOUNT_FUNCTIONS, CREATE_AND_DELETE_APPLICATION, CREATE_CUSTOM_DASHBOARDS, MANAGE_ALERT_NOTIFICATION_RULES, MANAGE_API_KEYS, MANAGE_APPLICATION_STACKS, MANAGE_AUTHENTICATION_SETTINGS, MANAGE_CLOUD_PROVIDERS, MANAGE_CONFIG_AS_CODE, MANAGE_CONNECTORS, MANAGE_CUSTOM_DASHBOARDS, MANAGE_DELEGATE_PROFILES, MANAGE_DELEGATES, MANAGE_DEPLOYMENT_FREEZES, MANAGE_IP_WHITELIST, MANAGE_PIPELINE_GOVERNANCE_STANDARDS, MANAGE_RESTRICTED_ACCESS, MANAGE_SECRET_MANAGERS, MANAGE_SECRETS, MANAGE_SSH_AND_WINRM, MANAGE_TAGS, MANAGE_TEMPLATE_LIBRARY, MANAGE_USER_AND_USER_GROUPS_AND_API_KEYS, MANAGE_USERS_AND_GROUPS, READ_USERS_AND_GROUPS, VIEW_AUDITS, VIEW_USER_AND_USER_GROUPS_AND_API_KEYS
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        appPermissions:
                          description: |-
                            (Block List, Max: 1) Application specific permissions (see below for nested schema)
                            Application specific permissions
                          items:
                            properties:
                              all:
                                description: |-
                                  (Block Set) The permission to perform actions against all resources. (see below for nested schema)
                                  The permission to perform actions against all resources.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              deployment:
                                description: |-
                                  (Block Set) Permission configuration to perform actions against deployments. (see below for nested schema)
                                  Permission configuration to perform actions against deployments.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are READ, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW, ABORT_WORKFLOW
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    envIds:
                                      description: |-
                                        (Set of String) The environment IDs to which the permission applies. Leave empty to apply to all environments.
                                        The environment IDs to which the permission applies. Leave empty to apply to all environments.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    filters:
                                      description: |-
                                        (Set of String) The filters to apply to the action. Valid options are: NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                                        The filters to apply to the action. Valid options are: NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              environment:
                                description: |-
                                  (Block Set) Permission configuration to perform actions against workflows. (see below for nested schema)
                                  Permission configuration to perform actions against workflows.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    envIds:
                                      description: |-
                                        (Set of String) The environment IDs to which the permission applies. Leave empty to apply to all environments.
                                        The environment IDs to which the permission applies. Leave empty to apply to all environments.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    filters:
                                      description: |-
                                        (Set of String) The filters to apply to the action. Valid options are: NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                                        The filters to apply to the action. Valid options are: NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              pipeline:
                                description: |-
                                  (Block Set) Permission configuration to perform actions against pipelines. (see below for nested schema)
                                  Permission configuration to perform actions against pipelines.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are [CREATE READ UPDATE DELETE]
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    envIds:
                                      description: |-
                                        (Set of String) The environment IDs to which the permission applies. Leave empty to apply to all environments.
                                        The environment IDs to which the permission applies. Leave empty to apply to all environments.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    filters:
                                      description: |-
                                        (Set of String) The filters to apply to the action. Valid options are: NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                                        The filters to apply to the action. Valid options are: NON_PRODUCTION_PIPELINES, PRODUCTION_PIPELINES.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              provisioner:
                                description: |-
                                  (Block Set) Permission configuration to perform actions against provisioners. (see below for nested schema)
                                  Permission configuration to perform actions against provisioners.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    provisionerIds:
                                      description: |-
                                        (Set of String) The provisioner IDs to which the permission applies. Leave empty to apply to all provisioners.
                                        The provisioner IDs to which the permission applies. Leave empty to apply to all provisioners.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              service:
                                description: |-
                                  (Block Set) Permission configuration to perform actions against services. (see below for nested schema)
                                  Permission configuration to perform actions against services.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    serviceIds:
                                      description: |-
                                        (Set of String) The service IDs to which the permission applies. Leave empty to apply to all services.
                                        The service IDs to which the permission applies. Leave empty to apply to all services.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              template:
                                description: |-
                                  (Block Set) Permission configuration to perform actions against templates. (see below for nested schema)
                                  Permission configuration to perform actions against templates.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    templateIds:
                                      description: |-
                                        (Set of String) The template IDs to which the permission applies. Leave empty to apply to all environments.
                                        The template IDs to which the permission applies. Leave empty to apply to all environments.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              workflow:
                                description: |-
                                  (Block Set) Permission configuration to perform actions against workflows. (see below for nested schema)
                                  Permission configuration to perform actions against workflows.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    filters:
                                      description: |-
                                        (Set of String) The filters to apply to the action. Valid options are: NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                                        The filters to apply to the action. Valid options are: NON_PRODUCTION_WORKFLOWS, PRODUCTION_WORKFLOWS, WORKFLOW_TEMPLATES.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  samlSettings:
                    description: |-
                      (Block List, Max: 1) The SAML settings for the user group. (see below for nested schema)
                      The SAML settings for the user group.
                    items:
                      properties:
                        groupName:
                          description: |-
                            (String) The group name of the LDAP user group.
                            The group name of the SAML user group.
                          type: string
                        ssoProviderId:
                          description: |-
                            (String) The ID of the SSO provider.
                            The ID of the SSO provider.
                          type: string
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: UserGroupStatus defines the observed state of UserGroup.
            properties:
              atProvider:
                properties:
                  description:
                    description: |-
                      (String) The description of the user group.
                      The description of the user group.
                    type: string
                  id:
                    description: (String) Unique identifier of the user group.
                    type: string
                  importedByScim:
                    description: |-
                      (Boolean) Indicates whether the user group was imported by SCIM.
                      Indicates whether the user group was imported by SCIM.
                    type: boolean
                  isSsoLinked:
                    description: |-
                      (Boolean) Indicates whether the user group is linked to an SSO provider.
                      Indicates whether the user group is linked to an SSO provider.
                    type: boolean
                  ldapSettings:
                    description: |-
                      (Block List, Max: 1) The LDAP settings for the user group. (see below for nested schema)
                      The LDAP settings for the user group.
                    items:
                      properties:
                        groupDn:
                          description: |-
                            (String) The group DN of the LDAP user group.
                            The group DN of the LDAP user group.
                          type: string
                        groupName:
                          description: |-
                            (String) The group name of the LDAP user group.
                            The group name of the LDAP user group.
                          type: string
                        ssoProviderId:
                          description: |-
                            (String) The ID of the SSO provider.
                            The ID of the SSO provider.
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) The name of the user group.
                      The name of the user group.
                    type: string
                  notificationSettings:
                    description: |-
                      (Block List, Max: 1) The notification settings of the user group. (see below for nested schema)
                      The notification settings of the user group.
                    items:
                      properties:
                        groupEmailAddresses:
                          description: |-
                            (List of String) The email addresses of the user group.
                            The email addresses of the user group.
                          items:
                            type: string
                          type: array
                        microsoftTeamsWebhookUrl:
                          description: |-
                            (String) The Microsoft Teams webhook URL of the user group.
                            The Microsoft Teams webhook URL of the user group.
                          type: string
                        sendMailToNewMembers:
                          description: |-
                            (Boolean) Indicates whether an email is sent when a new user is added to the group.
                            Indicates whether an email is sent when a new user is added to the group.
                          type: boolean
                        sendNotificationsToMembers:
                          description: |-
                            (Boolean) Enable this setting to have notifications sent to the members of this group.
                            Enable this setting to have notifications sent to the members of this group.
                          type: boolean
                        slackChannel:
                          description: |-
                            (String) The Slack channel to send notifications to.
                            The Slack channel to send notifications to.
                          type: string
                        slackWebhookUrl:
                          description: |-
                            (String) The Slack webhook URL to send notifications to.
                            The Slack webhook URL to send notifications to.
                          type: string
                      type: object
                    type: array
                  permissions:
                    description: |-
                      (Block List, Max: 1) The permissions of the user group. (see below for nested schema)
                      The permissions of the user group.
                    items:
                      properties:
                        accountPermissions:
                          description: |-
                            (Set of String) The account permissions of the user group. Valid options are ADMINISTER_OTHER_ACCOUNT_FUNCTIONS, CREATE_AND_DELETE_APPLICATION, CREATE_CUSTOM_DASHBOARDS, MANAGE_ALERT_NOTIFICATION_RULES, MANAGE_API_KEYS, MANAGE_APPLICATION_STACKS, MANAGE_AUTHENTICATION_SETTINGS, MANAGE_CLOUD_PROVIDERS, MANAGE_CONFIG_AS_CODE, MANAGE_CONNECTORS, MANAGE_CUSTOM_DASHBOARDS, MANAGE_DELEGATE_PROFILES, MANAGE_DELEGATES, MANAGE_DEPLOYMENT_FREEZES, MANAGE_IP_WHITELIST, MANAGE_PIPELINE_GOVERNANCE_STANDARDS, MANAGE_RESTRICTED_ACCESS, MANAGE_SECRET_MANAGERS, MANAGE_SECRETS, MANAGE_SSH_AND_WINRM, MANAGE_TAGS, MANAGE_TEMPLATE_LIBRARY, MANAGE_USER_AND_USER_GROUPS_AND_API_KEYS, MANAGE_USERS_AND_GROUPS, READ_USERS_AND_GROUPS, VIEW_AUDITS, VIEW_USER_AND_USER_GROUPS_AND_API_KEYS
                            The account permissions of the user group. Valid options are ADMINISTER_OTHER_ACCOUNT_FUNCTIONS, CREATE_AND_DELETE_APPLICATION, CREATE_CUSTOM_DASHBOARDS, MANAGE_ALERT_NOTIFICATION_RULES, MANAGE_API_KEYS, MANAGE_APPLICATION_STACKS, MANAGE_AUTHENTICATION_SETTINGS, MANAGE_CLOUD_PROVIDERS, MANAGE_CONFIG_AS_CODE, MANAGE_CONNECTORS, MANAGE_CUSTOM_DASHBOARDS, MANAGE_DELEGATE_PROFILES, MANAGE_DELEGATES, MANAGE_DEPLOYMENT_FREEZES, MANAGE_IP_WHITELIST, MANAGE_PIPELINE_GOVERNANCE_STANDARDS, MANAGE_RESTRICTED_ACCESS, MANAGE_SECRET_MANAGERS, MANAGE_SECRETS, MANAGE_SSH_AND_WINRM, MANAGE_TAGS, MANAGE_TEMPLATE_LIBRARY, MANAGE_USER_AND_USER_GROUPS_AND_API_KEYS, MANAGE_USERS_AND_GROUPS, READ_USERS_AND_GROUPS, VIEW_AUDITS, VIEW_USER_AND_USER_GROUPS_AND_API_KEYS
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        appPermissions:
                          description: |-
                            (Block List, Max: 1) Application specific permissions (see below for nested schema)
                            Application specific permissions
                          items:
                            properties:
                              all:
                                description: |-
                                  (Block Set) The permission to perform actions against all resources. (see below for nested schema)
                                  The permission to perform actions against all resources.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              deployment:
                                description: |-
                                  (Block Set) Permission configuration to perform actions against deployments. (see below for nested schema)
                                  Permission configuration to perform actions against deployments.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are READ, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW, ABORT_WORKFLOW
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    envIds:
                                      description: |-
                                        (Set of String) The environment IDs to which the permission applies. Leave empty to apply to all environments.
                                        The environment IDs to which the permission applies. Leave empty to apply to all environments.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    filters:
                                      description: |-
                                        (Set of String) The filters to apply to the action. Valid options are: NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                                        The filters to apply to the action. Valid options are: NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              environment:
                                description: |-
                                  (Block Set) Permission configuration to perform actions against workflows. (see below for nested schema)
                                  Permission configuration to perform actions against workflows.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    envIds:
                                      description: |-
                                        (Set of String) The environment IDs to which the permission applies. Leave empty to apply to all environments.
                                        The environment IDs to which the permission applies. Leave empty to apply to all environments.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    filters:
                                      description: |-
                                        (Set of String) The filters to apply to the action. Valid options are: NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                                        The filters to apply to the action. Valid options are: NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              pipeline:
                                description: |-
                                  (Block Set) Permission configuration to perform actions against pipelines. (see below for nested schema)
                                  Permission configuration to perform actions against pipelines.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are [CREATE READ UPDATE DELETE]
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    envIds:
                                      description: |-
                                        (Set of String) The environment IDs to which the permission applies. Leave empty to apply to all environments.
                                        The environment IDs to which the permission applies. Leave empty to apply to all environments.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    filters:
                                      description: |-
                                        (Set of String) The filters to apply to the action. Valid options are: NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                                        The filters to apply to the action. Valid options are: NON_PRODUCTION_PIPELINES, PRODUCTION_PIPELINES.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              provisioner:
                                description: |-
                                  (Block Set) Permission configuration to perform actions against provisioners. (see below for nested schema)
                                  Permission configuration to perform actions against provisioners.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    provisionerIds:
                                      description: |-
                                        (Set of String) The provisioner IDs to which the permission applies. Leave empty to apply to all provisioners.
                                        The provisioner IDs to which the permission applies. Leave empty to apply to all provisioners.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              service:
                                description: |-
                                  (Block Set) Permission configuration to perform actions against services. (see below for nested schema)
                                  Permission configuration to perform actions against services.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    serviceIds:
                                      description: |-
                                        (Set of String) The service IDs to which the permission applies. Leave empty to apply to all services.
                                        The service IDs to which the permission applies. Leave empty to apply to all services.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              template:
                                description: |-
                                  (Block Set) Permission configuration to perform actions against templates. (see below for nested schema)
                                  Permission configuration to perform actions against templates.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    templateIds:
                                      description: |-
                                        (Set of String) The template IDs to which the permission applies. Leave empty to apply to all environments.
                                        The template IDs to which the permission applies. Leave empty to apply to all environments.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                              workflow:
                                description: |-
                                  (Block Set) Permission configuration to perform actions against workflows. (see below for nested schema)
                                  Permission configuration to perform actions against workflows.
                                items:
                                  properties:
                                    actions:
                                      description: |-
                                        (Set of String) The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE, EXECUTE_WORKFLOW, EXECUTE_PIPELINE, ROLLBACK_WORKFLOW
                                        The actions allowed to be performed. Valid options are CREATE, READ, UPDATE, DELETE
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    appIds:
                                      description: |-
                                        (Set of String) The application IDs to which the permission applies. Leave empty to apply to all applications.
                                        The application IDs to which the permission applies. Leave empty to apply to all applications.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                    filters:
                                      description: |-
                                        (Set of String) The filters to apply to the action. Valid options are: NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                                        The filters to apply to the action. Valid options are: NON_PRODUCTION_WORKFLOWS, PRODUCTION_WORKFLOWS, WORKFLOW_TEMPLATES.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: set
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  samlSettings:
                    description: |-
                      (Block List, Max: 1) The SAML settings for the user group. (see below for nested schema)
                      The SAML settings for the user group.
                    items:
                      properties:
                        groupName:
                          description: |-
                            (String) The group name of the LDAP user group.
                            The group name of the SAML user group.
                          type: string
                        ssoProviderId:
                          description: |-
                            (String) The ID of the SSO provider.
                            The ID of the SSO provider.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
