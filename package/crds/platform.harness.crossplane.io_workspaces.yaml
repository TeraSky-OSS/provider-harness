---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: workspaces.platform.harness.crossplane.io
spec:
  group: platform.harness.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - harness
    kind: Workspace
    listKind: WorkspaceList
    plural: workspaces
    singular: workspace
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Workspace is the Schema for the Workspaces API. Resource for
          managing Workspaces
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WorkspaceSpec defines the desired state of Workspace
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  connector:
                    description: |-
                      (Block Set) Provider connectors configured on the Workspace. Only one connector of a type is supported (see below for nested schema)
                      Provider connectors configured on the Workspace. Only one connector of a type is supported
                    items:
                      properties:
                        connectorRef:
                          description: |-
                            (String) Connector Ref is the reference to the connector
                            Connector Ref is the reference to the connector
                          type: string
                        type:
                          description: |-
                            (String) Type is the connector type of the connector. Supported types: aws, azure, gcp
                            Type is the connector type of the connector. Supported types: aws, azure, gcp
                          type: string
                      type: object
                    type: array
                  costEstimationEnabled:
                    description: |-
                      (Boolean) Cost estimation enabled determines if cost estimation operations are performed.
                      Cost estimation enabled determines if cost estimation operations are performed.
                    type: boolean
                  defaultPipelines:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Default pipelines associated with this workspace
                      Default pipelines associated with this workspace
                    type: object
                    x-kubernetes-map-type: granular
                  description:
                    description: |-
                      (String) Description of the resource.
                      Description of the resource.
                    type: string
                  environmentVariable:
                    description: |-
                      (Block Set) Environment variables configured on the workspace (see below for nested schema)
                      Environment variables configured on the workspace
                    items:
                      properties:
                        key:
                          description: |-
                            (String) Key is the identifier for the variable. Must be unique within the workspace.
                            Key is the identifier for the variable. Must be unique within the workspace.
                          type: string
                        value:
                          description: |-
                            (String) Value is the value of the variable. For string value types this field should contain the value of the variable. For secret value types this should contain a reference to a valid harness secret.
                            Value is the value of the variable. For string value types this field should contain the value of the variable. For secret value types this should contain a reference to a valid harness secret.
                          type: string
                        valueType:
                          description: |-
                            (String) Value type indicates the value type of the variable. Currently we support string and secret.
                            Value type indicates the value type of the variable. Currently we support string and secret.
                          type: string
                      type: object
                    type: array
                  identifier:
                    description: |-
                      (String) Unique identifier of the resource.
                      Unique identifier of the resource.
                    type: string
                  name:
                    description: |-
                      (String) Name of the resource.
                      Name of the resource.
                    type: string
                  orgId:
                    description: |-
                      (String) Unique identifier of the organization.
                      Unique identifier of the organization.
                    type: string
                  orgIdRef:
                    description: Reference to a Organization in platform to populate
                      orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  orgIdSelector:
                    description: Selector for a Organization in platform to populate
                      orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  projectId:
                    description: |-
                      (String) Unique identifier of the project.
                      Unique identifier of the project.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in platform to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in platform to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  providerConnector:
                    description: |-
                      (String) Provider connector is the reference to the connector for the infrastructure provider
                      Provider connector is the reference to the connector for the infrastructure provider
                    type: string
                  provisionerType:
                    description: |-
                      (string) provisioner type defines the provisioning tool to use (provider or opentofu)
                      provisioner type defines the provisioning tool to use (provider or opentofu)
                    type: string
                  provisionerVersion:
                    description: |-
                      (String) Provisioner version defines the provisioner version to use.5.7.
                      Provisioner version defines the provisioner version to use.5.7.
                    type: string
                  repository:
                    description: |-
                      (String) Repository is the name of the repository to fetch the code from.
                      Repository is the name of the repository to fetch the code from.
                    type: string
                  repositoryBranch:
                    description: |-
                      (String) Repository branch is the name of the branch to fetch the code from. This cannot be set if repository commit or sha is set.
                      Repository branch is the name of the branch to fetch the code from. This cannot be set if repository commit or sha is set.
                    type: string
                  repositoryCommit:
                    description: |-
                      (String) Repository commit is tag to fetch the code from. This cannot be set if repository branch or sha is set.
                      Repository commit is tag to fetch the code from. This cannot be set if repository branch or sha is set.
                    type: string
                  repositoryConnector:
                    description: |-
                      (String) Repository connector is the reference to the connector used to fetch the code.
                      Repository connector is the reference to the connector used to fetch the code.
                    type: string
                  repositoryPath:
                    description: |-
                      (String) Repository path is the path in which the code resides.
                      Repository path is the path in which the code resides.
                    type: string
                  repositorySha:
                    description: |-
                      (String) Repository commit is commit SHA to fetch the code from. This cannot be set if repository branch or commit is set.
                      Repository commit is commit SHA to fetch the code from. This cannot be set if repository branch or commit is set.
                    type: string
                  runAll:
                    description: |-
                      all terragrunt modules
                      Boolean flag for run-all terragrunt modules
                    type: boolean
                  tags:
                    description: |-
                      (Set of String) Tags to associate with the resource.
                      Tags to associate with the resource.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  terraformVariable:
                    items:
                      properties:
                        key:
                          description: |-
                            (String) Key is the identifier for the variable. Must be unique within the workspace.
                            Key is the identifier for the variable. Must be unique within the workspace.
                          type: string
                        value:
                          description: |-
                            (String) Value is the value of the variable. For string value types this field should contain the value of the variable. For secret value types this should contain a reference to a valid harness secret.
                            Value is the value of the variable. For string value types this field should contain the value of the variable. For secret value types this should contain a reference to a valid harness secret.
                          type: string
                        valueType:
                          description: |-
                            (String) Value type indicates the value type of the variable. Currently we support string and secret.
                            Value type indicates the value type of the variable. Currently we support string and secret.
                          type: string
                      type: object
                    type: array
                  terraformVariableFile:
                    description: |-
                      (block set) provider variables files configured on the workspace (see below for nested schema)
                      provider variables files configured on the workspace
                    items:
                      properties:
                        repository:
                          description: |-
                            (String) Repository is the name of the repository to fetch the code from.
                            Repository is the name of the repository to fetch the code from.
                          type: string
                        repositoryBranch:
                          description: |-
                            (String) Repository branch is the name of the branch to fetch the code from. This cannot be set if repository commit or sha is set.
                            Repository branch is the name of the branch to fetch the variables from. This cannot be set if repository commit or sha is set
                          type: string
                        repositoryCommit:
                          description: |-
                            (String) Repository commit is tag to fetch the code from. This cannot be set if repository branch or sha is set.
                            Repository commit is tag to fetch the variables from. This cannot be set if repository branch or sha is set.
                          type: string
                        repositoryConnector:
                          description: |-
                            (String) Repository connector is the reference to the connector used to fetch the code.
                            Repository connector is the reference to the connector used to fetch the variables.
                          type: string
                        repositoryPath:
                          description: |-
                            (String) Repository path is the path in which the code resides.
                            Repository path is the path in which the variables reside.
                          type: string
                        repositorySha:
                          description: |-
                            (String) Repository commit is commit SHA to fetch the code from. This cannot be set if repository branch or commit is set.
                            Repository commit is SHA to fetch the variables from. This cannot be set if repository branch or commit is set.
                          type: string
                      type: object
                    type: array
                  terragruntProvider:
                    description: |-
                      (Boolean) Set to true to enable Terragrunt mode
                      Set to true to enable Terragrunt mode
                    type: boolean
                  terragruntVersion:
                    description: |-
                      (String) Terragrunt version to use (e.g., 0.45.0)
                      Terragrunt version to use (e.g., 0.45.0)
                    type: string
                  variableSets:
                    description: |-
                      (List of String) Variable sets to use.
                      Variable sets to use.
                    items:
                      type: string
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  connector:
                    description: |-
                      (Block Set) Provider connectors configured on the Workspace. Only one connector of a type is supported (see below for nested schema)
                      Provider connectors configured on the Workspace. Only one connector of a type is supported
                    items:
                      properties:
                        connectorRef:
                          description: |-
                            (String) Connector Ref is the reference to the connector
                            Connector Ref is the reference to the connector
                          type: string
                        type:
                          description: |-
                            (String) Type is the connector type of the connector. Supported types: aws, azure, gcp
                            Type is the connector type of the connector. Supported types: aws, azure, gcp
                          type: string
                      type: object
                    type: array
                  costEstimationEnabled:
                    description: |-
                      (Boolean) Cost estimation enabled determines if cost estimation operations are performed.
                      Cost estimation enabled determines if cost estimation operations are performed.
                    type: boolean
                  defaultPipelines:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Default pipelines associated with this workspace
                      Default pipelines associated with this workspace
                    type: object
                    x-kubernetes-map-type: granular
                  description:
                    description: |-
                      (String) Description of the resource.
                      Description of the resource.
                    type: string
                  environmentVariable:
                    description: |-
                      (Block Set) Environment variables configured on the workspace (see below for nested schema)
                      Environment variables configured on the workspace
                    items:
                      properties:
                        key:
                          description: |-
                            (String) Key is the identifier for the variable. Must be unique within the workspace.
                            Key is the identifier for the variable. Must be unique within the workspace.
                          type: string
                        value:
                          description: |-
                            (String) Value is the value of the variable. For string value types this field should contain the value of the variable. For secret value types this should contain a reference to a valid harness secret.
                            Value is the value of the variable. For string value types this field should contain the value of the variable. For secret value types this should contain a reference to a valid harness secret.
                          type: string
                        valueType:
                          description: |-
                            (String) Value type indicates the value type of the variable. Currently we support string and secret.
                            Value type indicates the value type of the variable. Currently we support string and secret.
                          type: string
                      type: object
                    type: array
                  identifier:
                    description: |-
                      (String) Unique identifier of the resource.
                      Unique identifier of the resource.
                    type: string
                  name:
                    description: |-
                      (String) Name of the resource.
                      Name of the resource.
                    type: string
                  orgId:
                    description: |-
                      (String) Unique identifier of the organization.
                      Unique identifier of the organization.
                    type: string
                  orgIdRef:
                    description: Reference to a Organization in platform to populate
                      orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  orgIdSelector:
                    description: Selector for a Organization in platform to populate
                      orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  projectId:
                    description: |-
                      (String) Unique identifier of the project.
                      Unique identifier of the project.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in platform to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in platform to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  providerConnector:
                    description: |-
                      (String) Provider connector is the reference to the connector for the infrastructure provider
                      Provider connector is the reference to the connector for the infrastructure provider
                    type: string
                  provisionerType:
                    description: |-
                      (string) provisioner type defines the provisioning tool to use (provider or opentofu)
                      provisioner type defines the provisioning tool to use (provider or opentofu)
                    type: string
                  provisionerVersion:
                    description: |-
                      (String) Provisioner version defines the provisioner version to use.5.7.
                      Provisioner version defines the provisioner version to use.5.7.
                    type: string
                  repository:
                    description: |-
                      (String) Repository is the name of the repository to fetch the code from.
                      Repository is the name of the repository to fetch the code from.
                    type: string
                  repositoryBranch:
                    description: |-
                      (String) Repository branch is the name of the branch to fetch the code from. This cannot be set if repository commit or sha is set.
                      Repository branch is the name of the branch to fetch the code from. This cannot be set if repository commit or sha is set.
                    type: string
                  repositoryCommit:
                    description: |-
                      (String) Repository commit is tag to fetch the code from. This cannot be set if repository branch or sha is set.
                      Repository commit is tag to fetch the code from. This cannot be set if repository branch or sha is set.
                    type: string
                  repositoryConnector:
                    description: |-
                      (String) Repository connector is the reference to the connector used to fetch the code.
                      Repository connector is the reference to the connector used to fetch the code.
                    type: string
                  repositoryPath:
                    description: |-
                      (String) Repository path is the path in which the code resides.
                      Repository path is the path in which the code resides.
                    type: string
                  repositorySha:
                    description: |-
                      (String) Repository commit is commit SHA to fetch the code from. This cannot be set if repository branch or commit is set.
                      Repository commit is commit SHA to fetch the code from. This cannot be set if repository branch or commit is set.
                    type: string
                  runAll:
                    description: |-
                      all terragrunt modules
                      Boolean flag for run-all terragrunt modules
                    type: boolean
                  tags:
                    description: |-
                      (Set of String) Tags to associate with the resource.
                      Tags to associate with the resource.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  terraformVariable:
                    items:
                      properties:
                        key:
                          description: |-
                            (String) Key is the identifier for the variable. Must be unique within the workspace.
                            Key is the identifier for the variable. Must be unique within the workspace.
                          type: string
                        value:
                          description: |-
                            (String) Value is the value of the variable. For string value types this field should contain the value of the variable. For secret value types this should contain a reference to a valid harness secret.
                            Value is the value of the variable. For string value types this field should contain the value of the variable. For secret value types this should contain a reference to a valid harness secret.
                          type: string
                        valueType:
                          description: |-
                            (String) Value type indicates the value type of the variable. Currently we support string and secret.
                            Value type indicates the value type of the variable. Currently we support string and secret.
                          type: string
                      type: object
                    type: array
                  terraformVariableFile:
                    description: |-
                      (block set) provider variables files configured on the workspace (see below for nested schema)
                      provider variables files configured on the workspace
                    items:
                      properties:
                        repository:
                          description: |-
                            (String) Repository is the name of the repository to fetch the code from.
                            Repository is the name of the repository to fetch the code from.
                          type: string
                        repositoryBranch:
                          description: |-
                            (String) Repository branch is the name of the branch to fetch the code from. This cannot be set if repository commit or sha is set.
                            Repository branch is the name of the branch to fetch the variables from. This cannot be set if repository commit or sha is set
                          type: string
                        repositoryCommit:
                          description: |-
                            (String) Repository commit is tag to fetch the code from. This cannot be set if repository branch or sha is set.
                            Repository commit is tag to fetch the variables from. This cannot be set if repository branch or sha is set.
                          type: string
                        repositoryConnector:
                          description: |-
                            (String) Repository connector is the reference to the connector used to fetch the code.
                            Repository connector is the reference to the connector used to fetch the variables.
                          type: string
                        repositoryPath:
                          description: |-
                            (String) Repository path is the path in which the code resides.
                            Repository path is the path in which the variables reside.
                          type: string
                        repositorySha:
                          description: |-
                            (String) Repository commit is commit SHA to fetch the code from. This cannot be set if repository branch or commit is set.
                            Repository commit is SHA to fetch the variables from. This cannot be set if repository branch or commit is set.
                          type: string
                      type: object
                    type: array
                  terragruntProvider:
                    description: |-
                      (Boolean) Set to true to enable Terragrunt mode
                      Set to true to enable Terragrunt mode
                    type: boolean
                  terragruntVersion:
                    description: |-
                      (String) Terragrunt version to use (e.g., 0.45.0)
                      Terragrunt version to use (e.g., 0.45.0)
                    type: string
                  variableSets:
                    description: |-
                      (List of String) Variable sets to use.
                      Variable sets to use.
                    items:
                      type: string
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.costEstimationEnabled is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.costEstimationEnabled)
                || (has(self.initProvider) && has(self.initProvider.costEstimationEnabled))'
            - message: spec.forProvider.identifier is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.identifier)
                || (has(self.initProvider) && has(self.initProvider.identifier))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.provisionerType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.provisionerType)
                || (has(self.initProvider) && has(self.initProvider.provisionerType))'
            - message: spec.forProvider.provisionerVersion is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.provisionerVersion)
                || (has(self.initProvider) && has(self.initProvider.provisionerVersion))'
            - message: spec.forProvider.repository is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.repository)
                || (has(self.initProvider) && has(self.initProvider.repository))'
            - message: spec.forProvider.repositoryConnector is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.repositoryConnector)
                || (has(self.initProvider) && has(self.initProvider.repositoryConnector))'
            - message: spec.forProvider.repositoryPath is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.repositoryPath)
                || (has(self.initProvider) && has(self.initProvider.repositoryPath))'
          status:
            description: WorkspaceStatus defines the observed state of Workspace.
            properties:
              atProvider:
                properties:
                  connector:
                    description: |-
                      (Block Set) Provider connectors configured on the Workspace. Only one connector of a type is supported (see below for nested schema)
                      Provider connectors configured on the Workspace. Only one connector of a type is supported
                    items:
                      properties:
                        connectorRef:
                          description: |-
                            (String) Connector Ref is the reference to the connector
                            Connector Ref is the reference to the connector
                          type: string
                        type:
                          description: |-
                            (String) Type is the connector type of the connector. Supported types: aws, azure, gcp
                            Type is the connector type of the connector. Supported types: aws, azure, gcp
                          type: string
                      type: object
                    type: array
                  costEstimationEnabled:
                    description: |-
                      (Boolean) Cost estimation enabled determines if cost estimation operations are performed.
                      Cost estimation enabled determines if cost estimation operations are performed.
                    type: boolean
                  defaultPipelines:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Default pipelines associated with this workspace
                      Default pipelines associated with this workspace
                    type: object
                    x-kubernetes-map-type: granular
                  description:
                    description: |-
                      (String) Description of the resource.
                      Description of the resource.
                    type: string
                  environmentVariable:
                    description: |-
                      (Block Set) Environment variables configured on the workspace (see below for nested schema)
                      Environment variables configured on the workspace
                    items:
                      properties:
                        key:
                          description: |-
                            (String) Key is the identifier for the variable. Must be unique within the workspace.
                            Key is the identifier for the variable. Must be unique within the workspace.
                          type: string
                        value:
                          description: |-
                            (String) Value is the value of the variable. For string value types this field should contain the value of the variable. For secret value types this should contain a reference to a valid harness secret.
                            Value is the value of the variable. For string value types this field should contain the value of the variable. For secret value types this should contain a reference to a valid harness secret.
                          type: string
                        valueType:
                          description: |-
                            (String) Value type indicates the value type of the variable. Currently we support string and secret.
                            Value type indicates the value type of the variable. Currently we support string and secret.
                          type: string
                      type: object
                    type: array
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  identifier:
                    description: |-
                      (String) Unique identifier of the resource.
                      Unique identifier of the resource.
                    type: string
                  name:
                    description: |-
                      (String) Name of the resource.
                      Name of the resource.
                    type: string
                  orgId:
                    description: |-
                      (String) Unique identifier of the organization.
                      Unique identifier of the organization.
                    type: string
                  projectId:
                    description: |-
                      (String) Unique identifier of the project.
                      Unique identifier of the project.
                    type: string
                  providerConnector:
                    description: |-
                      (String) Provider connector is the reference to the connector for the infrastructure provider
                      Provider connector is the reference to the connector for the infrastructure provider
                    type: string
                  provisionerType:
                    description: |-
                      (string) provisioner type defines the provisioning tool to use (provider or opentofu)
                      provisioner type defines the provisioning tool to use (provider or opentofu)
                    type: string
                  provisionerVersion:
                    description: |-
                      (String) Provisioner version defines the provisioner version to use.5.7.
                      Provisioner version defines the provisioner version to use.5.7.
                    type: string
                  repository:
                    description: |-
                      (String) Repository is the name of the repository to fetch the code from.
                      Repository is the name of the repository to fetch the code from.
                    type: string
                  repositoryBranch:
                    description: |-
                      (String) Repository branch is the name of the branch to fetch the code from. This cannot be set if repository commit or sha is set.
                      Repository branch is the name of the branch to fetch the code from. This cannot be set if repository commit or sha is set.
                    type: string
                  repositoryCommit:
                    description: |-
                      (String) Repository commit is tag to fetch the code from. This cannot be set if repository branch or sha is set.
                      Repository commit is tag to fetch the code from. This cannot be set if repository branch or sha is set.
                    type: string
                  repositoryConnector:
                    description: |-
                      (String) Repository connector is the reference to the connector used to fetch the code.
                      Repository connector is the reference to the connector used to fetch the code.
                    type: string
                  repositoryPath:
                    description: |-
                      (String) Repository path is the path in which the code resides.
                      Repository path is the path in which the code resides.
                    type: string
                  repositorySha:
                    description: |-
                      (String) Repository commit is commit SHA to fetch the code from. This cannot be set if repository branch or commit is set.
                      Repository commit is commit SHA to fetch the code from. This cannot be set if repository branch or commit is set.
                    type: string
                  runAll:
                    description: |-
                      all terragrunt modules
                      Boolean flag for run-all terragrunt modules
                    type: boolean
                  tags:
                    description: |-
                      (Set of String) Tags to associate with the resource.
                      Tags to associate with the resource.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  terraformVariable:
                    items:
                      properties:
                        key:
                          description: |-
                            (String) Key is the identifier for the variable. Must be unique within the workspace.
                            Key is the identifier for the variable. Must be unique within the workspace.
                          type: string
                        value:
                          description: |-
                            (String) Value is the value of the variable. For string value types this field should contain the value of the variable. For secret value types this should contain a reference to a valid harness secret.
                            Value is the value of the variable. For string value types this field should contain the value of the variable. For secret value types this should contain a reference to a valid harness secret.
                          type: string
                        valueType:
                          description: |-
                            (String) Value type indicates the value type of the variable. Currently we support string and secret.
                            Value type indicates the value type of the variable. Currently we support string and secret.
                          type: string
                      type: object
                    type: array
                  terraformVariableFile:
                    description: |-
                      (block set) provider variables files configured on the workspace (see below for nested schema)
                      provider variables files configured on the workspace
                    items:
                      properties:
                        repository:
                          description: |-
                            (String) Repository is the name of the repository to fetch the code from.
                            Repository is the name of the repository to fetch the code from.
                          type: string
                        repositoryBranch:
                          description: |-
                            (String) Repository branch is the name of the branch to fetch the code from. This cannot be set if repository commit or sha is set.
                            Repository branch is the name of the branch to fetch the variables from. This cannot be set if repository commit or sha is set
                          type: string
                        repositoryCommit:
                          description: |-
                            (String) Repository commit is tag to fetch the code from. This cannot be set if repository branch or sha is set.
                            Repository commit is tag to fetch the variables from. This cannot be set if repository branch or sha is set.
                          type: string
                        repositoryConnector:
                          description: |-
                            (String) Repository connector is the reference to the connector used to fetch the code.
                            Repository connector is the reference to the connector used to fetch the variables.
                          type: string
                        repositoryPath:
                          description: |-
                            (String) Repository path is the path in which the code resides.
                            Repository path is the path in which the variables reside.
                          type: string
                        repositorySha:
                          description: |-
                            (String) Repository commit is commit SHA to fetch the code from. This cannot be set if repository branch or commit is set.
                            Repository commit is SHA to fetch the variables from. This cannot be set if repository branch or commit is set.
                          type: string
                      type: object
                    type: array
                  terragruntProvider:
                    description: |-
                      (Boolean) Set to true to enable Terragrunt mode
                      Set to true to enable Terragrunt mode
                    type: boolean
                  terragruntVersion:
                    description: |-
                      (String) Terragrunt version to use (e.g., 0.45.0)
                      Terragrunt version to use (e.g., 0.45.0)
                    type: string
                  variableSets:
                    description: |-
                      (List of String) Variable sets to use.
                      Variable sets to use.
                    items:
                      type: string
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
