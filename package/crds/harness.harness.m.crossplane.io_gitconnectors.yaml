---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: gitconnectors.harness.harness.m.crossplane.io
spec:
  group: harness.harness.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - harness
    kind: GitConnector
    listKind: GitConnectorList
    plural: gitconnectors
    singular: gitconnector
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GitConnector is the Schema for the GitConnectors API. Resource
          for creating a git connector
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GitConnectorSpec defines the desired state of GitConnector
            properties:
              forProvider:
                properties:
                  branch:
                    description: |-
                      (String) The branch of the git connector to use
                      The branch of the git connector to use
                    type: string
                  commitDetails:
                    description: |-
                      (Block Set, Max: 1) Custom details to use when making commits using this git connector (see below for nested schema)
                      Custom details to use when making commits using this git connector
                    items:
                      properties:
                        authorEmailId:
                          description: |-
                            (String) The email id of the author
                            The email id of the author
                          type: string
                        authorName:
                          description: |-
                            (String) The name of the author
                            The name of the author
                          type: string
                        message:
                          description: |-
                            (String) Commit message
                            Commit message
                          type: string
                      type: object
                    type: array
                  delegateSelectors:
                    description: |-
                      (Set of String) Delegate selectors to apply to this git connector.
                      Delegate selectors to apply to this git connector.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  generateWebhookUrl:
                    description: |-
                      (Boolean) Boolean indicating whether or not to generate a webhook url.
                      Boolean indicating whether or not to generate a webhook url.
                    type: boolean
                  name:
                    description: |-
                      (String) Name of the git connector.
                      Name of the git connector.
                    type: string
                  passwordSecretId:
                    description: |-
                      (String) The id of the secret for connecting to the git repository.
                      The id of the secret for connecting to the git repository.
                    type: string
                  sshSettingId:
                    description: |-
                      (String) The id of the SSH secret to use
                      The id of the SSH secret to use
                    type: string
                  url:
                    description: |-
                      (String) The URL of the git repository or account/organization
                      The URL of the git repository or account/organization
                    type: string
                  urlType:
                    description: |-
                      (String) The type of git url being used. Options are ACCOUNT, and REPO.
                      The type of git url being used. Options are `ACCOUNT`, and `REPO.`
                    type: string
                  usageScope:
                    description: |-
                      (Block Set) This block is used for scoping the resource to a specific set of applications or environments. (see below for nested schema)
                      This block is used for scoping the resource to a specific set of applications or environments.
                    items:
                      properties:
                        applicationId:
                          description: |-
                            (String) Id of the application to scope to. If empty then this scope applies to all applications.
                            Id of the application to scope to. If empty then this scope applies to all applications.
                          type: string
                        environmentFilterType:
                          description: |-
                            (String) Type of environment filter applied. Cannot be used with environment_id. Valid options are NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                            Type of environment filter applied. Cannot be used with `environment_id`. Valid options are NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                          type: string
                        environmentId:
                          description: |-
                            (String) Id of the id of the specific environment to scope to. Cannot be used with environment_filter_type.
                            Id of the id of the specific environment to scope to. Cannot be used with `environment_filter_type`.
                          type: string
                      type: object
                    type: array
                  username:
                    description: |-
                      (String) The name of the user used to connect to the git repository
                      The name of the user used to connect to the git repository
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  branch:
                    description: |-
                      (String) The branch of the git connector to use
                      The branch of the git connector to use
                    type: string
                  commitDetails:
                    description: |-
                      (Block Set, Max: 1) Custom details to use when making commits using this git connector (see below for nested schema)
                      Custom details to use when making commits using this git connector
                    items:
                      properties:
                        authorEmailId:
                          description: |-
                            (String) The email id of the author
                            The email id of the author
                          type: string
                        authorName:
                          description: |-
                            (String) The name of the author
                            The name of the author
                          type: string
                        message:
                          description: |-
                            (String) Commit message
                            Commit message
                          type: string
                      type: object
                    type: array
                  delegateSelectors:
                    description: |-
                      (Set of String) Delegate selectors to apply to this git connector.
                      Delegate selectors to apply to this git connector.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  generateWebhookUrl:
                    description: |-
                      (Boolean) Boolean indicating whether or not to generate a webhook url.
                      Boolean indicating whether or not to generate a webhook url.
                    type: boolean
                  name:
                    description: |-
                      (String) Name of the git connector.
                      Name of the git connector.
                    type: string
                  passwordSecretId:
                    description: |-
                      (String) The id of the secret for connecting to the git repository.
                      The id of the secret for connecting to the git repository.
                    type: string
                  sshSettingId:
                    description: |-
                      (String) The id of the SSH secret to use
                      The id of the SSH secret to use
                    type: string
                  url:
                    description: |-
                      (String) The URL of the git repository or account/organization
                      The URL of the git repository or account/organization
                    type: string
                  urlType:
                    description: |-
                      (String) The type of git url being used. Options are ACCOUNT, and REPO.
                      The type of git url being used. Options are `ACCOUNT`, and `REPO.`
                    type: string
                  usageScope:
                    description: |-
                      (Block Set) This block is used for scoping the resource to a specific set of applications or environments. (see below for nested schema)
                      This block is used for scoping the resource to a specific set of applications or environments.
                    items:
                      properties:
                        applicationId:
                          description: |-
                            (String) Id of the application to scope to. If empty then this scope applies to all applications.
                            Id of the application to scope to. If empty then this scope applies to all applications.
                          type: string
                        environmentFilterType:
                          description: |-
                            (String) Type of environment filter applied. Cannot be used with environment_id. Valid options are NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                            Type of environment filter applied. Cannot be used with `environment_id`. Valid options are NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                          type: string
                        environmentId:
                          description: |-
                            (String) Id of the id of the specific environment to scope to. Cannot be used with environment_filter_type.
                            Id of the id of the specific environment to scope to. Cannot be used with `environment_filter_type`.
                          type: string
                      type: object
                    type: array
                  username:
                    description: |-
                      (String) The name of the user used to connect to the git repository
                      The name of the user used to connect to the git repository
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.url is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.url)
                || (has(self.initProvider) && has(self.initProvider.url))'
            - message: spec.forProvider.urlType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.urlType)
                || (has(self.initProvider) && has(self.initProvider.urlType))'
          status:
            description: GitConnectorStatus defines the observed state of GitConnector.
            properties:
              atProvider:
                properties:
                  branch:
                    description: |-
                      (String) The branch of the git connector to use
                      The branch of the git connector to use
                    type: string
                  commitDetails:
                    description: |-
                      (Block Set, Max: 1) Custom details to use when making commits using this git connector (see below for nested schema)
                      Custom details to use when making commits using this git connector
                    items:
                      properties:
                        authorEmailId:
                          description: |-
                            (String) The email id of the author
                            The email id of the author
                          type: string
                        authorName:
                          description: |-
                            (String) The name of the author
                            The name of the author
                          type: string
                        message:
                          description: |-
                            (String) Commit message
                            Commit message
                          type: string
                      type: object
                    type: array
                  createdAt:
                    description: |-
                      (String) The time the git connector was created
                      The time the git connector was created
                    type: string
                  delegateSelectors:
                    description: |-
                      (Set of String) Delegate selectors to apply to this git connector.
                      Delegate selectors to apply to this git connector.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  generateWebhookUrl:
                    description: |-
                      (Boolean) Boolean indicating whether or not to generate a webhook url.
                      Boolean indicating whether or not to generate a webhook url.
                    type: boolean
                  id:
                    description: (String) Id of the git connector.
                    type: string
                  name:
                    description: |-
                      (String) Name of the git connector.
                      Name of the git connector.
                    type: string
                  passwordSecretId:
                    description: |-
                      (String) The id of the secret for connecting to the git repository.
                      The id of the secret for connecting to the git repository.
                    type: string
                  sshSettingId:
                    description: |-
                      (String) The id of the SSH secret to use
                      The id of the SSH secret to use
                    type: string
                  url:
                    description: |-
                      (String) The URL of the git repository or account/organization
                      The URL of the git repository or account/organization
                    type: string
                  urlType:
                    description: |-
                      (String) The type of git url being used. Options are ACCOUNT, and REPO.
                      The type of git url being used. Options are `ACCOUNT`, and `REPO.`
                    type: string
                  usageScope:
                    description: |-
                      (Block Set) This block is used for scoping the resource to a specific set of applications or environments. (see below for nested schema)
                      This block is used for scoping the resource to a specific set of applications or environments.
                    items:
                      properties:
                        applicationId:
                          description: |-
                            (String) Id of the application to scope to. If empty then this scope applies to all applications.
                            Id of the application to scope to. If empty then this scope applies to all applications.
                          type: string
                        environmentFilterType:
                          description: |-
                            (String) Type of environment filter applied. Cannot be used with environment_id. Valid options are NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                            Type of environment filter applied. Cannot be used with `environment_id`. Valid options are NON_PRODUCTION_ENVIRONMENTS, PRODUCTION_ENVIRONMENTS.
                          type: string
                        environmentId:
                          description: |-
                            (String) Id of the id of the specific environment to scope to. Cannot be used with environment_filter_type.
                            Id of the id of the specific environment to scope to. Cannot be used with `environment_filter_type`.
                          type: string
                      type: object
                    type: array
                  username:
                    description: |-
                      (String) The name of the user used to connect to the git repository
                      The name of the user used to connect to the git repository
                    type: string
                  webhookUrl:
                    description: |-
                      (String) The generated webhook url
                      The generated webhook url
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
