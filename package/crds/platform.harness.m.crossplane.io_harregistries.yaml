---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: harregistries.platform.harness.m.crossplane.io
spec:
  group: platform.harness.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - harness
    kind: HarRegistry
    listKind: HarRegistryList
    plural: harregistries
    singular: harregistry
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HarRegistry is the Schema for the HarRegistrys API. Resource
          for creating and managing Harness Registries.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HarRegistrySpec defines the desired state of HarRegistry
            properties:
              forProvider:
                properties:
                  allowedPattern:
                    description: |-
                      (List of String) Allowed artifact patterns
                      Allowed artifact patterns
                    items:
                      type: string
                    type: array
                  blockedPattern:
                    description: |-
                      (List of String) Blocked artifact patterns
                      Blocked artifact patterns
                    items:
                      type: string
                    type: array
                  config:
                    description: |-
                      (Block List, Max: 1) Configuration for the registry (see below for nested schema)
                      Configuration for the registry
                    items:
                      properties:
                        auth:
                          description: |-
                            (Block List, Max: 1) Authentication configuration for UPSTREAM registry type (see below for nested schema)
                            Authentication configuration for UPSTREAM registry type
                          items:
                            properties:
                              accessKeyIdentifier:
                                description: (String)
                                type: string
                              accessKeySecretPath:
                                description: (String)
                                type: string
                              accessKeySecretRef:
                                description: (String, Sensitive)
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                              authType:
                                description: |-
                                  (String) Type of authentication for UPSTREAM registry type (UserPassword, Anonymous, AccessKeySecretKey)
                                  Type of authentication (UserPassword, Anonymous).
                                type: string
                              secretIdentifier:
                                description: |-
                                  (String) Secret identifier for UserPassword auth type
                                  Secret identifier for UserPassword auth type. The secret must be stored in Harness Secret Manager.
                                type: string
                              secretKeyIdentifier:
                                description: (String)
                                type: string
                              secretKeySecretPath:
                                description: (String)
                                type: string
                              secretSpacePath:
                                description: |-
                                  (String) Secret space path for UserPassword auth type
                                  Secret space path for UserPassword auth type
                                type: string
                              userName:
                                description: |-
                                  (String) Username for UserPassword auth type
                                  User name for UserPassword auth type
                                type: string
                            type: object
                          type: array
                        authType:
                          description: |-
                            (String) Type of authentication for UPSTREAM registry type (UserPassword, Anonymous, AccessKeySecretKey)
                            Type of authentication for UPSTREAM registry type (UserPassword, Anonymous)
                          type: string
                        source:
                          description: |-
                            (String) Upstream source
                            Upstream source
                          type: string
                        type:
                          description: |-
                            (String) Type of registry (VIRTUAL or UPSTREAM)
                            Type of registry (VIRTUAL or UPSTREAM)
                          type: string
                        upstreamProxies:
                          description: |-
                            (List of String) List of upstream proxies for VIRTUAL registry type
                            List of upstream proxies for VIRTUAL registry type
                          items:
                            type: string
                          type: array
                        url:
                          description: |-
                            (String) URL of the registry
                            URL of the upstream (required if type=UPSTREAM & package_type=HELM)
                          type: string
                      type: object
                    type: array
                  description:
                    description: |-
                      (String) Description of the registry
                      Description of the registry
                    type: string
                  identifier:
                    description: |-
                      (String) Unique identifier of the registry
                      Unique identifier of the registry
                    type: string
                  packageType:
                    description: |-
                      (String) Type of package (DOCKER, HELM, MAVEN, etc.)
                      Type of package (DOCKER, HELM, MAVEN, etc.)
                    type: string
                  parentRef:
                    description: |-
                      (String) Parent reference for the registry
                      Parent reference for the registry
                    type: string
                  spaceRef:
                    description: |-
                      (String) Space reference for the registry
                      Space reference for the registry
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  allowedPattern:
                    description: |-
                      (List of String) Allowed artifact patterns
                      Allowed artifact patterns
                    items:
                      type: string
                    type: array
                  blockedPattern:
                    description: |-
                      (List of String) Blocked artifact patterns
                      Blocked artifact patterns
                    items:
                      type: string
                    type: array
                  config:
                    description: |-
                      (Block List, Max: 1) Configuration for the registry (see below for nested schema)
                      Configuration for the registry
                    items:
                      properties:
                        auth:
                          description: |-
                            (Block List, Max: 1) Authentication configuration for UPSTREAM registry type (see below for nested schema)
                            Authentication configuration for UPSTREAM registry type
                          items:
                            properties:
                              accessKeyIdentifier:
                                description: (String)
                                type: string
                              accessKeySecretPath:
                                description: (String)
                                type: string
                              accessKeySecretRef:
                                description: (String, Sensitive)
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                              authType:
                                description: |-
                                  (String) Type of authentication for UPSTREAM registry type (UserPassword, Anonymous, AccessKeySecretKey)
                                  Type of authentication (UserPassword, Anonymous).
                                type: string
                              secretIdentifier:
                                description: |-
                                  (String) Secret identifier for UserPassword auth type
                                  Secret identifier for UserPassword auth type. The secret must be stored in Harness Secret Manager.
                                type: string
                              secretKeyIdentifier:
                                description: (String)
                                type: string
                              secretKeySecretPath:
                                description: (String)
                                type: string
                              secretSpacePath:
                                description: |-
                                  (String) Secret space path for UserPassword auth type
                                  Secret space path for UserPassword auth type
                                type: string
                              userName:
                                description: |-
                                  (String) Username for UserPassword auth type
                                  User name for UserPassword auth type
                                type: string
                            type: object
                          type: array
                        authType:
                          description: |-
                            (String) Type of authentication for UPSTREAM registry type (UserPassword, Anonymous, AccessKeySecretKey)
                            Type of authentication for UPSTREAM registry type (UserPassword, Anonymous)
                          type: string
                        source:
                          description: |-
                            (String) Upstream source
                            Upstream source
                          type: string
                        type:
                          description: |-
                            (String) Type of registry (VIRTUAL or UPSTREAM)
                            Type of registry (VIRTUAL or UPSTREAM)
                          type: string
                        upstreamProxies:
                          description: |-
                            (List of String) List of upstream proxies for VIRTUAL registry type
                            List of upstream proxies for VIRTUAL registry type
                          items:
                            type: string
                          type: array
                        url:
                          description: |-
                            (String) URL of the registry
                            URL of the upstream (required if type=UPSTREAM & package_type=HELM)
                          type: string
                      type: object
                    type: array
                  description:
                    description: |-
                      (String) Description of the registry
                      Description of the registry
                    type: string
                  identifier:
                    description: |-
                      (String) Unique identifier of the registry
                      Unique identifier of the registry
                    type: string
                  packageType:
                    description: |-
                      (String) Type of package (DOCKER, HELM, MAVEN, etc.)
                      Type of package (DOCKER, HELM, MAVEN, etc.)
                    type: string
                  parentRef:
                    description: |-
                      (String) Parent reference for the registry
                      Parent reference for the registry
                    type: string
                  spaceRef:
                    description: |-
                      (String) Space reference for the registry
                      Space reference for the registry
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.identifier is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.identifier)
                || (has(self.initProvider) && has(self.initProvider.identifier))'
            - message: spec.forProvider.packageType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.packageType)
                || (has(self.initProvider) && has(self.initProvider.packageType))'
            - message: spec.forProvider.parentRef is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.parentRef)
                || (has(self.initProvider) && has(self.initProvider.parentRef))'
            - message: spec.forProvider.spaceRef is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.spaceRef)
                || (has(self.initProvider) && has(self.initProvider.spaceRef))'
          status:
            description: HarRegistryStatus defines the observed state of HarRegistry.
            properties:
              atProvider:
                properties:
                  allowedPattern:
                    description: |-
                      (List of String) Allowed artifact patterns
                      Allowed artifact patterns
                    items:
                      type: string
                    type: array
                  blockedPattern:
                    description: |-
                      (List of String) Blocked artifact patterns
                      Blocked artifact patterns
                    items:
                      type: string
                    type: array
                  config:
                    description: |-
                      (Block List, Max: 1) Configuration for the registry (see below for nested schema)
                      Configuration for the registry
                    items:
                      properties:
                        auth:
                          description: |-
                            (Block List, Max: 1) Authentication configuration for UPSTREAM registry type (see below for nested schema)
                            Authentication configuration for UPSTREAM registry type
                          items:
                            properties:
                              accessKeyIdentifier:
                                description: (String)
                                type: string
                              accessKeySecretPath:
                                description: (String)
                                type: string
                              authType:
                                description: |-
                                  (String) Type of authentication for UPSTREAM registry type (UserPassword, Anonymous, AccessKeySecretKey)
                                  Type of authentication (UserPassword, Anonymous).
                                type: string
                              secretIdentifier:
                                description: |-
                                  (String) Secret identifier for UserPassword auth type
                                  Secret identifier for UserPassword auth type. The secret must be stored in Harness Secret Manager.
                                type: string
                              secretKeyIdentifier:
                                description: (String)
                                type: string
                              secretKeySecretPath:
                                description: (String)
                                type: string
                              secretSpacePath:
                                description: |-
                                  (String) Secret space path for UserPassword auth type
                                  Secret space path for UserPassword auth type
                                type: string
                              userName:
                                description: |-
                                  (String) Username for UserPassword auth type
                                  User name for UserPassword auth type
                                type: string
                            type: object
                          type: array
                        authType:
                          description: |-
                            (String) Type of authentication for UPSTREAM registry type (UserPassword, Anonymous, AccessKeySecretKey)
                            Type of authentication for UPSTREAM registry type (UserPassword, Anonymous)
                          type: string
                        source:
                          description: |-
                            (String) Upstream source
                            Upstream source
                          type: string
                        type:
                          description: |-
                            (String) Type of registry (VIRTUAL or UPSTREAM)
                            Type of registry (VIRTUAL or UPSTREAM)
                          type: string
                        upstreamProxies:
                          description: |-
                            (List of String) List of upstream proxies for VIRTUAL registry type
                            List of upstream proxies for VIRTUAL registry type
                          items:
                            type: string
                          type: array
                        url:
                          description: |-
                            (String) URL of the registry
                            URL of the upstream (required if type=UPSTREAM & package_type=HELM)
                          type: string
                      type: object
                    type: array
                  createdAt:
                    description: |-
                      (String) Creation timestamp
                      Creation timestamp
                    type: string
                  description:
                    description: |-
                      (String) Description of the registry
                      Description of the registry
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  identifier:
                    description: |-
                      (String) Unique identifier of the registry
                      Unique identifier of the registry
                    type: string
                  packageType:
                    description: |-
                      (String) Type of package (DOCKER, HELM, MAVEN, etc.)
                      Type of package (DOCKER, HELM, MAVEN, etc.)
                    type: string
                  parentRef:
                    description: |-
                      (String) Parent reference for the registry
                      Parent reference for the registry
                    type: string
                  spaceRef:
                    description: |-
                      (String) Space reference for the registry
                      Space reference for the registry
                    type: string
                  url:
                    description: |-
                      (String) URL of the registry
                      URL of the registry
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
